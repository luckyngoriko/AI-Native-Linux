{
  "project_goal": "AIOS — AI-native Linux distribution. Implement the spec contracts from rev.2 (53 sub-specs CONTRACT-grade) into working Rust + Python + KDE code, following §22 MVP golden path discipline.",
  "current_milestone": "M11 opened",
  "next_milestone": "M11 — aios-cognitive (ongoing)",
  "current_task_id": "T-097",
  "current_task_title": "T-097 completed — aios-cognitive Model Router (S13.2) — ModelRouter + RouterState + 20 integration tests. Committed 6778042.",
  "current_step_T097": "T-097 completed — S13.2 deterministic precedence-table ModelRouter (13-rule table, S13.2 §C4 determinism) + RouterState (INV-014 health tracking via rolling observation window) + InMemoryCognitiveCore router integration (with_router ctor). New files: router.rs (468 lines, ModelRouter + RoutingRule + build_decision + route with 13 precedence rules), router_state.rs (149 lines, RouterState + BackendHealthWindow + observe_invocation/get_health/set_health/mint_routing_id). Tests: tests/router.rs (20 tests). InMemoryCognitiveCore now has optional router/router_state fields + with_router() ctor; translate_intent routes via router when configured, falls back to T-095 stub otherwise. Translator version bumped to 0.1.0-T097. Clippy fixes applied: (router.rs) too_many_arguments/unused_self/unnecessary_wraps/redundant_closure on build_decision; (router_state.rs) cast_precision_loss on error_rate, significant_drop_tightening on observe_invocation/set_health; (tests/router.rs) doc_markdown backticks, dead_code on suspended_entry, missing_const_for_fn on routing_inputs, unwrap_used → expect. All 3 quality gates PASS: clippy -D warnings (0 warnings), cargo test --workspace (zero failures), cargo fmt --check (clean). Commit 6778042.",
  "current_step_T096": "T-096 completed — LatencyClassifier with stub heuristic classification per S1.2. New latency_classifier.rs with LatencyClassifier unit struct, new_with_defaults() ctor, classify() method with 3 guard rules (recovery→T1, CLASSIFIED→T2, SECRET_BEARING→T3), stub_derive_tier() using word count + estimated tokens + multi-action detection, helper functions min_tier/parse_privacy_class/stub_is_multi_action. Updated lib.rs with pub mod latency_classifier + re-export LatencyClassifier. 16 inline unit tests + 16 integration tests in tests/latency_classifier.rs. Key fix: raised word_count threshold from 50→100 in stub_is_multi_action so 80-word intents reach T3 (not false-positive multi-action → T4). Clippy fixes: const fn new_with_defaults/min_tier, #[must_use] on classify, removed &self from stub_derive_tier (unused_self), backticks on SECRET_BEARING doc, removed duplicate SENSITIVE match arm, expect() with allow in test module. All 4 gates PASS: cargo check (clean), test (1842 total, +32 over T-095 1810), clippy -D warnings (0 warnings), fmt --check (clean).",
  "current_step_T095": "T-095 completed — CognitiveCore async trait + InMemoryCognitiveCore. 6 scope items: (1) core.rs with CognitiveCore trait (3 methods: translate_intent, list_supported_intents, get_translation) + TranslationContext (7 fields) + IntentCapability (5 fields), (2) in_memory_core.rs with InMemoryCognitiveCore (RwLock<HashMap> cache, Arc<Vec<CognitiveModel>> catalog, new/with_models ctors, all 3 trait methods), (3) lib.rs updated with pub mod core + pub mod in_memory_core + re-exports, (4) Cargo.toml updated with tokio (macros,rt,sync) + async-trait workspace deps, (5) tests/cognitive_core.rs with 14 tests (new/valid_intent/determinism/version/ai_subject_envelope/get_translation/unknown_intent/list/with_models/serde_roundtrips_x2/trait_dyn/concurrent_3_tasks/privacy_routing), (6) INV-002 enforced: translate_intent always produces typed ActionEnvelope (is_ai=true), never raw shell commands. SPEC DEVIATION: get_translation unknown intent_id → CognitiveError::NoMatchingCapability (not IntentNotFound — error.rs frozen with 9 variants, no IntentNotFound exists). Stub routing picks deterministic backend from privacy class (Public/Internal→LocalCpu, Sensitive→LocalGpu, SecretBearing/Classified→FallbackRuleBased degraded). Provenance: translator_version=0.1.0-T095, model_used=stub, tokens 0/0, model_signed_response=None. All 4 gates PASS: cargo check (clean), test (1810 total, >=1810 min), clippy -D warnings (0 warnings), fmt --check (clean). Commit 2cc79cd.",
  "current_step_T094": "T-094 completed — aios-cognitive types-only crate skeleton for M11. 11 items from brief: (1) workspace member added, (2) Cargo.toml with workspace deps, (3) lib.rs with module decls + re-exports, (4-10) 7 module files (circuit.rs, error.rs, intent.rs, latency.rs, model.rs, routing.rs, translator.rs) with closed enums + specs, (11) tests/types_roundtrip.rs with 23 tests. SPEC DEVIATIONS: ModelBackendKind=8 variants (brief said 5, spec S13.2 §4), BackendHealthState=5 variants (brief said 4, spec S13.2 §9.1), CircuitState PascalCase wire form (spec S14.1 §6), ProviderClass variant names per spec. Clippy fixes applied: doc_markdown backticks on lib.rs module docs + test file needless_collect/float_cmp/default_trait_access. All 4 gates PASS: clippy (0 warnings), fmt (clean), tests (1796, above 1785+ lower bound), check (implicit). Commit d48a44c.", Added crates/aios-sgr/tests/mvp_service_graph.rs with 5 end-to-end SGR scenarios over the M3-M9 stack: multi-unit dependency boot through SgrCapabilityAdapter, 3-unit SCC cycle detection, failed-unit isolation, recovery pause/resume, and adapter fail-closed mismatch. Added tests/acceptance_fixtures.rs with 12 S15.1/S15.2/S15.3 fixtures and tests/m10_closure.rs with 10 closure invariants. Bumped aios-sgr Cargo.toml 0.0.1 -> 0.1.0 and DEFAULT_CODE_VERSION -> 0.1.0-T093. Added local dev-dependencies for closure-only composition with aios-policy, aios-fs, aios-vault, and aios-verification. Deferred surfaces documented without M10 debt: UnitManifest requires shim via adapter_target.requires, recovery_mode_allowed via labels/UnitKind::RecoveryTask, and DEPENDENCY_DECLARED folded to GRAPH_EVALUATED. Workspace tests 1748 -> 1775 (+27), aios-sgr package tests = 169. Required gates PASS: cargo check --workspace; cargo test -p aios-sgr; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check; cargo build -p aios-renderer-cli --bin aios; cargo bench -p aios-policy --no-run. M10 CLOSED; M11 ready.",
  "current_step_T092": "T-092 landed the SGR renderer/CLI bridge in aios-renderer-cli. Added aios-sgr path dependency and new src/sgr_render.rs with Renderable impls for ServiceUnit, UnitState, UnitKind, DependencyKind, GraphState, RegisteredAdapter, AdapterRegistrationState plus SgrUnitListView/SgrGraphView. AiosClient now has a public SgrServiceClient and convenience methods list_units, get_unit, traverse_graph, evaluate_graph. AiosEndpoints gained sgr, endpoint parsing accepts keyed sgr and positional policy,runtime,fs,vault,verification,recovery,sgr[,evidence]. InProcessBackend now starts SGR alongside the prior six services and service_count is 7. CLI adds aios sgr list/get/graph/evaluate; graph combines traverse_graph + evaluate_graph and renders as Tree. Tests: +14 in crates/aios-renderer-cli/tests/sgr_render.rs; workspace tests 1734 -> 1748. Required gates PASS: cargo check --workspace; cargo test -p aios-renderer-cli; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check; cargo build -p aios-renderer-cli --bin aios. Scope guard held: no closed aios-sgr/backend crate source changes; M10 closer T-093 deferred.",
  "current_step_T091": "T-091 landed aios-sgr cross-crate integration. Added aios-capability-runtime and aios-recovery path deps. Added runtime_adapter.rs with UnitActionFactory, DefaultUnitActionFactory, and SgrCapabilityAdapter over Arc<dyn ServiceGraph>, Arc<UnitFsmDriver>, Arc<dyn CapabilityRuntime>, and Arc<dyn UnitActionFactory>; start_unit_via_runtime and stop_unit_via_runtime build typed ActionEnvelope requests, submit through CapabilityRuntime, and drive UnitFsmDriver transitions to Running/Stopped or Failed. Added recovery_adapter.rs with SgrRecoveryHook implementing RuntimeRecoveryHook and pausing/resuming normal-mode Running units around recovery boundaries; recovery allowance uses labels.recovery_mode_allowed == true or UnitKind::RecoveryTask because UnitManifest is frozen and has no typed flag. Added tests/cross_crate_integration.rs with 15 tests covering runtime success/denial, envelope construction, recovery hook behavior, backward-compatible capability-runtime/recovery APIs, end-to-end SGR/runtime/evidence receipts, concurrent starts, factory error -> mark_failed, missing adapter_id, and stop. Workspace tests 1719 -> 1734. Required gates PASS: cargo check --workspace; cargo test -p aios-sgr; cargo test -p aios-capability-runtime; cargo test -p aios-recovery; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no closed crate source changes; renderer (T-092) and M10 closer (T-093) deferred.",
  "current_step_T084": "T-084 opened M10 with crates/aios-sgr v0.0.1 as the S15.1/S15.2/S15.3 typed core skeleton. Added workspace member after aios-renderer-cli. Modules: unit.rs (UnitId, UnitManifest, ServiceUnit, UnitKind, DesiredState, RestartPolicy, RestartBudget, HealthCheckKind, HealthCheckSpec, VerificationIntentRef, RollbackPointer, ResourceBudget/GpuBudget), state.rs (UnitState 11-state S15.1 FSM plus GraphState, GraphEvaluationResult, TransitionKind, DependencySolveResult, ABPromotionState, and S15.2 companion enums), dependency.rs (UnitDependency, DependencyEdge, DependencyKind 3-value S15.1 set), adapter.rs (S15.3 AdapterManifest, AdapterActionDeclaration, AdapterCapability, AdapterDeclaration, registration/capability/IO/dispatch/stability/failure/rollback enums), error.rs (SgrError taxonomy). Tests: crates/aios-sgr/tests/types_roundtrip.rs adds 15 tests; workspace tests 1592 -> 1607. Required gates PASS: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no ServiceGraph trait, no FSM driver, no graph evaluator, no adapter registry, no gRPC, no evidence emission, no cross-crate integration, no renderer.",
  "current_step_T085": "T-085 landed S15.1 ServiceGraph trait + InMemoryServiceGraph. Added graph.rs with async_trait ServiceGraph {register_unit, get_unit, list_units, declare_dependency, list_dependencies, graph_state, set_unit_state}. Added in_memory_graph.rs with tokio RwLock unit/dependency maps and flat trusted_authorities HashMap<String, VerifyingKey>. register_unit verifies publisher_root_id authority, Ed25519 signature over BLAKE3-256 digest of UnitManifest with publisher_signature/canonical_hash stripped, checks canonical_hash prefix, rejects duplicate UnitId via SgrError::UnitAlreadyRegistered, imports manifest dependency edges when targets are registered, and admits units at UnitState::Queued per S15.1 DRAFT->QUEUED admission semantics. set_unit_state validates the full S15.1 11-state transition table. declare_dependency rejects unknown source with UnitNotFound and unknown target with DependencyTargetNotRegistered; no cycle detection by design (T-087). graph_state derives Empty/Converging/Converged/Degraded/Failed with criticality label support for critical failed units. Tests: crates/aios-sgr/tests/graph.rs adds 18 tests; workspace tests 1607 -> 1625. Required gates PASS: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no FSM driver (T-086), no graph evaluator/cycle detection (T-087), no adapter registry (T-088), no gRPC (T-089), no evidence (T-090), no cross-crate integration (T-091), no renderer (T-092), no M10 closer (T-093).",
  "current_step_T086": "T-086 landed the SGR UnitFsmDriver. Added crates/aios-sgr/src/state_fsm.rs with UnitFsmDriver { graph: Arc<dyn ServiceGraph> }, TRANSITIONS const holding the exhaustive S15.1 §3.2.1 UnitState table consumed by S15.2, and public is_legal_transition. start/stop/restart/mark_failed/transition call the T-085 low-level set_unit_state for every edge; start and stop are idempotent for RUNNING/STOPPED; restart from FAILED uses the spec recovery edge FAILED -> STARTING -> RUNNING; direct FAILED -> RUNNING and FAILED -> STOPPED remain invalid per spec. Concurrent start is race-tolerant by re-reading after InvalidStateTransition caused by another caller advancing the unit. Tests: crates/aios-sgr/tests/state_fsm.rs adds 20 tests; workspace tests 1625 -> 1645. Required gates PASS: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Spec deviation from brief: brief requested FAILED -> STOPPED and STOPPED -> FAILED/mark_failed from stopped, but S15.1 §3.2.1 permits FAILED -> STARTING/RETIRED and STOPPED -> QUEUED/RETIRED only; TRUST SPEC applied. Scope guard held: no graph evaluator/cycle detection (T-087), no adapter registry (T-088), no gRPC (T-089), no evidence (T-090), no cross-crate integration (T-091), no renderer (T-092), no M10 closer (T-093).",
  "current_step_T087": "T-087 landed the S15.2 GraphEvaluator. Added crates/aios-sgr/src/evaluator.rs with GraphEvaluator { graph: Arc<dyn ServiceGraph> }, topological_sort via Kahn with alphabetical UnitId tie-break and DependencyCycleDetected on cycles, detect_cycles via iterative Tarjan SCC (no recursion; includes self-loop single-node SCCs; component root-pop order preserved), identify_parallel_batches, evaluate_readiness, is_converged, and convergence_state. Added crates/aios-sgr/tests/evaluator.rs with 19 tests: empty/linear/diamond topo, direct cycle topo error, 2-cycle, 3-cycle, linear no-cycle, two SCCs, self-loop, diamond/linear/empty batches, readiness true/false/no deps, empty convergence true, desired Running vs Stopped false, GraphState variants, and 100-unit linear topo under 100ms. Workspace tests 1645 -> 1664. Required gates PASS: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: adapter model (T-088), gRPC (T-089), evidence (T-090), cross-crate integration (T-091), renderer (T-092), and M10 closer (T-093) deferred.",
  "current_step_T088": "T-088 landed the S15.3 SGR-side adapter registry. Added crates/aios-sgr/src/adapter_registry.rs with SgrAdapterRegistry { adapters: RwLock<HashMap<String, RegisteredAdapter>>, trusted_authorities: HashMap<String, VerifyingKey> }, RegisteredAdapter, and 4-state AdapterRegistrationState {Pending, Active, Suspended, Retired}. register_adapter verifies AdapterCapability.manifest_signature_ed25519 with the declaration signing_key_id against trusted authorities and fails closed with ManifestSignatureInvalid/ManifestUnknownAuthority; identical duplicate capability registrations are idempotent, divergent duplicates reject. lookup/list/find_for_unit/suspend/retire implemented; find_for_unit sorts Active candidates by registered_at and requires one adapter to provide every required capability, returning None fail-closed on no/partial match so T-091 can reject the unit. Tests: crates/aios-sgr/tests/adapter_registry.rs adds 16 tests; workspace tests 1664 -> 1680. Required gates PASS: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Compatibility note: T-084 UnitManifest lacks typed requires and unit.rs was frozen, so find_for_unit reads adapter_target.requires until T-091/S15.1 wiring. Scope guard held: no gRPC (T-089), evidence (T-090), cross-crate integration (T-091), renderer (T-092), or M10 closer (T-093).",
  "current_step_T089": "T-089 landed the aios-sgr gRPC SgrService. Added crates/aios-sgr/proto/aios.sgr.v1alpha1.proto with the SgrService RPC list from the brief; build.rs mirrors aios-recovery tonic-build + vendored protoc; Cargo.toml adds tonic/prost/prost-types and build deps. Added src/service/mod.rs with include_proto, re-exports, and SCHEMA_VERSION=aios.sgr.v1alpha1+T089. Added conversions.rs for Rust<->proto mappings, JSON/timestamp helpers, AdapterDeclaration JSON preservation, and SgrError -> tonic::Status mapping. Added server.rs wrapping Arc<InMemoryServiceGraph>, Arc<UnitFsmDriver>, Arc<GraphEvaluator>, and Arc<SgrAdapterRegistry>. lib.rs exports service types and DEFAULT_CODE_VERSION=0.0.1-T089. Tests: crates/aios-sgr/tests/grpc_roundtrip.rs adds 25 tests, including direct service calls, conversion roundtrips, status-code mapping, and tonic loopback smoke. Workspace tests 1680 -> 1705. Required gates PASS: cargo check --workspace; cargo test -p aios-sgr; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no evidence emission (T-090), no cross-crate integration (T-091), no renderer (T-092), no M10 closer (T-093).",
  "current_step_T090": "T-090 landed SGR evidence emission. Added aios-evidence path dep; evidence_emit.rs with SgrEvidenceLog, InMemorySgrEvidenceLog, SgrEvidenceEmitter, SgrSubjectRef and AIOS_SGR_SUBJECT; evidence_payloads.rs with typed UNIT_* / DependencyDeclared / GraphConverged / AdapterRegistered payloads. Wired optional emitters into InMemoryServiceGraph, UnitFsmDriver, GraphEvaluator, and SgrAdapterRegistry; added SgrError::EvidenceEmitFailed and gRPC status mapping. RecordType mappings use dedicated S15.x variants where present; DependencyDeclared folds into GRAPH_EVALUATED because aios-evidence has no DEPENDENCY_DECLARED variant. Tests: crates/aios-sgr/tests/evidence_emission.rs adds 14 tests; workspace tests 1705 -> 1719. Required gates PASS: cargo check --workspace; cargo test -p aios-sgr; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no cross-crate runtime integration (T-091), no renderer (T-092), no M10 closer (T-093).",
  "current_step_T053": "T-053 landed aios-vault evidence emission for S5.2/S5.4 -> S3.1. Added crates/aios-vault/src/evidence_emit.rs with VaultEvidenceLog, InMemoryVaultEvidenceLog, VaultEvidenceEmitter, Ed25519 signing, and BLAKE3 ReceiptChain linkage. Added crates/aios-vault/src/evidence_payloads.rs with typed CapabilityIssued/Used/Revoked/Expired and OverrideGranted/Consumed/Revoked payloads. Cargo edge aios-vault -> aios-evidence added. InMemoryVaultBroker gained optional evidence_emitter + with_evidence_emitter and emits VAULT_CAPABILITY_ISSUED, VAULT_OPERATION for use, and VAULT_CAPABILITY_REVOKED on successful issue/use/revoke. InMemoryOverrideBroker gained optional emitter and emits OVERRIDE_GRANTED/CONSUMED/REVOKED on successful grant/consume/revoke. CapabilityLifecycleDriver gained optional emitter and emits expiration payloads as VAULT_OPERATION on expire_capability and run_expiration_pass transitions. VaultError gained EvidenceEmitFailed(String); service conversion maps it to INTERNAL for exhaustive matching. Tests: crates/aios-vault/tests/evidence_emission.rs adds 21 tests covering serde round-trips, record payloads, INV-015/INV-018 no plaintext/key/handle leaks, BLAKE3 chain coherence, Ed25519 signatures, no-emitter backward compatibility, and concurrent issue+use+revoke chain integrity. Workspace tests: 1105 -> 1126. Required gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Spec note: current S3.1 Rust RecordType vocabulary has VAULT_OPERATION, not CAPABILITY_USED or VAULT_CAPABILITY_EXPIRED; T-053 maps use and expiration payloads to VAULT_OPERATION. Scope guard held: no cross-crate vault↔policy/runtime integration, no M6 closer.",
  "current_step_T054": "T-054 landed cross-crate integration shims in aios-vault while preserving closed aios-policy and aios-capability-runtime surfaces. Added aios-policy path dependency to aios-vault. New crates/aios-vault/src/integration.rs implements From<HydratedSubjectSnapshot> for aios_policy::HydratedSubject, From<OverrideBinding> for aios_policy::override_boundary::EmergencyOverride, VaultPolicyHydrator implementing aios_policy::SubjectHydrator, and VaultPolicyOverrideBoundary standalone lookup shim over InMemoryOverrideBroker. InMemoryOverrideBroker gained read-only list_overrides() snapshot for shim lookup. Added crates/aios-vault/tests/cross_crate_integration.rs with 11 tests covering type conversion, LocalOperator policy mapping, hydrator canonical/session failure paths, kernel allow path with vault hydration, override shim hit/miss/expired behavior, and backward-compat commands. Workspace tests: 1126 -> 1137 (+11). Gates PASS: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Backward compat PASS: cargo test -p aios-policy --test subject_hydration; cargo test -p aios-capability-runtime --test adapter_registry. Scope guard held: no aios-policy/src changes, no aios-capability-runtime/src changes, no action/evidence/fs changes. Runtime vault-backed manifest verifier skipped as M7+ because adding it cleanly would require a new runtime verifier contract rather than a pure adapter_registry ctor.",
  "current_step_T055": "T-055 closed M6 for aios-vault v0.1.0. Added crates/aios-vault/tests/mvp_vault_integration.rs with 4 §22 vault-mediated scenarios: vault-brokered external model call via KEY_SIGN use-without-reveal, AI raw-secret policy denial plus encrypted-only vault output, STRONG_SOLO emergency override through VaultPolicyOverrideBoundary, and override anti-replay. Added crates/aios-vault/tests/acceptance_fixtures.rs with 10 S5.1/S5.2/S5.4 fixtures. Added crates/aios-vault/tests/m6_closure.rs with 9 invariants: version marker, DEFAULT_CODE_VERSION, no Unimplemented/todo/unimplemented, every RPC implemented, CapabilityClass/OverrideClass/CapabilityState coverage, and deferred-surface documentation. Added aios-capability-runtime as a dev-dependency only for the integration harness. Bumped aios-vault Cargo.toml 0.0.1 -> 0.1.0 and DEFAULT_CODE_VERSION -> aios-vault/0.1.0-T055. Workspace tests: 1137 -> 1160 (+23). Required gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check; cargo bench -p aios-policy --no-run. Spec note: current S3.1 Rust RecordType uses VAULT_OPERATION for capability use rather than CAPABILITY_USED; SECRET_GET reveal mechanics and vault-backed manifest verifier remain M7+ work, with no M6 debt carried forward.",
  "current_step_T056": "T-056 opened M7 with crates/aios-renderer-cli v0.0.1. Added workspace member after aios-vault; Cargo deps limited to serde, serde_json, chrono, strum, strum_macros, and thiserror. Added S7.6 §3 closed CLI enum vocabulary (CliRenderMode, CliCompilationResult, CliInputMode, AnsiSupportLevel, CliEvidenceRecordKind), crate-local OutputFormat {TEXT, JSON, TREE, TABLE}, RenderContext terminal/pipe defaults (NO_COLOR, TERM, COLUMNS; redact_secrets default true), Renderable trait, RenderError taxonomy, and primitive Renderable impls for String, &str, i64, u64, bool, Option, Vec, and BTreeMap. Tests: crates/aios-renderer-cli/tests/types_roundtrip.rs adds 12 tests; workspace 1160 -> 1172. Gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Spec note: S7.6 does not define OutputFormat; T-056 keeps it crate-local per task brief while representing the actual §3 spec enums separately. Scope guard held: no format-specific renderers, no cross-crate Renderable impls, no gRPC client, no CLI binary/clap.",
  "current_step_T057": "T-057 landed format-specific renderers on top of T-056 primitives. Added crates/aios-renderer-cli/src/text_renderer.rs (TextRenderer::render plus render_kv/render_section/render_list/color_for_state; secret-shaped key redaction in render_kv; ANSI state colors only when ctx.color). Added json_renderer.rs (JsonRenderer::new/new_pretty/render; serde_json serialization; ctx.redact_secrets recursive object/array walk redacts key_material/secret/password/token fields to <redacted>). Added table_renderer.rs (TableRenderer, TableSpec, TableAlign; fixed-width UTF-8 box drawing when ctx.color + UTF-8 locale, ASCII fallback otherwise, WidthOverflow guard against ctx.width). Added tree_renderer.rs (TreeRenderer, TreeNode; UTF-8 branch glyphs with ASCII fallback). lib.rs re-exports all four renderer modules. Tests: +28 in tests/text_renderer.rs (8), json_renderer.rs (8), table_renderer.rs (6), tree_renderer.rs (6); workspace 1172 -> 1200. Gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Note: first cargo test run hit known unrelated flaky aios-evidence uppercase-hex test; diagnosed root cause, targeted rerun passed, fresh full workspace rerun passed. Scope guard held: no output_format.rs/renderable.rs/error.rs/primitives.rs changes; no cross-crate Renderable, gRPC, or CLI binary.",
  "current_step_T058": "T-058 landed first cross-crate Renderable impls in aios-renderer-cli. Added path dependencies to aios-capability-runtime and aios-action. New src/action_render.rs implements Renderable for ActionContext, ActionLifecycleState, RollbackOutcome, ExecutionFailureReason, and ActionEnvelope across Text/Json/Tree/Table. ActionContext text uses TextRenderer key-value helpers; JSON uses serde_json serialization; tree shows Action root with status, dispatch_kind, queue_class, and evidence_chain count/head; table is one row action_id/status/adapter/duration_ms. State coloring maps Succeeded green, Failed/RollbackFailed/PolicyDenied red, OverrideDenied yellow, in-progress states blue, and strict terminals bold when ctx.color=true. Evidence chain intentionally renders only count + first 3 truncated receipt ids; full expansion deferred to T-059. Tests: +12 in crates/aios-renderer-cli/tests/action_render.rs; workspace 1200 -> 1212. Gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Spec note: current ActionEnvelope type has no action_id or risk flags, so the summary uses exposed identity/action/target/dry_run/parent_action_id/phase fields.",
  "current_step_T059": "T-059 landed evidence rendering in aios-renderer-cli. Added path dependency aios-evidence and new src/evidence_render.rs. Implements Renderable for EvidenceReceipt, RecordType, and EvidenceChainView across Text/Json/Tree/Table. Receipt text/tree summarize receipt_id, record_type, timestamp, subject, content hash, payload marker/hex, and signature marker/hex. Table uses receipt_id/type/timestamp/subject rows. JSON round-trips in debug-unredacted mode while renderer-level redaction replaces payload/signature with byte-count markers when ctx.redact_secrets=true. INV-015 tests prove AIOS_TEST_SECRET_MARKER does not leak in any format when redacted, and signatures show <64 bytes> instead of hex. INV-018 renderer-level scrub keeps key_material byte values out even in debug mode. Chain tree renders evidence_chain root, each receipt child, record_type leaf, and 12-char prev_receipt_hash link labels colored green for valid BLAKE3 links and red for mismatches via TextRenderer.color_for_state. Tests: +14 in crates/aios-renderer-cli/tests/evidence_render.rs; workspace 1212 -> 1226. Gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no aios-evidence changes, no policy/vault/fs Renderable, no gRPC clients, no CLI binary.",
  "current_step_T060": "T-060 landed policy/vault/fs rendering in aios-renderer-cli. Added path dependencies aios-policy, aios-vault, and aios-fs. New src/policy_render.rs implements Renderable for PolicyDecision, Decision, Constraints, and ApprovalRequirement across Text/Json/Tree/Table, with Allow green, Deny red, and RequireApproval yellow when ctx.color=true. New src/vault_render.rs implements Renderable for VaultCapability, CapabilityClass, CapabilityState, OverrideBinding, OverrideClass, and KeyMaterialHandle; INV-018 is enforced by rendering KeyMaterialHandle and VaultCapability handle surfaces only as <vault-handle>, including when ctx.redact_secrets=false, and tests scan for 32-hex key-shaped leaks. New src/fs_render.rs implements Renderable for Object, Version, Pointer, AiosPath, and NamespaceClass; Version content_hash is truncated to 12 chars and states color Active green, Purged red, Quarantined yellow. Tests: +19 in crates/aios-renderer-cli/tests/{policy_render,vault_render,fs_render}.rs; workspace 1226 -> 1245. Gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no aios-policy/aios-vault/aios-fs crate changes, no gRPC clients, no CLI binary.",
  "current_step_T061": "T-061 landed gRPC client wiring in aios-renderer-cli. Added tonic/prost/tokio runtime deps, src/client/{endpoint,aios_client,in_process}.rs, client module re-exports, and RenderError::{ClientConnectFailed,ClientCallFailed}. AiosEndpoints defines canonical localhost ports for policy/runtime/fs/vault plus optional evidence; AiosClient composes generated tonic clients for PolicyKernel, CapabilityRuntime, AiosFs, VaultBroker, and optional EvidenceLog, with convenience methods for submit_action, evaluate_policy, write_object, read_object, and list_capabilities. InProcessBackend::spawn_and_connect starts 4 ephemeral policy/runtime/fs/vault servers and returns AiosClient plus ShutdownHandle. Tests: +10 in crates/aios-renderer-cli/tests/client_integration.rs; workspace 1245 -> 1255. Gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no backend crate changes, no CLI binary.",
  "current_step_T062": "T-062 landed the `aios` CLI binary for aios-renderer-cli (§22 phase-9). Added workspace deps clap (derive+env) and anyhow, [[bin]] name=aios path=src/main.rs, new src/cli.rs with AiosCli flags format/no-color/no-redact/verbose/endpoints and subcommands action submit/status, fs read/list/list-versions, policy evaluate, vault list-capabilities/issue, evidence chain/get. AiosCli::execute builds RenderContext from flags, reads envelope JSON, dispatches through AiosClient, renders via Renderable, and returns stdout text for in-process tests. main.rs parses clap, resolves endpoints from --endpoints/AIOS_ENDPOINTS or localhost defaults, connects AiosClient, prints rendered output, formats errors as text or JSON, and exits 1 on runtime/render/client errors. AiosClient gained minimal methods for action_status, list_objects, list_versions, issue_capability, evidence_chain, and evidence_receipt. Tests: +10 in crates/aios-renderer-cli/tests/cli_integration.rs; workspace 1255 -> 1265. Gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check; cargo build -p aios-renderer-cli --bin aios. Scope guard held: no aios-policy/aios-cap-runtime/aios-fs/aios-vault/aios-evidence crate changes; T-063 M7 closer deferred.",
  "current_step_T063": "T-063 closed M7 for aios-renderer-cli v0.1.0 and moved the §22 MVP golden path from layer-by-layer proof to runnable CLI proof. Added tests/mvp_full_golden_path.rs covering boot documentation, mounted-system object stub, typed action submit/status, versioned AIOS-FS object read + list-versions, S2.1-style semantic list, policy Allow, vault capability issue/list, evidence chain CLI surface, and Text+JSON rendering. Added tests/acceptance_fixtures.rs with 8 S7.6/S7.2-derived fixtures. Added tests/m7_closure.rs with version/no-todo/binary/OutputFormat/subcommand/Renderable coverage invariants. Bumped aios-renderer-cli Cargo.toml 0.0.1 -> 0.1.0. Workspace baseline 1265 -> 1281 after new tests. Known MVP limitations documented in tests: L1 boot and /aios path mount remain M9 work; InProcessBackend intentionally starts 4 services and has no EvidenceLog endpoint, so the expected evidence record-type chain is rendered through a local EvidenceChainView stub while `aios evidence chain` remains runnable against the CLI surface.",
  "current_step_T064": "T-064 opened M8 with crates/aios-verification v0.0.1 as the S2.4 typed core skeleton. Added workspace member after aios-renderer-cli. Cargo deps: aios-action path plus serde, serde_json, chrono, strum, strum_macros, thiserror, ulid, blake3 from workspace. New modules: intent.rs with IntentId vrfi_<ULID> and VerificationIntent {intent_id, action_id, expression, expression_hash, created_at, timeout_seconds}; primitive.rs with 36-entry S2.4 Wave-15 VerificationPrimitive closed enum; result.rs with VerificationStatus {Passed, Failed, Timeout, ProbeError, Skipped} serialized as VERIFICATION_* tokens plus PrimitiveResult and VerificationResult; error.rs with VerificationError taxonomy. Tests: crates/aios-verification/tests/types_roundtrip.rs adds 11 round-trip/count/error/cross-crate tests. Workspace tests: 1281 -> 1292 confirmed by cargo test --workspace -- --list. Required gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Spec deviations from brief: status set, primitive vocabulary, and IntentId prefix follow S2.4 rather than brief examples. Scope guard held: no engine trait, primitive impls, parser, executor, gRPC, evidence emission, runtime integration, or renderer integration.",
  "current_step_T065": "T-065 landed VerificationEngine trait + VerificationContext and InMemoryVerificationEngine. Added crates/aios-verification/src/engine.rs with async_trait VerificationEngine {run_verification, list_primitives}. Added src/in_memory_engine.rs with Arc<RwLock<HashMap<IntentId, VerificationResult>>> completed-run cache, get_result, serde_json Vec<String> expression seam that maps invalid JSON to IntentParseFailed and unknown wire names to UnknownPrimitive, deterministic placeholder PrimitiveResult execution (passed=true, actual=expected, elapsed_ms=0), aggregate status helper, vrf_<ULID> result ids, and list_primitives over the 36-entry S2.4 vocabulary. Added tokio + async-trait deps and lib.rs exports. Tests: crates/aios-verification/tests/engine.rs adds 11 tests including Arc<dyn VerificationEngine>. Workspace tests: 1292 -> 1303 confirmed by cargo test --workspace -- --list. Required gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no primitive impls (T-066), no grammar parser (T-067), no executor aggregation, no gRPC, no evidence, no runtime or renderer integration.",
  "current_step_T066": "T-066 replaced T-065 all-pass primitive stubs with real three-tier execution for the frozen 36-entry S2.4 vocabulary. New crates/aios-verification/src/primitives/{mod,tier1,tier2,tier3}.rs. mod.rs documents tier classification, defines LocalProbe, StdLocalProbe, MockLocalProbe, MockProbeKey/MockProbeValue, ProbeVerdict, and result helpers. Tier1 covers pure deterministic helpers/payload predicates: json_field_eq helper, blake3 inline-bytes helper, regex helper, plus TreeMaxDepth/WebChromeZIndexAtLeast/NamespaceCatalogVersion/SubjectSessionFlagState/SecretPatternMatch execution when observed payload is supplied. Tier2 covers ServiceActive/Inactive via process_running, PackageInstalled via command_exit_code/dpkg-query, PortOpen/Closed, FileExists, FileHash, RepoExists, and WebRendererBoundTo through LocalProbe. StdLocalProbe uses std::fs, BLAKE3 file read, Linux /proc process scan, tokio::net::TcpListener bind for local TCP port detection, std::env, and std::process::Command. Tier3 returns deterministic ProbeError for HttpOk and all network/control-plane/cross-crate primitives deferred to M16/control-plane integration. InMemoryVerificationEngine now holds Arc<dyn LocalProbe>, adds with_local_probe, parses expression arrays of either legacy strings or minimal JSON invocation objects, dispatches by primitive_tier, and aggregates any per-primitive error to VerificationStatus::ProbeError. Added regex workspace dependency. Tests: primitives_tier1.rs (9), primitives_tier2.rs (8), primitives_tier3.rs (3); engine.rs updated for real behavior. Workspace tests: 1303 -> 1323 confirmed by cargo test --workspace -- --list. Required gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Spec deviations/notes: T-064 vocabulary has no JsonFieldEq/EnvVarEq/Blake3Matches/GrpcReachable top-level primitives, so those are helpers or omitted while trusting T-064. sysinfo was not added because its source was absent from the local cargo cache under no-network permissions; StdLocalProbe uses a deterministic Linux /proc process scan instead. No full grammar parser, executor aggregation, gRPC, evidence, runtime, or renderer integration.",
  "current_step_T067": "T-067 landed the S2.4 closed grammar parser for all/any/not/eventually expressions. Parser accepts full spec composition vocabulary with depth limiting, required arg validation, canonical primitive aliases, and duration literals. Workspace baseline noted by Governor: 1344 tests. Executor semantics were deferred to T-068.",
  "current_step_T068": "T-068 landed VerificationExecutor for parsed S2.4 grammar. Added crates/aios-verification/src/executor.rs with all/any/not/eventually dispatch, short-circuit placeholders, tokio::time::timeout per primitive, explicit S2.4 eventually max_duration/interval retry window, per-primitive elapsed_ms, retry_count annotation, and load-bearing ProbeError aggregation with not(...) suppression. InMemoryVerificationEngine now routes compiled grammar through VerificationExecutor while preserving flat JSON primitive-list compatibility via all(...). Added crates/aios-verification/tests/executor.rs with 14 combinator/timeout/retry/nesting/aggregation/determinism tests. Workspace tests: 1344 -> 1358. Gates PASS: cargo check --workspace; cargo test -p aios-verification; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no intent/result/primitive/grammar/error changes; no gRPC, evidence emission, cross-crate runtime integration, or renderer work.",
  "current_step_T069": "T-069 landed the aios-verification gRPC VerificationEngine service per S2.4 section 10. Added crates/aios-verification/proto/aios.verification.v1alpha1.proto with RunVerification, ExplainResult, and GetEngineInfo (spec RPC list; brief-only ListPrimitives/GetResult not implemented). Added build.rs mirroring the tonic-build + protoc-bin-vendored pattern from policy/runtime/fs/vault. Added src/service/{mod,conversions,server}.rs with tonic::include_proto!, SCHEMA_VERSION=aios.verification.v1alpha1+T069, Rust<->proto conversions for VerificationIntent/Result/Status/PrimitiveResult/Primitive, ActionId placeholder bytes, chrono Timestamp conversions, JSON Value conversions, VerificationError->tonic::Status mapping, and VerificationEngineService over Arc<InMemoryVerificationEngine>. lib.rs now exports service types and DEFAULT_CODE_VERSION=aios-verification/0.1.0-T069. Tests: crates/aios-verification/tests/grpc_roundtrip.rs adds 13 integration tests including run/all/any/timeout/invalid intent, engine info 36 primitives, ExplainResult completed/unknown, conversion round-trips, in-process tonic channel, and status mapping table; service unit tests add 2 more. Workspace tests: 1358 -> 1373. Required gates green: cargo check --workspace; cargo test -p aios-verification; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no evidence emission (T-070), no runtime integration (T-071), no renderer (T-072), no M8 closer (T-073).",
  "current_step_T070": "T-070 landed aios-verification evidence emission per S2.4 -> S3.1. Added aios-verification -> aios-evidence Cargo edge and ed25519-dalek signing dep. New evidence_payloads.rs defines VerificationStartedPayload, VerificationResultPayload, and PrimitiveExecutedPayload; payloads round-trip through serde_json and enforce INV-015 by carrying only ids/counts/status/timestamps/redacted error classes, never raw expression/actual/expected values. New evidence_emit.rs mirrors aios-fs T-044 / aios-vault T-053 with VerificationEvidenceLog, InMemoryVerificationEvidenceLog, VerificationEvidenceEmitter, Ed25519 signing, BLAKE3 ReceiptChain linkage, and helpers emit_verification_started, emit_verification_result, emit_primitive_executed. InMemoryVerificationEngine gained optional evidence_emitter + with_evidence_emitter; run_verification emits start + result receipts and stores result.evidence_receipt_id while no-emitter behavior stays backward compatible. VerificationError gained EvidenceEmitFailed(String); service conversion maps it to INTERNAL for exhaustive matching. Tests: crates/aios-verification/tests/evidence_emission.rs adds 13 tests covering payload round-trips, pass/fail/timeout result payloads, 2 receipts per run, BLAKE3 chain coherence, Ed25519 signatures, no-emitter compatibility, INV-015 secret marker non-leak, primitive redaction, and 5-task concurrent runs. Workspace tests: 1373 -> 1386. Required gates green: cargo check --workspace; cargo test -p aios-verification; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Spec note: current aios-evidence RecordType vocabulary has VERIFICATION_RESULT (ID 10) but no VERIFICATION_STARTED or primitive-executed variant, so started/primitive helper payloads fold into VERIFICATION_RESULT per task fallback. Also fixed a pre-existing flaky aios-evidence uppercase-signature test by uppercasing the first alphabetic hex char instead of the first 4 chars. Scope guard held: no runtime integration (T-071), no renderer (T-072), no M8 closer (T-073).",
  "current_step_T071": "T-071 replaced the M4 step_verify stub with cross-crate verification runtime integration while preserving the no-engine/no-intent stub-pass path. Added aios-verification -> aios-capability-runtime Cargo edge. aios-capability-runtime gained local async RuntimeVerificationEngine trait, optional verification_engine slot, with_verification_engine ctor, and async step_verify_with_engine / step_verify_with_engine_and_emit wiring; failures map to existing ExecutionFailureReason::AdapterRefused because the closed enum has no VerificationFailed variant. aios-verification added runtime_adapter.rs with VerificationRuntimeAdapter over Arc<dyn VerificationEngine>, parsing serialized VerificationIntent JSON or raw expression input, constructing VerificationContext from the runtime action_id, normalizing the brief's single-term all(...) legacy expression shape, and mapping VerificationStatus::Passed to true with every other status false. Tests: crates/aios-verification/tests/runtime_integration.rs adds 13 tests for adapter construction, valid/invalid intent parsing, pass/fail submit_action, no-intent no-op compatibility, real file_exists marker present/absent, concurrent submissions, runtime action_id result binding, and §22 trustworthy pass/fail contrast with verification evidence. Workspace tests: 1386 -> 1399. Required gates green: cargo check --workspace; cargo test -p aios-verification; cargo test -p aios-capability-runtime; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Spec note: current aios-action::ActionEnvelope has no top-level verification_intent field, so T-071 uses request.target.verification_intent as the envelope-carried source and no-ops when absent. Scope guard held: no renderer (T-072), no M8 closer (T-073).",
  "current_step_T072": "T-072 landed aios-renderer-cli rendering and CLI integration for S2.4 verification. Added Cargo edge aios-renderer-cli -> aios-verification. New src/verification_render.rs implements Renderable for VerificationIntent, VerificationResult, VerificationStatus, PrimitiveResult, VerificationPrimitive, plus VerificationPrimitiveList for CLI list output across Text/Json/Tree/Table. Status colors: passed green, failed red, timeout yellow, probe error magenta, skipped gray. VerificationResult table is a summary row; Text/Tree expand per_primitive, and PrimitiveResult table gives per-primitive rows. CLI adds `aios verify run <intent.json> [--action-id ...]` and `aios verify list-primitives`. AiosClient gained verify/list_primitives using T-069 RunVerification/GetEngineInfo; AiosEndpoints gained verification endpoint; InProcessBackend now starts policy/runtime/fs/vault/verification (service_count=5). Tests: crates/aios-renderer-cli/tests/verification_render.rs adds 14 tests covering required render formats, colors, JSON roundtrip, primitive count, CLI run/list, and 5-service backend invariant. Workspace test count by `cargo test --workspace -- --list`: 1410. Gates PASS: cargo check --workspace; cargo test -p aios-renderer-cli; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check; cargo build -p aios-renderer-cli --bin aios. Scope guard held: no aios-action/evidence/policy/runtime/fs/vault/verification source changes; no M8 closer/version bump.",
  "current_step_T073": "T-073 closed M8 for aios-verification v0.1.0 and upgraded the §22 MVP from RUNNABLE to TRUSTWORTHY. Added tests/mvp_trustworthy_path.rs with the contrast pair: same verification_intent + marker present -> ActionContext::Succeeded and VERIFICATION_RESULT PASSED; same intent + marker absent -> ActionContext::Failed with ExecutionFailureReason::AdapterRefused and VERIFICATION_RESULT FAILED; same intent without a configured engine preserves the T-027 stub-success path. Added tests/acceptance_fixtures.rs with 8 S2.4 §13 golden fixtures plus timeout coverage; unsupported property/privacy/network bodies are asserted at current M8 level and documented as M16/control-plane follow-up surfaces. Added tests/m8_closure.rs with 10 closure invariants: version 0.1.0, DEFAULT_CODE_VERSION 0.1.0-T073, no Unimplemented/todo/unimplemented, all gRPC RPCs implemented, VerificationStatus reachability, 30+ primitive test-path coverage, RuntimeVerificationEngine impl binding, deferred-surface docs, and closure files present. Bumped crates/aios-verification/Cargo.toml 0.0.1 -> 0.1.0 and DEFAULT_CODE_VERSION -> 0.1.0-T073. Workspace tests: 1410 -> 1436 (+26). M8 CLOSED; next M9 aios-recovery.",
  "current_step_T074": "T-074 opened M9 with crates/aios-recovery v0.0.1 as the S9.1/S9.2/S9.3 typed core skeleton. Added workspace member after aios-verification. Modules: mode.rs (RecoveryMode per S9.1 NORMAL/RECOVERY/DEGRADED/FIRST_BOOT + RecoveryState), boot.rs (BootId boot_<ULID>, BootPhase, FirstBootContext, FirstBootPhase per S9.2 15-stage FSM, FirstBootStatus), kernel.rs (CandidateId kc_<ULID>, CandidateState per S9.3 KernelImageState 9-value FSM, KernelManifest, KernelCandidate), bundle.rs (RecoveryBundle carrying pre-loaded signed degraded-subset material for S2.3 hard-denies + S5.4 STRONG_SOLO overrides), error.rs (RecoveryError taxonomy). Tests: crates/aios-recovery/tests/types_roundtrip.rs adds 12 tests; workspace tests 1436 -> 1448. Gates PASS: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no RecoveryBoundary trait, no first-boot driver, no kernel pipeline driver, no INV-012 enforcement, no gRPC, no evidence, no cross-crate integration, no renderer.",
  "current_step_T075": "T-075 landed S9.1 RecoveryBoundary trait + InMemoryRecoveryBoundary. Added crates/aios-recovery/src/boundary.rs with async_trait RecoveryBoundary and EnterRecoveryRequest {reason, operator_grant, expected_phases, bundle}. Added in_memory_boundary.rs with tokio RwLock<RecoveryState>, trusted_authorities HashMap<String, VerifyingKey>, active opaque rexit_<ULID> exit-token guard, NORMAL -> RECOVERY -> NORMAL enforcement, re-entry guard AlreadyInRecovery, exit guard RecoveryNotActive, wrong-token RecoveryAuthorizationInvalid, and entry precondition requiring either operator_grant or one of the S9.1 documented fallback reasons. Bundle verification mirrors aios-policy T-022 ordering: trusted authority lookup before Ed25519 verification; signature bytes are hex `ed25519:` material carried in the existing RecoveryBundle signature vectors, verified against canonical signed body bytes excluding the envelope signature entries. Added one RecoveryError variant: RecoveryAuthorizationInvalid(String). Added deps async-trait, tokio, ed25519-dalek. Tests: crates/aios-recovery/tests/boundary.rs adds 14 tests, workspace tests 1448 -> 1462. Gates PASS: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no first-boot driver, no kernel pipeline, no INV-012 enforcement, no gRPC, no evidence, no cross-crate integration.",
  "current_step_T076": "T-076 landed S9.2 FirstBootDriver shell. Added crates/aios-recovery/src/first_boot.rs with FirstBootDriver over RwLock<FirstBootContext>, Arc<dyn RecoveryBoundary>, ordered FIRST_BOOT_PROVISIONING_PHASES, FirstBootStageRecord, FirstBootStageStatus, detect(), run_provisioning(), mark_complete(), skip_stage(), and fail_stage() harness hook. run_provisioning deterministically records success for the S9.2 happy path, enforces ordering via RecoveryError::InvalidPhaseTransition, returns the existing context when Completed or Failed, rejects active S9.1 RECOVERY mode per S9.1/S9.2 mutual exclusion, and serializes concurrent callers through the state lock. Added RecoveryError::InvalidPhaseTransition { from: FirstBootPhase, to: FirstBootPhase }. lib.rs exports first_boot module and FirstBootDriver. Tests: crates/aios-recovery/tests/first_boot.rs adds 14 tests; workspace tests 1462 -> 1476. Gates PASS: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Spec note: S9.2 has 15 enum phases, but the happy path performs 14 provisioning phases; STAGE_FAILED_REQUIRES_RECOVERY remains the non-linear terminal failure state. Scope guard held: no kernel pipeline, no INV-012 enforcement, no gRPC, no evidence, no cross-crate integration.",
  "current_step_T077": "T-077 landed S9.3 KernelPipelineDriver. Added crates/aios-recovery/src/kernel_pipeline.rs with in-memory candidate store, active id, trusted authority map, RecoveryBoundary handle, and bounded rollback stack (last 8). register_candidate verifies Ed25519 signatures over deterministic KernelManifest JSON bytes against configured VerifyingKey authorities and creates BUILT candidates; verify_candidate applies BUILT -> GATING -> GATE_PASSED; activate_candidate applies GATE_PASSED -> A_PROMOTED, gates manifest.requires_recovery_install through boundary.is_recovery_active(), and tracks displaced active ids; rollback_candidate applies A_PROMOTED -> ROLLBACK and restores previous active id; retire_candidate/list_candidates/get_active landed. Added RecoveryError::KernelUnknownAuthority plus lib.rs module/export. Tests: crates/aios-recovery/tests/kernel_pipeline.rs adds 15 tests; workspace tests 1476 -> 1491. Gates PASS: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no INV-012 enforcement, no gRPC, no evidence emission, no cross-crate integration.",
  "current_step_T078": "T-078 landed RecoveryGuard runtime-side INV-012 enforcement. Added Cargo edge aios-recovery -> aios-fs. New crates/aios-recovery/src/recovery_guard.rs defines RecoveryGuard { boundary: Arc<dyn RecoveryBoundary> } with check_mutation(path, subject, is_ai) reading live boundary.is_recovery_active(), reading path.namespace_class(), and delegating to aios_fs::NamespacePolicy::can_mutate(path, subject, recovery_mode, is_ai). RecoveryError gained RecoveryOnlyPathMutationDenied { path, reason } and AiPathMutationDenied { path }. Tests: crates/aios-recovery/tests/recovery_guard.rs adds 12 tests for normal/recovery mode recovery-only paths, user-space allow, AI-locked deny, AI-allowed allow, human allow on AI-locked path, live enter/exit reflection, 5-task concurrent consistency, NamespacePolicy composition, and foreign namespace rejection. Backward compatibility verified with cargo test -p aios-fs. Workspace tests 1491 -> 1503. Gates PASS: cargo check --workspace; cargo test -p aios-recovery; cargo test -p aios-fs; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no aios-fs source changes, no gRPC, no evidence emission, no T-081 cross-crate service integration, no renderer.",
  "current_step_T079": "T-079 landed the aios-recovery gRPC RecoveryService. Added crates/aios-recovery/proto/aios.recovery.v1alpha1.proto with the 12-RPC service surface: EnterRecovery, ExitRecovery, GetRecoveryState, RegisterKernelCandidate, VerifyKernelCandidate, ActivateKernelCandidate, RollbackKernelCandidate, ListKernelCandidates, GetActiveKernel, RunFirstBootProvisioning, GetFirstBootStatus, CheckRecoveryMutation. Added build.rs mirroring the tonic-build/protoc-bin-vendored pattern from aios-verification T-069. Added src/service/{mod,conversions,server}.rs with tonic::include_proto!, SCHEMA_VERSION=aios.recovery.v1alpha1+T079, Rust<->proto conversions, RecoveryError->tonic::Status mapping, and RecoveryServiceImpl over Arc<InMemoryRecoveryBoundary>, Arc<FirstBootDriver>, Arc<KernelPipelineDriver>, Arc<RecoveryGuard>. Cargo.toml adds tonic/prost/prost-types and build deps. lib.rs exports service types and DEFAULT_CODE_VERSION. Tests: crates/aios-recovery/tests/grpc_roundtrip.rs adds 15 tests covering recovery lifecycle, kernel candidate lifecycle, first-boot, recovery guard, conversion round-trips, and tonic channel smoke. Workspace tests 1503 -> 1518. Gates PASS: cargo check --workspace; cargo test -p aios-recovery; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no evidence emission, no T-081 cross-crate runtime integration, no renderer.",
  "current_step_T080": "T-080 landed aios-recovery evidence emission for S9.x -> S3.1. Added Cargo edge aios-recovery -> aios-evidence. New crates/aios-recovery/src/evidence_emit.rs defines RecoveryEvidenceLog, InMemoryRecoveryEvidenceLog, RecoveryEvidenceEmitter, RecoverySubjectRef, AIOS_RECOVERY_SUBJECT, Ed25519 signing, BLAKE3 ReceiptChain linkage, and expected previous receipt validation. New crates/aios-recovery/src/evidence_payloads.rs defines typed RecoveryEntered/Exited, FirstBootStarted/PhaseCompleted/Completed, KernelCandidateRegistered/Activated/RolledBack, and KernelGateResult payloads. InMemoryRecoveryBoundary gained optional evidence_emitter + with_evidence_emitter and emits RECOVERY_BOOT_ENTERED/RECOVERY_BOOT_EXITED on successful enter/exit, hashing the raw exit token as exit_hash so S3.1 token-key redaction does not erase the hash. FirstBootDriver gained optional emitter + with_evidence_emitter and emits FIRST_BOOT_STARTED, FIRST_BOOT_STAGE_COMPLETED per phase, and FIRST_BOOT_COMPLETE. KernelPipelineDriver gained optional emitter + with_evidence_emitter and emits KERNEL_PIPELINE_STARTED on register, KERNEL_GATE_RESULT on verify, KERNEL_PROMOTED_TO_A on activate, and KERNEL_ROLLBACK_PERFORMED with previous_candidate_id on rollback. RecoveryError gained EvidenceEmitFailed(String); gRPC conversion maps it to INTERNAL for exhaustive matching. Tests: crates/aios-recovery/tests/evidence_emission.rs adds 17 tests covering serde round-trips, record payloads, BLAKE3 chain coherence, Ed25519 signatures, no-emitter backward compatibility, INV-018 no raw signature bytes, INV-015 no plaintext secrets, concurrent enter+exit, and full recovery-kernel lifecycle. Workspace tests 1518 -> 1535. Gates PASS: cargo check --workspace; cargo test -p aios-recovery; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard: no T-081 cross-crate runtime integration, no renderer, no M9 closer.",
  "current_step_T081": "T-081 landed aios-recovery cross-crate integration for recovery ↔ runtime + policy. Added Cargo edges aios-recovery -> aios-capability-runtime and aios-policy. aios-capability-runtime gained the dependency-free RuntimeRecoveryHook async trait, optional InMemoryCapabilityRuntime recovery_hook, with_recovery_hook ctor, and step_execute live recovery consultation. crates/aios-recovery/src/runtime_adapter.rs adds RecoveryRuntimeAdapter over Arc<dyn RecoveryBoundary> implementing RuntimeRecoveryHook. crates/aios-recovery/src/policy_adapter.rs adds RecoveryPolicyHydratorEnhancer wrapping an existing SubjectHydrator and overriding HydratedSubject.recovery_mode from boundary.is_recovery_active(). Tests: crates/aios-recovery/tests/cross_crate_integration.rs adds 14 tests for RuntimeRecoveryHook trait-object use, runtime recovery-only allow/deny, no-hook backward compatibility, policy hydration override, and recovery-only fs mutation allow/deny through live hydrated state. Workspace tests 1535 -> 1549. Gates PASS: cargo check --workspace; cargo test -p aios-recovery; cargo test -p aios-capability-runtime; cargo test -p aios-policy; cargo test -p aios-fs; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Spec note: AdapterManifest has no requires_recovery field, so the runtime gate uses the S10.1 W8/W9 recovery-only declared-action catalog and AdapterRefused. Scope guard: renderer T-082 and M9 closer T-083 deferred.",
  "current_step_T082": "T-082 landed aios-renderer-cli recovery/kernel rendering and CLI wiring. Added Cargo edge aios-renderer-cli -> aios-recovery. New src/recovery_render.rs implements Renderable for RecoveryState, RecoveryMode, FirstBootContext, FirstBootPhase, FirstBootStatus, KernelCandidate, CandidateState, and KernelManifest across Text/Json/Tree/Table, with recovery mode/status/candidate-state colors and 12-char kernel_blake3 human truncation. AiosClient now has a public RecoveryServiceClient field and methods enter_recovery, exit_recovery, get_recovery_state, run_first_boot, list_kernel_candidates, activate_kernel, rollback_kernel. AiosEndpoints gained mandatory recovery endpoint; InProcessBackend wires InMemoryRecoveryBoundary + FirstBootDriver + KernelPipelineDriver + RecoveryGuard and now spawns 6 services. CLI adds aios recovery {status, enter, exit, first-boot} and aios kernel {list, activate, rollback}. Tests: crates/aios-renderer-cli/tests/recovery_render.rs adds 19 tests; existing service_count assertions updated 5 -> 6. Workspace tests 1549 -> 1568. Gates PASS: cargo check --workspace; cargo test -p aios-renderer-cli; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check; cargo build -p aios-renderer-cli --bin aios. Spec note: T-079 RecoveryService does not expose the minted exit_token in EnterRecoveryResponse, so CLI enter renders the returned state only; exit still requires a token when supplied externally. Scope guard: M9 closer T-083 deferred.",
  "current_step_T083": "T-083 closed M9 for aios-recovery v0.1.0 and made §22 FULL-REAL. Added crates/aios-recovery/tests/mvp_full_real_path.rs: one full stack walk with InMemoryRecoveryBoundary in NORMAL mode, FirstBootDriver through all 14 happy-path provisioning phases, KernelPipelineDriver signed candidate register/verify/activate, InMemoryAiosFs write/read/materialize_view, InMemoryVaultBroker construction, InMemoryCapabilityRuntime with real AdapterRegistry/DispatchQueue/PolicyKernel/EvidenceEmitter/RecoveryRuntimeAdapter/VerificationRuntimeAdapter, signed evidence chain, recovery enter/exit evidence, and ActionContext rendering through aios-renderer-cli. Added tests/acceptance_fixtures.rs with 12 S9.1/S9.2/S9.3 fixtures from worked examples/adversarial sections. Added tests/m9_closure.rs with 11 invariants: version 0.1.0, DEFAULT_CODE_VERSION 0.1.0-T083, no Status::Unimplemented/todo/unimplemented, every RecoveryService RPC implemented, RecoveryMode/CandidateState/FirstBootPhase reachability, 14 provisioning phases + terminal failure note, and deferred-surface documentation. Added local dev-deps aios-renderer-cli, aios-vault, and aios-verification for the full-real integration harness. Workspace tests 1568 -> 1592 (+24). M9 CLOSED; next M10 aios-sgr. Deferred surfaces documented: FirstBootPhase 15 vs 14 happy path, shallow kernel gate witness vs later deep attestation, evidence vocabulary mapping, recovery-only action catalog gate, and EnterRecovery gRPC exit_token omission.",
  "current_step_T052": "T-052 landed the aios-vault gRPC VaultBroker service surface. New crates/aios-vault/proto/aios.vault.v1alpha1.proto defines package aios.vault.v1alpha1 with IssueCapability, UseCapability, ListCapabilities, RevokeCapability, override grant/consume/revoke/lookup/list, subject/session registration and lookup, audit entry lookup/list, RunExpirationPass, and GetVaultInfo. New build.rs mirrors the tonic-build + protoc-bin-vendored pattern from policy/runtime/fs. New src/service/{mod,conversions,server}.rs includes tonic::include_proto!, SCHEMA_VERSION=aios.vault.v1alpha1+T052, Rust<->proto conversions, VaultError->tonic::Status mapping, and VaultBrokerService over Arc<InMemoryVaultBroker>, Arc<InMemoryOverrideBroker>, Arc<IdentityCatalog>, Arc<CapabilityAuditLog>, Arc<CapabilityLifecycleDriver>. lib.rs re-exports service types and DEFAULT_CODE_VERSION. Tests: crates/aios-vault/tests/grpc_roundtrip.rs adds 15 integration tests including tonic in-process channel smoke and INV-018 wire-format no-key-bytes assertions; service unit tests add 3 more. Workspace tests: 1087 -> 1105. Required gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no evidence emission, no cross-crate reconciliation, no M6 closer.",
  "current_step_T051": "T-051 landed the aios-vault S5.2 capability lifecycle audit log and expiration driver. New crates/aios-vault/src/audit.rs defines CapabilityAuditEntry and CapabilityAuditLog with issue/use/revoke/expire recording, lookup, and deterministic list_all. New crates/aios-vault/src/lifecycle.rs defines CapabilityLifecycleDriver, expire_capability, run_expiration_pass, and ExpirationPassReport. InMemoryVaultBroker gained optional audit_log plus with_audit_log; successful issue/use/revoke update audit when configured, while no-audit behavior remains backward compatible. use_capability now lazily transitions Active capabilities with past expires_at to Expired and records expiration before returning CapabilityExpired. Tests: crates/aios-vault/tests/lifecycle.rs adds 12 tests; workspace tests 1075 -> 1087. Required gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no gRPC, no evidence emission, no cross-crate reconciliation, no new VaultError variants.",
  "current_step_T050": "T-050 landed the aios-vault S5.4 emergency override broker. New crates/aios-vault/src/override_broker.rs defines GrantOverrideRequest, OverrideBroker async trait, and InMemoryOverrideBroker backed by RwLock<HashMap<String, OverrideBinding>> plus Arc<IdentityCatalog>. grant_override validates STRONG_SOLO/DUAL_HUMAN/TRIPLE_HUMAN approver counts, verifies every granting subject exists, blocks Agent/Application grant subjects with VaultError::AiCannotGrantOverride, and rejects other non-human grant subjects with OverrideRequiresHumanApprovers. consume_override holds one write lock across state validation and Granted -> Consumed mutation, so replay returns OverrideAlreadyConsumed and the parallel 10-task consume test has exactly one winner. lookup/list transition live expired bindings to Expired; consume on expired returns OverrideExpired; revoke moves any state to Revoked. VaultError gained OverrideRequiresHumanApprovers, AiCannotGrantOverride, OverrideClassApproverCountMismatch, and OverrideExpired. Tests: crates/aios-vault/tests/override_broker.rs adds 16 tests; workspace tests 1059 -> 1075. Required gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Spec note: the brief's human+agent human-approver-error bullet conflicts with the hard AI self-override defense; implementation follows the hard-deny and uses a Service subject for OverrideRequiresHumanApprovers coverage.",
  "current_step_T049": "T-049 landed real aios-vault crypto operations for S5.2 use-without-reveal. New crates/aios-vault/src/crypto/{aes_gcm,hmac,hkdf,ed25519}.rs implement AES-256-GCM encrypt/decrypt, HMAC-SHA256 generate/verify, HKDF-SHA256 derive-to-opaque-handle, and Ed25519 sign/verify. InMemoryVaultBroker issue_capability now validates class/algorithm compatibility with VaultError::KeyAlgorithmMismatch and generates fresh key material via OsRng for AES-256-GCM, HMAC-SHA256, HKDF IKM, Ed25519 SigningKey, and X25519 StaticSecret. use_capability now dispatches to real crypto; RandomGenerate uses OsRng; SecretGet returns OperationUnsupportedInT049. VaultError gained CryptoError(String), KeyAlgorithmMismatch, and OperationUnsupportedInT049. Tests: crates/aios-vault/tests/crypto_ops.rs adds 18 tests covering AES round-trip/AAD/nonce failures, HMAC valid/negative/malformed tag, HKDF deterministic + zero-length rejection, Ed25519 sign/verify negatives, fresh key randomness, INV-018 Debug/serde leak guards, algorithm mismatch, all 9 CapabilityClass variants, concurrent encrypt, and revoke-after-use. Existing broker tests updated from operation_simulated to real outputs. Workspace tests: 1041 -> 1059. Required gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Note: T-046 has no KDF capability class and T-047 Decrypt has no nonce field, so T-049 keeps the public surface stable by routing KDF through KeyEncrypt+HkdfSha256 and prefixing AES ciphertext with the 12-byte nonce while also returning nonce metadata.",
  "current_step_T048": "T-048 landed the aios-vault S5.1 identity catalog backend. New crates/aios-vault/src/identity_catalog.rs stores subjects, sessions, and group membership behind tokio RwLock<HashMap<...>>; supports duplicate-safe subject registration, lookup, session start/lookup/suspend/revoke, expiry transition on lookup, active-session duplicate guard, group add/remove, and with_fixtures() for family:alice, agent:dev, app:browser, _system:kwin, operator:root. New crates/aios-vault/src/hydrator.rs adds VaultSubjectHydrator over Arc<IdentityCatalog> plus HydratedSubjectSnapshot mirroring the aios-policy HydratedSubject shape without adding a cross-crate dependency; capabilities remain empty for T-049/T-054. VaultError additions only: SubjectAlreadyRegistered, SessionAlreadyActive, GroupMembershipUnchanged. Tests: crates/aios-vault/tests/identity_catalog.rs adds 17 tests; workspace tests 1024 -> 1041. Required gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Note: one initial cargo test run hit pre-existing flaky aios-evidence uppercase-hex test; exact test and full workspace rerun passed without code changes outside T-048 scope.",
  "current_step_T047": "T-047 added the aios-vault VaultBroker async trait with issue_capability, use_capability, list_capabilities, and revoke_capability. New DTOs/enums: IssueCapabilityRequest, UseCapabilityRequest, VaultOperation, UseCapabilityResult. InMemoryVaultBroker stores private RwLock<HashMap<CapabilityId, (VaultCapability, KeyMaterial)>> tuples and returns only public VaultCapability records or non-key operation outputs. T-047 crypto is deterministic placeholder only; T-049 lands real KeyGen/crypto. Operation/class compatibility is enforced with VaultError::OperationClassMismatch. SECRET_GET is typed but rejected as VaultError::OperationUnsupportedInT047 before any raw reveal. INV-018 checks cover VaultCapability serde, list serde, UseCapabilityResult Debug, and broker.rs inline static serialization defense. Workspace tests: 1008 -> 1024 (+16). Required gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no identity catalog, no real crypto, no override broker, no lifecycle driver, no gRPC, no evidence emission, no cross-crate reconciliation.",
  "current_step_T046": "T-046 opened M6 with crates/aios-vault v0.0.1. Added S5.2 VaultCapability with cap_<ULID> CapabilityId, nine-value CapabilityClass per spec, six-state CapabilityState per spec, and opaque KeyMaterialHandle with Display redaction. Added broker-internal KeyMaterial with custom Debug redaction and serialization-blocking KeyMaterialLeak guard plus KeyAlgorithm marker. Added S5.1 Subject/Session/SessionState and SubjectType mirroring aios-policy shape while serializing to S5.1 SubjectKind names including LOCAL_OPERATOR. Added S5.4 OverrideBinding, OverrideClass using spec OverrideStrength set STRONG_SOLO/DUAL_HUMAN/TRIPLE_HUMAN, and OverrideBindingState. Added VaultError taxonomy and 11 types_roundtrip/redaction tests. Workspace tests: 997 -> 1008 (+11). Required gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no VaultBroker trait, no crypto ops, no gRPC, no evidence emission, no cross-crate identity convergence.",
  "current_step_T045": "T-045 closed M5 for aios-fs. Added crates/aios-fs/tests/mvp_golden_path.rs with 6 tests covering the §22 phase-2/3 M5 stack: /aios mount stub, versioned object write, semantic view, BLAKE3 evidence chain, quarantine release round-trip, and GC shared-chunk reclaim. Added crates/aios-fs/tests/acceptance_fixtures.rs with 23 S1.3/S2.1/S4.1 acceptance fixtures. Added crates/aios-fs/tests/m5_closure.rs with 8 closure invariants: version 0.1.0, DEFAULT_CODE_VERSION T045, no Unimplemented/todo/unimplemented, every RPC implemented, VersionState and NamespaceClass test coverage. Cleanups: materialize_view now uses additive FsEnumerator instead of Debug discovery; ImplSpace duplicate binding returns typed FsError::ImplSpaceBindingDuplicate; InMemoryAiosFs derives kind/privacy/policy_tags/scope from write metadata; NamespacePolicy enforces cross-group denial, reserved id rejection, and virtual-view mutation denial. Version bumped aios-fs 0.0.1 -> 0.1.0; DEFAULT_CODE_VERSION -> aios-fs/0.1.0-T045; SCHEMA_VERSION -> aios.fs.v1alpha1+0.1.0-T045. Workspace tests: 960 -> 997 (+37). Required gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check; cargo bench -p aios-policy --no-run. M5 closed with no debt carried into M6.",
  "current_step_T044": "T-044 implemented evidence emission for aios-fs. Added crates/aios-fs/src/evidence_emit.rs with FsEvidenceLog, InMemoryFsEvidenceLog, FsEvidenceEmitter, ACTION_RECEIVED / QUARANTINE_EVENT / CONFLICT_EVENT / GC_PASS helpers, BLAKE3 ReceiptChain linkage, Ed25519 signing, and record_conflict_event. Added crates/aios-fs/src/evidence_payloads.rs with ActionReceivedPayload, QuarantineEventPayload, ConflictEventPayload, ConflictResolutionKind, and GcPassPayload. Cargo edge aios-fs -> aios-evidence added; ed25519-dalek added for SigningKey. InMemoryAiosFs gained optional evidence_emitter + with_evidence_emitter and emits ACTION_RECEIVED after successful write_object. QuarantineDriver gained optional emitter + with_evidence_emitter and emits QUARANTINE_EVENT on successful enter/exit. GcPassDriver gained optional emitter + with_evidence_emitter and emits GC_PASS after successful run_pass. FsError gained EvidenceEmitFailed(String); service conversion maps it to INTERNAL to preserve exhaustive gRPC mapping. Tests: crates/aios-fs/tests/evidence_emission.rs with 16 tests covering payload round-trips, ACTION_RECEIVED, quarantine enter/exit, GC_PASS counts, conflict helper, BLAKE3 chain coherence, no-emitter compatibility, Ed25519 signatures, failing log propagation, concurrent writes, and versions_purged. Workspace total 960 (+16 over T-043 baseline 944). Gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no M5 closer/version bump/golden path fixtures.",
  "current_step_T043": "T-043 implemented the aios-fs gRPC service adapter. New crates/aios-fs/proto/aios.fs.v1alpha1.proto defines package aios.fs.v1alpha1, service AiosFs (ReadObject, WriteObject, ListVersions, ResolvePointer, GetSnapshot, QuarantineObject, RunGcPass, MaterializeView), core Object/Version/Chunk/Pointer/Snapshot/Query/Quarantine/Gc/ImplSpace proto messages, Timestamp fields for chrono values, ImplSpaceTarget oneof variants, and bytes placeholders for ActionId per open task #26. New build.rs mirrors aios-policy T-023 tonic-build + vendored protoc wiring. New src/service/{mod,conversions,server}.rs includes tonic::include_proto!, SCHEMA_VERSION=\"aios.fs.v1alpha1+T043\", Rust↔proto conversions, FsError→tonic::Status mapping, and AiosFsService over Arc<InMemoryAiosFs> delegating to AiosFs, QuarantineDriver::enter, GcPassDriver::run_pass, and query_eval::materialize_view. Cargo.toml adds tonic/prost/prost-types runtime deps and tonic-build/protoc-bin-vendored build deps; lib.rs re-exports the service. Tests: crates/aios-fs/tests/grpc_roundtrip.rs with 12 integration tests plus 2 service unit tests; workspace total 944 (+14 over T-042 baseline 930). Gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no evidence emission (T-044), no M5 closer (T-045), no changes to core object/version/chunk/pointer/transaction/namespace/lifecycle/fs/in_memory/quarantine/gc/query/impl_space files.",
  "current_step_T042": "T-042 implemented the aios-fs S4.1 namespace expansion and recovery/AI mutation admission gate. NamespaceClass now covers the Wave 8 S4.1 vocabulary: 52 classes total (50 reserved/catalog classes plus retained T-036 bare Group/User scope classes). New EvidenceGradeFloor newtype + NamespaceClass::{is_recovery_only_mutation,is_read_only_for_ai,evidence_grade_floor}. New crates/aios-fs/src/namespace_policy.rs exposes NamespacePolicy::can_mutate(path, subject, recovery_mode, is_ai), returning FsError::NamespaceMutationDenied for AI mutation attempts against AI-locked classes and non-recovery mutation attempts against recovery-only classes. FsError gained NamespaceMutationDenied { path, reason }; lib.rs re-exports NamespacePolicy and EvidenceGradeFloor. Tests: crates/aios-fs/tests/namespace.rs with 19 tests; workspace total 930. Gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Note: expanding NamespaceClass required mechanical token arms in crates/aios-fs/src/query_eval.rs so existing exhaustive matching compiles; no query behavior changed. Scope guard held otherwise: no gRPC, no evidence emission, no M5 closer.",
  "current_step_T041": "T-041 implemented the aios-fs S2.2 implementation-space binding surface. New crates/aios-fs/src/impl_space.rs defines ImplSpaceTarget (LocalFile, EncryptedBlob, RemoteBlob, AiosFsManaged), ImplSpaceSource (Object/Chunk/Version), IntegrityState (Verified/Stale/IntegrityFailed/Unknown), ImplSpaceBinding, ImplSpace async trait, and InMemoryImplSpace backed by RwLock<HashMap<ImplSpaceSource, Vec<ImplSpaceBinding>>>. with_fixtures() loads 3 canonical metadata-only bindings (LocalFile + EncryptedBlob + AiosFsManaged). verify() is the M5 stub and returns recorded integrity_state; real content checks are deferred to M6+. FsError gained ImplSpaceBindingNotFound, ImplSpaceTargetUnreachable, ImplSpaceIntegrityFailed. Tests: crates/aios-fs/tests/impl_space.rs with 20 tests; workspace total 911. Gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no namespace expansion, no gRPC, no evidence emission, no real I/O/encryption/network.",
  "current_step_T040": "T-040 implemented the aios-fs S2.1 query predicate core. New crates/aios-fs/src/query.rs defines Query::And(Vec<Predicate>), Predicate, closed QueryNamespace/QueryField/QueryOperator, and typed QueryValue. New query_parser.rs mirrors the aios-policy T-019 hand-written recursive-descent pattern: AND-only, no OR/NOT/parens, closed vocabulary errors, and 1-based line/column diagnostics. New query_eval.rs evaluates predicates over QueryEvalContext borrowed from Object/Version/Pointer/NamespaceClass and materializes read-only View results through AiosFs read_object/resolve_pointer, with View { snapshot_id, matched, query_hash } using BLAKE3 over JCS canonical Query. FsError gained QueryParse and QueryEval. Tests: 20 new (11 parser + 9 eval), workspace total 891. Gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no implementation space, no namespace expansion, no gRPC, no evidence emission.",
  "current_step_T039": "T-039 implemented S1.3 §7.3 GC/refcounting for aios-fs. New crates/aios-fs/src/gc.rs defines GcPassDriver, GcPassReport, GcReason, and VersionPurgeReason with gcp_<ULID> pass ids. MutableAiosFs gained decrement_chunk_refcount, reclaim_chunk, and purge_version; InMemoryAiosFs implements those methods, tracks purged_versions for VersionAlreadyPurged detection, keeps write_object chunk refcount increments, decrements refs on purge_version, and reclaims zero-ref chunks via GC. FsError gained ChunkStillReferenced and VersionAlreadyPurged. Tests: crates/aios-fs/tests/gc.rs with 12 tests; workspace test count 871. Gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Spec note: VersionState has no Purged variant per S1.3/T-036, so QuarantineDisposition::Purged remains RetiredVersion and GC purges retired versions. Scope guard held: no Query/View, no implementation space, no namespace expansion, no gRPC, no evidence emission.",
  "current_step_T037": "T-037 landed in commit fb38671. New modules: crates/aios-fs/src/fs_trait.rs (AiosFs async trait with read_object/write_object/list_versions/resolve_pointer/get_snapshot plus ObjectReadResult/ObjectWriteRequest/ObjectWriteResult/FsContext/SnapshotSummary), snapshot_id.rs (SnapshotId snap_<32hex>, deterministic BLAKE3+JCS over sorted object-head/pointer-target/version entries), in_memory.rs (InMemoryAiosFs HashMap-backed harness with objects/versions/chunks/pointers/transactions/snapshots, stale SNAPSHOT detection, write_object, read_object, list/resolve/get snapshot). error.rs gained SnapshotStale, WriteRequiresParent, ChunkUnknown. lib.rs re-exports the new surface. Tests: crates/aios-fs/tests/fs_trait.rs with 14 tests. Workspace 831 → 845 (+14). Gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Spec deviations documented in commit: S1.3 §11 exact snapshot preimage is not specified, so T-037 hashes sorted object-head/pointer-target/version entries; write_object lacks chunk payload bytes, so in-memory Version.content_hash hashes ordered chunk ids; read_object has no caller FsContext, so quarantine recovery check uses creating transaction subject as the T-037 proxy until real L4 identity lands.",
  "current_step_T038": "T-038 implemented S1.3 §12 quarantine entry/exit for aios-fs. New crates/aios-fs/src/quarantine.rs defines QuarantineDriver, MutableAiosFs, QuarantineTrigger, QuarantineDisposition, and QuarantineReceipt. InMemoryAiosFs implements quarantine entry/exit, sets VersionState::Quarantined + quarantined_at/quarantine_reason, exits Released to VersionState::Verified, exits Purged to VersionState::RetiredVersion because VersionState has no PURGED variant and version.rs is frozen. Pointer move rule implemented atomically under the in-memory write lock: affected CURRENT/STABLE pointers move to same-object ROLLBACK target first, else prior stable pointer target, else first verified parent; missing fallback returns FsError::NoPriorStablePointer before mutation. FsError gained QuarantineAlreadyApplied, QuarantineNotApplied, NoPriorStablePointer. Tests: crates/aios-fs/tests/quarantine.rs with 12 tests; workspace test count 859. Gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. Scope guard held: no GC, no Query/View, no gRPC, no evidence emission; T-037 AiosFs trait unchanged.",
  "current_step_T036": "T-036 landed. New crate crates/aios-fs v0.0.1 registered in workspace. Modules: lib.rs + id.rs helper + object.rs (ObjectId, SubjectRef, ObjectKind, PrivacyClass stub for S1.2-owned canonical, ScopeKind, ScopeBinding, ObjectMetadata, ObjectInit, Object), version.rs (VersionId, VersionState {Staged, Verified, Quarantined, RetiredVersion}, Version), chunk.rs (ChunkId full BLAKE3 chk_ hash, ChunkRef, Chunk), pointer.rs (PointerId, PointerKind {Current, Stable, Candidate, Rollback, Quarantine}, Pointer), transaction.rs (TransactionId, TransactionState, ConsistencyClass, WriteOp, PointerMoveOp, Transaction), namespace.rs (AiosPath + NamespaceClass classifier for /aios/system and /aios/groups scopes), lifecycle.rs (LifecycleState Active/Retired/Purged + is_terminal), error.rs (FsError taxonomy). Tests: crates/aios-fs/tests/types_roundtrip.rs with 10 tests. Workspace 821 → 831 (+10). Gates green: cargo check --workspace; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check. TRUST SPEC deviations from brief: used S1.3 §5..§9 and S4.1 actual schema: Object current_pointer_id/metadata/retired_at/purge_at/index_hints/scope_binding; Version parent_version_ids/chunk_refs/content_hash/metadata_delta/action+transaction linkage; Chunk ref_count only; Pointer CURRENT/STABLE/CANDIDATE/ROLLBACK/QUARANTINE; Transaction §9 state/writes/pointer_moves/evidence_receipt_id with ConsistencyClass as separate §11 enum; namespace /aios/system + /aios/groups; lifecycle ACTIVE/RETIRED/PURGED.",
  "current_step_T033": "T-033 closed — gRPC CapabilityRuntime service surface (S10.1 §5). Pattern mirrors aios-policy T-023 + aios-evidence T-011 verbatim. New proto/aios.runtime.v1alpha1.proto (10 RPCs per §5.1 closed surface; 8 vocabulary enums per §3; AdapterManifest + AdapterActionDeclaration + RegisteredAdapter + ActionContext messages). New build.rs (tonic-build with vendored protoc). New src/service/{mod,conversions,server}.rs: conversions module covers every value type round-trip + RuntimeError → tonic::Status mapping table + runtime_error_to_code projection; server module wraps Arc<InMemoryCapabilityRuntime> behind CapabilityRuntimeService with full impls for ValidateAction, ExecuteAction, GetActionStatus, ListAdapters, GetAdapterCapabilities, GetCapabilityRuntimeInfo; stubs (Code::Unimplemented) for EvaluatePolicyForAction, RequestApprovalForAction, VerifyAction, RollbackAction per T-034/T-035 scope. RuntimeError → tonic::Status table covered by full-coverage test: ActionNotFound→NotFound, InvalidTransition→FailedPrecondition, AdapterUnknown→NotFound, AdapterSignatureInvalid/AdapterUnknownAuthority→PermissionDenied, AdapterAlreadyRegistered→AlreadyExists, ManifestInvalid→InvalidArgument, QueueFull/RateLimited→ResourceExhausted, PolicyEvalFailed→FailedPrecondition, EvidenceEmitFailed→Internal, Internal→Internal. Cross-crate envelope kept JSON placeholder per open task #26. 29 new tests workspace 740 -> 769 (16 service unit + 13 grpc_roundtrip integration). All 4 gates green.",
  "current_step_T031": "T-031 landed. Files added: crates/aios-capability-runtime/src/evidence_emit.rs (~470 LoC, 3 inline tests covering the InMemoryEvidenceSink seal+chain semantics); crates/aios-capability-runtime/src/evidence_payloads.rs (~330 LoC, 9 inline JSON round-trip tests); crates/aios-capability-runtime/tests/evidence_emission.rs (~700 LoC, 15 integration tests covering: full 7-step happy path chain; DENY short-circuit; REQUIRE_APPROVAL short-circuit; AI subject downgrade marker; BLAKE3 chain integrity end-to-end; Ed25519 signature verification on every receipt; backward-compat no-emitter empty chain; EvidenceEmitFailed propagation from a FailingSink; 6-way concurrent submission with per-action chain isolation; ROUTING_DECISION payload contains adapter_id+dispatch_kind+action_kind; POLICY_DECISION payload contains policy_decision_id+decision+bundle_version; payload determinism modulo timestamps; constitutional subject CAPABILITY_RUNTIME_SUBJECT on every receipt; envelope-validation-failure emits zero receipts; EXECUTION_COMPLETED.outcome=ADAPTER_OK + VERIFICATION_RESULT.passed=true + retention=STANDARD_24M). Files modified: Cargo.toml (+ aios-evidence path dep); src/lib.rs (+ pub mod evidence_emit + pub mod evidence_payloads + 12 re-exports); src/error.rs (+ EvidenceEmitFailed(String) only); src/runtime.rs (+ evidence_emitter Option<Arc<EvidenceEmitter>> field + with_evidence_emitter chainable ctor + evidence_emitter() const accessor + Debug field; submit_action route gained if-let branch through run_with_full_engines_and_evidence with no behaviour change when emitter is None); src/pipeline.rs (+ run_with_full_engines_and_evidence top-level driver + 4 emitter-aware step variants: step_policy_evaluate_with_kernel_and_emit emits POLICY_DECISION after the kernel's §4.2 mapping; step_queue_with_engine_and_emit reads pre-enrolment downgrade marker via ActionDispatcher::apply_ai_interactive_downgrade then emits ACTION_DISPATCHED(queued) + conditional AI_INTERACTIVE_QUEUE_DOWNGRADE; step_execute_with_engines_and_emit emits ROUTING_DECISION + EXECUTION_STARTED after adapter resolution; step_verify_and_emit emits EXECUTION_COMPLETED before EXECUTING->VERIFYING transition then VERIFICATION_RESULT after VERIFYING->SUCCEEDED. The T-027..T-030 sync paths and run_with_full_engines preserved verbatim — emitter-aware path is pure addition). Spec deviation: S3.1 vocabulary (Wave 13 / DEC-051) does NOT include ACTION_QUEUED at any wire ID; TRUST SPEC applied — queue-enrolment evidence emitted under RecordType::ActionDispatched (ID 116) with payload field dispatched=false marking it as the queue-enrolment phase (EXECUTION_STARTED handles the actual dispatch). Documented inline in evidence_payloads.rs ActionQueuedPayload doc comment. RecordTypes used (10): ActionReceived(1) | PolicyDecision(4) | RoutingDecision(3) | ActionDispatched(116) | ExecutionStarted(8) | ExecutionCompleted(9) | VerificationResult(10) | RollbackCompleted(11) | AiInteractiveQueueDowngrade(129) | FOREVER retention override path for ROLLBACK_FAILED via emit_with_retention. Workspace 688 -> 715 (+27: 12 unit + 15 integration). All 4 gates PASS: cargo check --workspace + cargo test --workspace (715) + cargo clippy --workspace --all-targets -- -D warnings (clean after 13 lint fixes: 2 doc-paragraph-too-long + 8 missing-backticks + 1 temporary-with-significant-Drop early-drop via explicit drop(guard) before Ok-return + 1 if-let-else→and_then refactor + 1 future-not-send Sync bound on generic P) + cargo fmt --all -- --check. Scope guard held: status.rs / dispatch.rs / failure.rs / adapter_manifest.rs / context.rs / adapter_registry.rs / adapter_handle.rs / dispatch_queue.rs / dispatcher.rs UNTOUCHED. error.rs gained exactly EvidenceEmitFailed(String) variant. pipeline.rs additions are pure additions; existing T-027..T-030 step methods preserved verbatim. runtime.rs additions are pure additions. No rollback driver (T-032), no gRPC (T-033), no approval orchestration (T-034), no M4 closer (T-035) — all deferred. M4 7/10 complete; T-032 next.",
  "current_step_T030": "T-030 landed. Files: M crates/aios-capability-runtime/Cargo.toml (+ aios-policy path dep — L4→L3 consumes-only edge). M crates/aios-capability-runtime/src/error.rs (+ RuntimeError::PolicyEvalFailed(String) — only addition; existing variants untouched. SubjectUnauthenticated does NOT use this variant — it maps onto T6 PolicyDenied with error=EnvelopeValidationFailed because §3.6 enum is closed at T-026 and the envelope's identity is the runtime's input contract). M crates/aios-capability-runtime/src/runtime.rs (replaced subject_id: String with subject: HydratedSubject on RuntimeContext; helper default_hydrated_subject builds the constitutional-default Human/INTERNAL/non-recovery wrap so the 3-arg new() ctor stays backward-compat for T-027/T-028/T-029 tests; added from_subject(HydratedSubject,bundle,code) ctor + with_hydrated_subject chainable + subject_id()->&str accessor preserving the dispatcher's plain-string lookup contract; added policy_constraints: Arc<Mutex<Option<Constraints>>> interior-mutable slot + policy_constraints_snapshot reader + install_policy_constraints pub(crate) writer for Option-A Constraints projection — chosen over a new ActionContext field because context has #[serde(deny_unknown_fields)] and the spec field set is closed; added policy_kernel: Option<Arc<dyn PolicyKernel>> + with_policy_kernel chainable + policy_kernel accessor; added policy_double_check_warnings: Arc<AtomicU64> §17 tripwire counter + policy_double_check_warnings reader; custom Debug impl for the struct since dyn-trait field is not Debug; submit_action now routes through pipeline.run_with_full_engines threading registry + queue + kernel + RuntimeContext + tripwire). M crates/aios-capability-runtime/src/pipeline.rs (added run_with_full_engines async with 8 args under #[allow(clippy::too_many_arguments)] — additive; T-027/T-028/T-029 sync paths preserved verbatim; added step_policy_evaluate_with_kernel async: kernel=None→falls back to T-027 stub T4-allow; kernel=Some builds PolicyContext from RuntimeContext (subject + EnrichmentSnapshot::default + bundle_version + code_version), awaits evaluate_policy, maps Decision per §4.2 verbatim: Allow→T4 PolicyPending→Approved + projects decision.constraints onto rctx.install_policy_constraints + §17 tripwire fires when subject.is_ai && no approver_classes has Human or Operator; RequireApproval→T5 PolicyPending→ApprovalPending short-circuit + constraints projection; Deny→T6 PolicyPending→PolicyDenied short-circuit + clears constraints; Decision::Unspecified→RuntimeError::PolicyEvalFailed (proto invariant violated); PolicyError::SubjectUnauthenticated→T6 PolicyDenied with EnvelopeValidationFailed reason + clears constraints; other PolicyError→RuntimeError::PolicyEvalFailed propagated). New crates/aios-capability-runtime/tests/policy_integration.rs (12 new tests with a ScriptedKernel mock impl PolicyKernel: allow_decision_drives_t4_and_reaches_succeeded / require_approval_decision_drives_t5_and_short_circuits / deny_decision_drives_t6_to_policy_denied / no_policy_kernel_preserves_t027_baseline / hydrated_subject_reaches_policy_kernel_unchanged (EchoSubjectKernel asserts kernel observes exact HydratedSubject incl groups+capabilities+session_class) / allow_constraints_visible_via_policy_constraints_snapshot (asserts sandbox_profile_id reaches RuntimeContext) / ai_subject_with_allow_increments_tripwire / human_subject_with_allow_does_not_increment_tripwire / ai_subject_with_human_approver_does_not_increment_tripwire (approver_classes=[Human] suppresses tripwire) / subject_unauthenticated_short_circuits_to_policy_denied / bundle_version_mismatch_propagates_as_policy_eval_failed / end_to_end_allow_walks_full_pipeline_to_succeeded). Workspace 676 → 688 (+12). All 4 gates: cargo check --workspace + cargo test --workspace (688) + cargo clippy --workspace --all-targets -- -D warnings (clean after 5 lint fixes: 3 doc-markdown backtick fixes around REQUIRE_APPROVAL / EnrichmentSnapshot / RequireApproval, 1 default-constructed-unit-structs removal on ActionLifecyclePipeline init, 1 redundant_clone on decision.constraints already moved by Some(...) ownership) + cargo fmt --all -- --check. Spec deviation reports: (a) T7 OverridePending mapping deferred to T-031 — aios_policy::InMemoryPolicyKernel's pipeline step 5 already consults its own OverrideBoundary handle during evaluate_policy, so any successful override relaxation converts Deny→Allow inside the kernel before reaching the runtime; if a Deny reaches the runtime it means no override path was authored at the kernel layer. Until the runtime explicitly threads an OverrideBoundary handle of its own (T-031 scope), T7 is structurally unreachable from this seam. (b) SubjectUnauthenticated mapped onto T6 (POLICY_PENDING→POLICY_DENIED) with EnvelopeValidationFailed reason — §4.2 does not list POLICY_PENDING→FAILED so the FSM walk must remain legal; the typed reason is preserved on ctx.error for T-031 evidence emission. (c) ScriptedKernel test mock is local to the test file (not pub) — keeps T-030 scope to the L3⇄L4 wiring proof; re-testing L4 itself is aios-policy's responsibility. Scope guard held: status.rs / dispatch.rs / failure.rs / adapter_manifest.rs / context.rs / adapter_registry.rs / adapter_handle.rs / dispatch_queue.rs / dispatcher.rs UNTOUCHED. error.rs gained exactly the 1 specified variant. pipeline.rs additions are pure additions (run_with_full_engines + step_policy_evaluate_with_kernel); the T-027/T-028/T-029 sync paths preserved verbatim. runtime.rs subject_id String → HydratedSubject swap remains backward-compat via the constitutional-default helper. M4 7/10 complete; T-031 evidence emission (S10.1 ↔ S3.1) next.",
  "current_step_T029": "T-029 landed. New crates/aios-capability-runtime/src/dispatch_queue.rs (~410 LoC, 5 inline TokenBucket unit tests): TokenBucket {tokens,capacity,refill_per_second,last_refill:Instant} implements §11.2 monotonic-clock token-bucket rate limiter; default_ai_agent() = (15.0 burst, 0.5/sec refill) — strictest §11.2 row. DispatchQueue holds per-class FIFO RwLock<HashMap<QueueClass,VecDeque<ActionContext>>> + capacity_per_class HashMap + per-subject rate_limiters RwLock; new_with_defaults() seeds 300/400/250/50 absolute slots from §11.1 30%/40%/25%/5% shares against DEFAULT_TOTAL_CAPACITY=1_000 (matches §11.3 backpressure threshold). enroll() runs 3 ordered gates: (1) per-subject token-bucket consume → RateLimited(subject_id); (2) per-class capacity → QueueFull(class); (3) 50% AGENT_PROPOSAL hard cap against TOTAL CAPACITY (TRUST SPEC: §11.1 'Max share' is capacity-relative, not enrolment-relative — a lone AI submission must be admitted) using integer cross-multiply with AGENT_PROPOSAL_CAP_NUM/DEN constants. dequeue() FIFO pop_front. depth_per_class() + total_len() inspectors. New src/dispatcher.rs (~155 LoC, pure helpers): ActionDispatcher::select_queue_class follows §3.5 order (recovery_mode → RecoveryPriority; is_ai → AgentProposal — §11.4 silent downgrade folded into selection; else Interactive). select_dispatch_kind implements §3.2 closed table verbatim. apply_ai_interactive_downgrade returns Some(AI_INTERACTIVE_QUEUE_DOWNGRADE) iff is_ai+Interactive (marker constant exposed for T-031 evidence emission). Modified files: src/error.rs (+ QueueFull(QueueClass) + RateLimited(String) — exactly the 2 brief-specified additions; existing variants unchanged). src/lib.rs (+ 2 module decls + 7 re-exports: DispatchQueue, TokenBucket, 5 constants, ActionDispatcher, AI_INTERACTIVE_DOWNGRADE_MARKER, DispatchKindInputs, compute_dispatch_kind). src/pipeline.rs (additions only — preserves T-027/T-028 sync paths verbatim): run_with_engines (async) + step_queue_with_engine (async — calls ActionDispatcher::select_queue_class, applies §11.4 downgrade marker logging, drives §4.2 T12 APPROVED→QUEUED, calls DispatchQueue::enroll; on QueueFull/RateLimited drives T14 QUEUED→FAILED with error=ExecutionFailureReason::ResourceBudgetExceeded) + step_execute_with_engines (records compute_dispatch_kind selection onto ctx.dispatch_kind when registry resolves the action_kind) + DispatchKindInputs struct (allow struct_excessive_bools with reason — §3.2 has exactly 4 boolean inputs) + compute_dispatch_kind pure function. src/runtime.rs (+ dispatch_queue: Option<Arc<DispatchQueue>> field + with_dispatch_queue chainable ctor + dispatch_queue() const accessor; submit_action now threads both adapter_registry and dispatch_queue refs into pipeline.run_with_engines().await). New tests/dispatch_queue.rs (13 tests including 1 sync TokenBucket) + tests/dispatcher.rs (12 tests). Workspace 646 → 676 (+30: 25 new integration + 5 new TokenBucket inline). All 4 gates PASS: cargo check --workspace + cargo test --workspace (676) + cargo clippy --workspace --all-targets -- -D warnings (clean after 17 lint fixes: 4 first-doc-paragraph splits, 3 const-fn promotions, 4 significant_drop_in_scrutinee resolved by explicit drop(guard), 2 map(..).unwrap_or → map_or refactors, 1 match-arms-identical-bodies collapsed into if-else, 1 missing # Errors section added, 3 doc-markdown backticks added around ISOLATED_SANDBOX/IN_PROCESS_RPC/action_kind, 1 cast_possible_truncation in tests replaced with 15_usize literal, 1 struct_excessive_bools allowed inline with reason) + cargo fmt --all -- --check. Spec deviations: (a) brief said hard cap should be against live enrolment count; TRUST SPEC applied — §11.1 'Max share 50%' is capacity-relative (the lone-AI-submission edge case forces this reading). (b) brief mentioned subject_type ∈ human distinction for §3.5; aios-action Identity has only is_ai bool today, so T-029 defaults non-AI/non-recovery to Interactive (Background classification queued for T-030's HydratedSubject). (c) the §11.4 downgrade marker is logged via ActionDispatcher::apply_ai_interactive_downgrade but evidence emission itself is T-031 — the marker constant is the evidence record type name to ensure forensic continuity. Scope guard held: status.rs / dispatch.rs / failure.rs / adapter_manifest.rs / context.rs / adapter_registry.rs / adapter_handle.rs UNTOUCHED. error.rs gained exactly the 2 specified variants. pipeline.rs additions are pure additions (the T-027/T-028 sync paths preserved verbatim). runtime.rs additions are pure additions. M4 6/10 complete; T-030 policy integration (S10.1 ↔ S2.3, wires aios_policy::PolicyKernel into step_policy_evaluate + replaces RuntimeContext.subject_id with HydratedSubject) next.",
  "current_step_T028": "T-028 landed. New crates/aios-capability-runtime/src/adapter_registry.rs (~470 LoC, 5 inline unit tests for hex codec). SignedManifestBody<'a> view over AdapterManifest fields 1..11 (adapter_id, adapter_version, vendor, name, declared_stability, io_mode, dispatch_kind, declared_actions, declared_invariants_supported, default_adapter_timeout_seconds, default_sandbox_profile_id) — excludes fields 12-15 (adapter_signature, signing_key_id, manifest_created_at, manifest_expires_at) per §10.2 \"Ed25519 over JCS of fields 1..11\". pub fn canonical_signed_manifest_bytes(manifest) -> Result<Vec<u8>, RuntimeError> wraps serde_json::to_vec deterministically. pub fn encode_hex_signature(sig: &[u8;64]) -> String + private decode_hex_signature(hex_lower: &str) -> Option<[u8;64]> enforce §10.1 field 12 lower-hex 128-char discipline (UPPER-CASE rejected). RegisteredAdapter {manifest: AdapterManifest, registered_at: DateTime<Utc>} carries the registry's ingest timestamp (distinct from manifest_created_at issuance + manifest_expires_at revocation horizon). InMemoryAdapterRegistry holds trusted_authorities: HashMap<String, VerifyingKey> + adapters: tokio::sync::RwLock<HashMap<String, Arc<RegisteredAdapter>>>. Constructors: new(trusted) / empty(). Async API: register(manifest, now) -> Result<(), RuntimeError> runs 5 ordered checks (1. authority lookup → AdapterUnknownAuthority; 2. signature hex decode → AdapterSignatureInvalid; 3. Ed25519 verify over canonical_signed_manifest_bytes → AdapterSignatureInvalid; 4. uniqueness under write lock → AdapterAlreadyRegistered; 5. insert behind Arc + stamp registered_at). lookup_by_id(adapter_id) → Option<RegisteredAdapter> — AdapterStability::Retired collapses to None (§3.4 FAIL_CLOSED, \"no new dispatches accepted\"). lookup_for_target(action_kind) → Option<RegisteredAdapter> — scans declared_actions across all manifests, skips Retired, first match wins (§10.5 exclusivity guarantees uniqueness when present). list() → Vec<RegisteredAdapter> includes retired (forensic ListAdapters per §5.1). len/is_empty/trusts_authority inspectors. impl AdapterRegistry for InMemoryAdapterRegistry — sync trait method uses adapters.try_read().ok()?; returns Arc<dyn AdapterHandle> wrapping a fresh RealAdapterHandle (Arc<AdapterManifest>); momentarily-unreadable registry returns None (caller observes adapter-not-found and FAIL_CLOSEs). New src/adapter_handle.rs (~70 LoC). RealAdapterHandle {manifest: Arc<AdapterManifest>} implements AdapterHandle::dispatch_kind by reading manifest.dispatch_kind. Accessor manifest() -> &AdapterManifest for T-029's dispatcher to read declared_actions / default_adapter_timeout_seconds / default_sandbox_profile_id. New tests/adapter_registry.rs (16 tests) covers brief's 12 mandatory behaviours: (1) register valid signed manifest succeeds; (2) tampered hex byte → AdapterSignatureInvalid; (2b — bonus) post-sign body mutation → AdapterSignatureInvalid; (3) unknown signing_key_id → AdapterUnknownAuthority(name); (4) duplicate adapter_id → AdapterAlreadyRegistered(id); (5) lookup_by_id present → Some; (6) lookup_by_id absent → None; (7) lookup_by_id retired → None (FAIL_CLOSED); (8) lookup_for_target match → Some; (9) lookup_for_target miss → None; (10) list() returns all incl. retired; (11) Arc<InMemoryAdapterRegistry> coerces to Arc<dyn AdapterRegistry> + trait lookup hits known kind / misses unknown kind; (12) InMemoryCapabilityRuntime.with_adapter_registry + submit_action with unknown target → ActionContext.status = FAILED + error = DependencyUnready; bonus regression test: runtime without registry preserves T-027 baseline (unknown kind still drives to SUCCEEDED via stub steps). Modified files: src/error.rs (+ AdapterUnknownAuthority(String) + AdapterAlreadyRegistered(String); T-026 variants unchanged). src/lib.rs (+ pub mod adapter_handle + pub mod adapter_registry + re-exports RealAdapterHandle, InMemoryAdapterRegistry, RegisteredAdapter, canonical_signed_manifest_bytes, encode_hex_signature). src/pipeline.rs (added run_with_registry method; step_execute signature now takes registry: Option<&InMemoryAdapterRegistry> — when Some, looks up via the synchronous trait method and on miss transitions QUEUED → FAILED (T14) with ExecutionFailureReason::DependencyUnready + ShortCircuit; when None, structural pass-through preserved (T-027 contract). Old `pub fn run` retained as thin delegate to run_with_registry(.., None).). src/runtime.rs (+ adapter_registry: Option<Arc<InMemoryAdapterRegistry>> field on InMemoryCapabilityRuntime + with_adapter_registry chainable ctor + adapter_registry() const accessor; submit_action passes self.adapter_registry.as_deref() into pipeline.run_with_registry). Cargo.toml (+ ed25519-dalek workspace + base64 workspace to [dependencies]; + rand_core workspace to [dev-dependencies]). Workspace 625 → 646 (+21: 16 new integration tests + 5 new hex codec unit tests). All 4 gates PASS: cargo check --workspace + cargo test --workspace (646) + cargo clippy --workspace --all-targets -- -D warnings (clean after 17 lint fixes: 13 doc-markdown backticks around FAIL_CLOSED / hex_lower / adapter_id / step_execute identifiers, 4 significant_drop_in_scrutinee resolved by explicit drop(guard) before return clones, 1 missing_const_for_fn promoted on adapter_registry() accessor, 1 cast_possible_truncation allowed inline for the test fixture's u8 wrap, 1 doc_lazy_continuation fixed by collapsing multi-line list item in integration test header, 1 redundant_clone resolved by explicit Arc::clone + keeping concrete in scope) + cargo fmt --all -- --check. Spec deviation reports: (a) brief said T-028 belongs to S10.1 §4 — spec §4 is the FSM lifecycle table; §10 is the adapter manifest contract and §10.2 is the signature discipline. TRUST SPEC applied; section reference updated to §10. (b) brief instructed ExecutionFailureReason::AdapterUnknown on registry miss — §3.6 is a closed 12-variant enum that does NOT declare AdapterUnknown; failure.rs is in T-028's disallowed-modify list (it is a T-026 file). TRUST SPEC applied: T-028 uses ExecutionFailureReason::DependencyUnready as the closest spec-pinned surrogate (adapter not registered = dependency not in a ready state at dispatch); the surrogate choice is documented in runtime.rs and pipeline.rs with the explicit reasoning. A future spec extension may introduce a dedicated reason; T-029 / T-035 will reconcile when the gRPC RuntimeErrorCode::UnknownAdapter is wired. (c) brief said `lookup_by_id` should also exclude `AdapterStability::Removed` — §3.4 ladder closes on `RETIRED` and does not declare `REMOVED`. TRUST SPEC applied: only Retired collapses to None. Scope guard held: T-026 files (status.rs, dispatch.rs, failure.rs, adapter_manifest.rs, context.rs) UNTOUCHED. error.rs gained exactly the 2 specified variants. pipeline.rs change limited to step_execute signature + the registry lookup short-circuit + the new run_with_registry entry. runtime.rs change limited to adapter_registry field + with_adapter_registry ctor + accessor + 3-line submit_action route change. T-028 does NOT yet enforce §10.5 action-kind exclusivity collision rejection, §10.4 manifest_expires_at watchdog auto-de-registration, §10.3 stability promotion path (runtime.adapter.set_stability typed action), or publisher endorsement domain checks (§10.2 step 2) — all queued.",
  "current_step_T027": "T-027 landed. New crates/aios-capability-runtime/src/pipeline.rs (~560 LoC). TRANSITIONS const &[(ActionLifecycleState, ActionLifecycleState)] holds the §4.2 exhaustive transition table (20 rows T2..T21; T1 is the constructor and has no `from`). apply_transition(ctx, to, now) -> Result<(), RuntimeError> is the single writer; mismatched (from,to) → RuntimeError::InvalidTransition{from,to}. PipelineState enum {Continue(ActionContext), ShortCircuit(ActionContext)} with context()/into_context() helpers. ActionLifecyclePipeline (zero-sized stateless driver) with run(envelope, ctx, now) chaining 8 steps: step_validate (REAL — rejects empty action/empty subject_id → T3 to FAILED + EnvelopeValidationFailed), step_policy_evaluate (T-030 STUB — unconditional T4 to APPROVED), step_request_approval (T-034 STUB — Continue passthrough), step_queue (T-029 STUB — T12 to QUEUED), step_execute (T-028/T-029 STUB — T13 to EXECUTING), step_verify (STUB — T15 + T17 drives EXECUTING→VERIFYING→SUCCEEDED + ShortCircuit since SUCCEEDED is strict terminal), step_rollback (T-032 STUB — Continue passthrough), step_emit_evidence (T-031 STUB — Continue passthrough). fresh_context(action_id, now) const fn helper seeds ActionContext with SubprocessFork + Interactive defaults. New src/runtime.rs (~280 LoC). CapabilityRuntime async trait {submit_action(&self, envelope, context) -> Result<ActionContext>, get_action_status(&self, action_id) -> Result<ActionContext>}. RuntimeContext {subject_id (opaque, T-030 swaps for HydratedSubject), bundle_version, code_version, adapter_registry: Option<Arc<dyn AdapterRegistry>>} + with_adapter_registry chainable ctor. AdapterRegistry marker trait (lookup(action_kind) -> Option<Arc<dyn AdapterHandle>>) + NoOpAdapterRegistry impl. AdapterHandle marker trait (dispatch_kind() -> ActionDispatchKind) + NoOpAdapterHandle impl. InMemoryCapabilityRuntime struct (pipeline: ActionLifecyclePipeline + contexts: Arc<RwLock<HashMap<ActionId, ActionContext>>>) implements CapabilityRuntime: submit_action mints ActionId::new, drives pipeline, stores final ctx; get_action_status reads from map or ActionNotFound. Cargo.toml +tokio (workspace) +async-trait (workspace) dep additions; dev-dep tokio with macros+rt-multi-thread+sync features for the concurrent test. lib.rs +pub mod pipeline/runtime + re-exports apply_transition/fresh_context/ActionLifecyclePipeline/PipelineState/TRANSITIONS/AdapterHandle/AdapterRegistry/CapabilityRuntime/InMemoryCapabilityRuntime/NoOpAdapterHandle/NoOpAdapterRegistry/RuntimeContext. Tests: 12 new in tests/pipeline.rs: transitions_table_contains_exactly_the_spec_listed_rows (asserts TRANSITIONS.len()==20 + spot-checks T17/T19/T20/T21), pipeline_short_circuits_to_failed_on_empty_action_kind, pipeline_short_circuits_to_failed_on_empty_subject_id, pipeline_drives_clean_envelope_to_succeeded, in_memory_runtime_submit_action_returns_populated_context, get_action_status_returns_persisted_context, get_action_status_returns_action_not_found_for_unknown_id, apply_transition_rejects_created_to_succeeded_jump, apply_transition_accepts_created_to_policy_pending, succeeded_is_strict_terminal_and_rejects_every_outbound_transition, concurrent_submissions_of_distinct_envelopes_do_not_collide (#[tokio::test(flavor=multi_thread, worker_threads=2)] + tokio::join!), all_strict_terminals_reject_every_outbound_transition. Workspace 613 → 625 (+12). All 4 gates PASS: cargo check --workspace + cargo test --workspace (625) + cargo clippy --workspace --all-targets -- -D warnings (clean after 13 lint fixes: 6 doc-list overindent fixes in module header, 3 missing-backtick fixes on Decision::Deny/ShortCircuit/step_queue doc identifiers, 3 missing-const-for-fn opt-out on T-031/T-032/T-034 stub methods (signature will need mut once successor lands; reason= explicit), 1 const-fn promotion of fresh_context, 1 items-after-statements fix in the all-terminals test (use moved to function top)) + cargo fmt --all -- --check. Scope guard held: T-026 files (status.rs/dispatch.rs/failure.rs/context.rs/adapter_manifest.rs/error.rs) untouched except lib.rs gained 2 module decls + 2 re-export groups. No real policy integration (T-030 deferred), no real evidence integration (T-031 deferred), no adapter registry beyond marker trait (T-028 deferred), no gRPC (T-033 deferred), no rollback driver (T-032 deferred), no approval orchestrator (T-034 deferred). Spec deviation report: brief asked for the policy crate's HydratedSubject as the RuntimeContext.subject type; aios-policy is not yet a direct dep of aios-capability-runtime (T-030 wires it), so the T-027 context holds an opaque subject_id String. Will swap to aios_policy::HydratedSubject in T-030 when the policy dep edge is added.",
  "current_step_T026": "T-026 landed. M4 opens with the types-only skeleton, mirrors M3 T-016 pattern. New crate crates/aios-capability-runtime v0.0.1 registered in workspace members. Cargo.toml deps: aios-action (path) + serde + serde_json + thiserror + chrono + strum + strum_macros + ulid (workspace pins). Module layout: src/lib.rs (#![forbid(unsafe_code)] + module decls + re-exports) + src/status.rs (ActionLifecycleState 14-variant FSM enum per §3.1, SCREAMING_SNAKE_CASE wire form, EnumIter+EnumCount; is_terminal() const fn returns true for the 4 strict terminals per §4.2 forbidden-transition table: Succeeded, RolledBack, RollbackFailed, OverrideDenied. POLICY_DENIED and FAILED deliberately NOT strict terminals — §4.2 T21 allows POLICY_DENIED → OVERRIDE_PENDING and T19/T20 allow FAILED → ROLLED_BACK / ROLLBACK_FAILED) + src/dispatch.rs (4 closed enums: ActionDispatchKind 4-variant §3.2 / AdapterIOMode 2-variant §3.3 / AdapterStability 5-variant §3.4 / QueueClass 4-variant §3.5, all SCREAMING_SNAKE_CASE serde) + src/failure.rs (3 closed enums: ExecutionFailureReason 12-variant §3.6 / RollbackOutcome 4-variant §3.7 / RuntimeErrorCode 20-variant §3.8) + src/adapter_manifest.rs (AdapterManifest struct per §10.1 — actual spec section, not §4 as brief assumed; brief STOP condition triggered → TRUST SPEC applied. Fields: adapter_id, adapter_version, vendor, name, declared_stability, io_mode, dispatch_kind, declared_actions (Vec<AdapterActionDeclaration> with action_kind/target_schema/response_schema/rollback_strategy/timeout_seconds/template_string/template_substitution_variables), declared_invariants_supported, default_adapter_timeout_seconds, default_sandbox_profile_id, adapter_signature, signing_key_id, manifest_created_at, manifest_expires_at. #[serde(deny_unknown_fields)] on both structs) + src/context.rs (ActionContext struct: action_id (aios_action::ActionId), status (ActionLifecycleState), dispatch_kind, queue_class, created_at, last_updated_at, error: Option<ExecutionFailureReason>, rollback_outcome: Option<RollbackOutcome>, evidence_chain: Vec<String>; ActionContext::new const fn ctor seeds to Created/None/None/empty) + src/error.rs (RuntimeError thiserror taxonomy: ActionNotFound(ActionId) / InvalidTransition{from, to} / AdapterUnknown(String) / AdapterSignatureInvalid / ManifestInvalid(String) / Internal(String); canonical English Display strings pinned in tests). Tests: 14 in tests/types_roundtrip.rs — fourteen-state invariant (ActionLifecycleState::COUNT==14 via EnumCount), terminal classification (4 strict terminals match spec §4.2), 14-state SCREAMING_SNAKE_CASE round-trip with literal wire-form spot checks, ActionDispatchKind/AdapterIOMode/AdapterStability/QueueClass/ExecutionFailureReason/RollbackOutcome/RuntimeErrorCode round-trips with COUNT assertions matching spec, AdapterManifest round-trip + deny_unknown_fields rejection, ActionContext round-trip + fresh-context invariants, RuntimeError canonical English Display strings, cross-crate import smoke test (aios_action::ActionEnvelope + ActionId type paths resolve). Workspace 599 → 613 (+14). All 4 gates PASS: cargo check --workspace + cargo test --workspace (613) + cargo clippy --workspace --all-targets -- -D warnings (clean after 6 lint fixes: 4 missing-backticks in doc comments + 1 missing-const-for-fn on ActionContext::new + 1 doc-link rename — plus tests/types_roundtrip.rs gained the standard #![allow(clippy::expect_used, clippy::panic, clippy::no_effect_underscore_binding)] prelude mirroring aios-policy/tests/types_roundtrip.rs) + cargo fmt --all -- --check. Scope guard held: no trait impls, no gRPC, no proto file, no adapter registry, no dispatch queue, no policy/evidence integration — all queued for T-027..T-035. Brief spec deviation report: §4 in spec is the FSM diagram + allowed-transition table; §10.1 holds the AdapterManifest schema. Brief said '§4 AdapterManifest' — TRUST SPEC applied and used §10.1. ActionLifecycleState (per spec §3.1) used as the canonical type name, not 'ActionStatus' from brief — public re-export still ergonomic via crate root. Re-exports: ActionLifecycleState, ActionDispatchKind, AdapterIOMode, AdapterStability, QueueClass, ExecutionFailureReason, RollbackOutcome, RuntimeErrorCode, AdapterManifest, ActionContext, RuntimeError. M4 next: T-027 CapabilityRuntime async trait + lifecycle pipeline driver (mirrors aios-policy T-017 pattern).",
  "_T025_carryforward_note": "T-025 closed — aios-policy v0.1.0 emergency override boundary + RollbackBundle RPC + §22 acceptance fixtures (M3 CLOSER).",
  "_legacy_current_task_T025_title": "T-025 closed — aios-policy v0.1.0 emergency override boundary + RollbackBundle RPC + §22 acceptance fixtures (M3 CLOSER).",
  "current_step_T025": "T-025 landed. M3 (T-016..T-025, 10 tasks) CLOSED honestly per 'no debt across milestones' discipline. New crates/aios-policy/src/override_boundary.rs (~430 LoC, 9 inline unit tests). EmergencyOverride receipt struct (override_id `ovr_<ULID>`, granted_by_subject_id, granted_at, expires_at, scope, reason, revoked). OverrideScope (rule_id, action non-empty, optional subjects[]). OverrideRequest carries granted_by_subject (HydratedSubject), scope, reason, ttl_seconds, attempted_hard_deny (Option<HardDenyClass>) — Some(_) triggers §16.2 rejection at grant time. OverrideError typed enum: HardDenyCannotBeOverridden(class) / HumanOnly{granted_by} / TtlExceeded{requested,max} / TtlZero / ScopeInvalid — each pinned to a §16.2 / §16.3 row. OverrideBoundary holds Arc<RwLock<HashMap<String, EmergencyOverride>>>; request_override(req) -> Result; request_override_at(req, now) for test determinism; is_overridden(action, subject_id) / is_overridden_at(action, subject_id, now) lookup by scope; revoke(override_id) -> bool; invalidate_for_bundle_flip() -> usize (§16.3); prune_expired(now) -> usize; len/is_empty/snapshot helpers. MAX_OVERRIDE_TTL_SECONDS = 24*60*60 (§16.3 24h cap). Pipeline step 5 wired: pipeline.rs adds DecisionPipeline::step_5_emergency_override_with_boundary + new public driver entry evaluate_with_chain_full(envelope, original_ctx, hydrated_ctx, engine, boundary). Old evaluate_with_chain remains a thin wrapper passing boundary=None (T-017..T-024 baseline preserved). On match the step emits Decision::Allow with reason_code=EmergencyOverrideRelaxed and reason_message containing override_id + granted_by + reason for evidence linkage. Kernel InMemoryPolicyKernel gained 3 fields: rollback_stack (Arc<RwLock<Vec<PolicyBundle>>>) + rollback_capacity (default 8 per DEFAULT_ROLLBACK_STACK_CAPACITY const) + override_boundary (Option<Arc<OverrideBoundary>>). set_active_bundle now pushes displaced bundle onto bounded stack (ring-buffers at capacity) + invalidates active override grants per §16.3. New rollback_active_bundle() -> Option<(PolicyBundle, Option<String>)> pops stack, swaps active pointer, clears override grants. with_rollback_capacity(usize) + with_override_boundary(Arc<OverrideBoundary>) + override_boundary_handle() + rollback_stack_depth/capacity inspectors. evaluate_policy now uses evaluate_with_chain_full and passes the override boundary. RollbackBundle RPC wired in src/service/server.rs (~70 LoC replaces the Unimplemented stub): requires new_in_memory ctor (otherwise Unimplemented); empty stack → failed_precondition with §12.5 message; mismatched target_bundle_version → failed_precondition (best-effort restores displaced bundle); on success swaps service-level version pointer, invalidates cache for displaced version, mints evr_rb_<ULID> evidence receipt id; returns RollbackBundleResponse{previous_bundle_version, current_bundle_version, evidence_receipt_id}. aios-policy Cargo.toml version 0.0.1 → 0.1.0 (M3 closure marker, matches aios-evidence M2 pattern). DEFAULT_CODE_VERSION constant in server.rs 0.0.1-T023 → 0.1.0-T025. lib.rs re-exports: EmergencyOverride, OverrideBoundary, OverrideError, OverrideRequest, OverrideScope, MAX_OVERRIDE_TTL_SECONDS. Tests: +44 net (11 tests/override_boundary.rs + 11 tests/acceptance_fixtures.rs covering all 10 §22 fixtures (22.9 split into 9a+9b sub-fixtures) + 6 tests/rollback_bundle.rs + 6 tests/m3_closure.rs + 10 inline override_boundary module). Workspace 555 → 599. Spec deviations: brief assumed §22 fixtures all wire end-to-end through scoped-allow rule matching; spec §22.1 / §22.6 / §22.7 / §22.8 / §22.10 exercise paths still partially stubbed in the M3 pipeline (scoped-allow rule index, subject-group cycle detection, degraded-mode operator flag, per-evaluation budget enforcement, hot-reload in-flight version pinning). TRUST SPEC applied — each fixture is asserted at the level the M3 pipeline can presently demonstrate (the constitutional contract under test) and the tests document the M4+ continuation point in inline comments. §16.2 enforcement: ALL §6 hard-deny classes (including the two recovery-mode-overridable rows) are rejected by this boundary at grant time. The two recovery-mode rows have their own flow in 05_emergency_override.md which is M4+ scope. All gates: cargo check + cargo test --workspace (599 PASS) + cargo clippy --workspace --all-targets -- -D warnings (PASS) + cargo fmt --all -- --check (PASS) + cargo bench -p aios-policy --no-run (PASS). Scope guard held: hard_deny_engine.rs / conditions*.rs / constraints.rs / bundle.rs / bundle_loader.rs / subject_hydration.rs / subject.rs / decision.rs / error.rs / precedence.rs / hard_deny.rs / snapshot.rs / cache.rs / explain.rs ALL UNTOUCHED. Pipeline modification limited to step 5 + the new driver entry that threads the boundary. M3 closed honestly: T-016..T-025 = 10 tasks complete, no debt carried into M4.",
  "current_step_T023": "T-023 landed. Spec deviation: brief assumed §14 for gRPC; spec §14 is Simulation, the gRPC surface lives at §20 + Appendix A. TRUST SPEC applied — implemented the verbatim §20 6-RPC list (EvaluatePolicy / SimulatePolicy / LoadBundle / RollbackBundle / ExplainDecision / GetPolicyEngineInfo). New crates/aios-policy/proto/aios.policy.v1alpha1.proto (verbatim Appendix A IDL). New crates/aios-policy/build.rs (tonic-build invocation mirroring aios-evidence T-011 pattern — protoc-bin-vendored override + rerun-if-changed). Cargo.toml +tonic +prost +prost-types runtime deps; +tonic-build +protoc-bin-vendored build-deps. lib.rs +pub mod service. New src/service/mod.rs (~100 LoC): proto submodule via tonic::include_proto! with the canonical clippy::all+pedantic+nursery+missing_docs allow block copied from aios-evidence; re-exports PolicyKernelClient / PolicyKernelGrpc / PolicyKernelGrpcServer / PolicyKernelService / build_router / serve / DEFAULT_ENGINE_ID / SCHEMA_VERSION. New conversions.rs (~700 LoC, 7 inline unit tests): datetime <-> Timestamp; decision_to_proto / decision_from_proto (const fn 4-variant bijection); 5 closed-string bridges (NetworkPolicy / SessionClass / EvidenceGrade SCREAMING_SNAKE_CASE; ApprovalScope / ApproverClass lowercase per spec); constraints_to_proto / constraints_from_proto (11-field round-trip, expires_at is audit helper with no wire counterpart); approval_to_proto / approval_from_proto (5-field round-trip, unknown approver_class tokens silently dropped for forward-compat); policy_decision_to_proto / policy_decision_from_proto (14-field round-trip; chrono ↔ Timestamp; ActionId via aios_action::ActionId::parse with default() fallback for wire defects); policy_error_to_status (S2.3 §18.2 PolicyError → tonic::Status mapping — SubjectUnauthenticated → Unauthenticated; EnrichmentUnavailable / BundleLoad / BundleSignatureInvalid / BundleVersionMismatch / BundleUnknownAuthority → FailedPrecondition; SchemaInvalid / ConstraintsInvalid / ConditionParse / ConditionEval / InvalidPolicyBundle → InvalidArgument); envelope_to_bytes / envelope_from_bytes (JSON placeholder wire format for EvaluatePolicyRequest.envelope_proto bytes per open task #26 — single touch-point for the future aios-action proto IDL swap). New server.rs (~330 LoC, 4 inline unit tests): PolicyKernelService struct (Arc<dyn PolicyKernel> + engine_id + code_version + active_bundle_version + default_subject + started_at); DEFAULT_ENGINE_ID / DEFAULT_CODE_VERSION / DEFAULT_BUNDLE_VERSION constants. check_schema_version() accepts empty or canonical aios.policy.v1alpha1; mismatch → FailedPrecondition. build_context() anchors EnrichmentSnapshot.snapshot_id on env.request.request_hash() — until T-024 wires real AIOS-FS enrichment-snapshot ids, the request hash is the determinism anchor's first component so triple-collapsing maintains §13. evaluate_internal() drives the kernel for both EvaluatePolicy + SimulatePolicy (simulated arg flips PolicyDecision.simulated). impl proto::policy_kernel_server::PolicyKernel: EvaluatePolicy + SimulatePolicy + GetPolicyEngineInfo REAL; LoadBundle / RollbackBundle / ExplainDecision return Status::unimplemented with explicit T-024/T-025 deferral message. build_router(svc) + serve(svc, addr) bootstrap helpers. New tests/grpc_roundtrip.rs (11 tests): pick_port + spawn_server harness (oneshot shutdown, 50ms post-bind sleep — mirrors evidence T-011); happy_path returns fully-populated PolicyDecision; default_deny path; hard_deny disable_policy_kernel asserts HardDeny: prefix; unknown_subject maps to in-band DENY+SubjectUnauthenticated reason (typed error → Status::unauthenticated is verified in conversions unit tests); simulate_policy flips simulated=true; ai_privileged routes through wire; schema_version_mismatch → FailedPrecondition; empty envelope_proto → InvalidArgument; get_policy_engine_info; deferred_rpcs return Unimplemented; lib_reexports compile-time guard. Tests workspace 498 → 520 (+22 net). All 4 gates PASS. Scope guard held: kernel.rs / pipeline.rs / hard_deny_engine.rs / conditions*.rs / constraints.rs / bundle*.rs / subject_hydration.rs / decision.rs / error.rs / hard_deny.rs / subject.rs UNTOUCHED. Clippy notes: module-level #![allow(clippy::result_large_err)] on conversions.rs + server.rs (tonic::Status is intentionally wide; boxing fights tonic ergonomics). T-024 (cache + perf budget, §13 + §15), T-025 (M3 closer) deferred.",
  "_T021_carryforward_note": "T-021 closed — Subject hydration + AI self-approval prevention (S2.3 §7 + §17).",
  "current_step_T021": "T-021 landed. New crates/aios-policy/src/subject_hydration.rs (~300 LoC, 3 inline unit tests). SubjectHydrator async trait (single method hydrate(&self, provisional: &str) -> Result<HydratedSubject, PolicyError>) + InMemoryHydrator HashMap<String, HydratedRecord>-backed impl. HydratedRecord {subject, valid_until: Option<DateTime<Utc>>, revoked: bool} with chainable builder helpers HydratedRecord::new / .revoked() / .with_expiry(t). InMemoryHydrator::with_fixtures() pre-loads 4 canonical subject classes: human:lucky (Human, is_ai=false), agent:dev (Agent, is_ai=true — anchor for §17 fixtures), application:planner (Application, is_ai=true), service:systemd (Service, is_ai=false). Unknown / revoked / past-expiry records all collapse to PolicyError::SubjectUnauthenticated per §7 (the three failure modes deliberately not discriminated at the boundary). Blanket impl async_trait for Arc<T: SubjectHydrator + ?Sized + Send + Sync> so kernel can hold the hydrator behind Arc<dyn ...> and clone freely. Pipeline §3 step 2 wired: new DecisionPipeline::evaluate_with_chain(envelope, original_context, hydrated_context: Option<&PolicyContext>, engine) — hydrated_context = None signals SubjectUnauthenticated and the driver emits Decision::Deny + reason_code=SubjectUnauthenticated using the ORIGINAL context's bundle_version/enrichment_snapshot_id (so the rejected decision remains auditable). Old evaluate_with_hard_deny_engine becomes a thin wrapper passing Some(context). Kernel InMemoryPolicyKernel gains subject_hydrator: Option<Arc<dyn SubjectHydrator + Send + Sync>> field + 2 ctors: new_with_full_chain(hydrator, engine) + new_with_subject_hydrator(hydrator) (engine None). Existing new() + new_with_hard_deny(engine) ctors unchanged (T-017/T-018 baseline preserved). has_subject_hydrator() inspector helper. Dropped #[derive(Debug)] on InMemoryPolicyKernel (dyn trait field is not Debug); custom impl Debug prints `subject_hydrator: <bool>` instead — only loses the trait-object's Debug rendering, every other field is preserved. Kernel evaluate_policy now awaits hydrator.hydrate(envelope.identity.subject_canonical_id) BEFORE the sync pipeline driver, clones the original context, replaces .subject with the hydrated record, and passes the (Option<PolicyContext>) into evaluate_with_chain. Pipeline §17 evaluator landed as pure function evaluate_ai_self_approval_prevention(decision, subject, envelope) -> Option<ApprovalRequirement> + DecisionPipeline::apply_step_8(decision, subject, envelope) -> PolicyDecision that mints the upgrade. §17.1 risk-flag detection over envelope.request.target.risk.<flag> (destructive/privileged/network_exposure/secret_access/recovery_path_affected — 5 closed flags); §17.3 carve-out: all-flags-false AI subject keeps ALLOW unchanged. Upgrade preserves policy_decision_id + request_hash + bundle_version + enrichment_snapshot_id + evaluated_at; mutates decision → RequireApproval, reason_code → AiSelfApprovalUpgrade, reason_message → English §17 explanation, approval → {required:true, ExactRequestHash, ttl 300, approver_classes:[Human], require_human_co_signer:false}; rules_consulted bumps by 1 (saturating). new reason_code constants: SUBJECT_UNAUTHENTICATED, AI_SELF_APPROVAL_UPGRADE. Pipeline driver's step_8_ai_self_approval_prevention() remains a Continue no-op because the step-7 scoped-allow stub means no ALLOW partial state reaches step 8 on the end-to-end path until T-022's rule index lands; apply_step_8 is the public hook tests + future T-022 call. lib.rs re-exports: SubjectHydrator, InMemoryHydrator, HydratedRecord, evaluate_ai_self_approval_prevention. New tests/subject_hydration.rs (8 tests): fixtures_provide_the_four_canonical_subject_classes / hydration_is_deterministic_for_the_same_provisional_id / unknown_provisional_id_rejects_with_subject_unauthenticated / expired_record_rejects_with_subject_unauthenticated / dyn_subject_hydrator_through_arc_is_usable / kernel_with_hydrator_short_circuits_unknown_subject_to_subject_unauthenticated / kernel_with_hydrator_replaces_placeholder_with_hydrated_subject (verifies hydration runs + DefaultDeny floor still fires after success) / kernel_without_hydrator_uses_passed_subject_unchanged (backward compat T-017/T-018). New tests/ai_self_approval.rs (8 tests): ai_subject_with_privileged_action_upgrades_allow_to_require_approval / human_subject_with_privileged_action_remains_allow / ai_subject_with_deny_decision_remains_deny (§17.2 never downgrades) / ai_subject_with_no_risk_flags_keeps_allow_per_section_17_3 (§17.3 carve-out) / upgraded_decision_carries_human_approver_class (+ ExactRequestHash + required:true) / pure_evaluator_returns_some_only_for_ai_allow_with_risk (full truth-table for Decision × subject.is_ai × risk_flag) / upgrade_preserves_original_identity_fields_for_audit (decision_id + request_hash + bundle_version + snapshot id preserved + reason_code changes + rules_consulted+1) / application_subject_also_triggers_section_17_upgrade. Total +19 (3 inline + 8 + 8). Workspace 479 → 498. All 4 gates: cargo check + test (498) + clippy --workspace --all-targets -- -D warnings + fmt --all -- --check ALL PASS. Scope guard held: hard_deny_engine.rs / hard_deny.rs / conditions*.rs / constraints.rs / decision.rs / subject.rs / error.rs UNTOUCHED. Pipeline step 2 + step 8 are the ONLY pipeline modifications (kernel + lib re-exports + new subject_hydration module). T-023 (gRPC PolicyKernel surface, S2.3 §14), T-024 (cache + perf budget, §13 + §15), T-025 (M3 closer: override boundary + acceptance fixtures + v0.1.0 bump) deferred.",
  "_T022_carryforward_note": "T-022 closed — Policy bundle loader (S2.3 §12, signed + version-pinned).",
  "current_step_T022": "T-022 landed. New crates/aios-policy/src/bundle.rs (~250 LoC). PolicyBundle envelope per S2.3 §12: bundle_version (polb_<hex> content-addressed), bundle_id (operator label), created_at (DateTime<Utc>), signing_authority (trust-store key), signature_ed25519 (Vec<u8>, base64-STANDARD on JSON wire via private `mod base64_bytes` serde codec), rules (Vec<PolicyRule>). PolicyRule per §11.2 proto shape: rule_id, scope (RuleScope), effect (RuleEffect closed enum {Unspecified, Allow, Deny}), priority (i32, default 0), subjects (Vec<String>), actions (Vec<String>), conditions (Vec<String> — multiple §9.1 predicates ANDed per spec, NOT single string as brief implied), constraints (Option<Constraints>), approval (Option<ApprovalRequirement>), reason_code (String). RuleScope closed enum: Global / PerSubjectType / PerActionTarget / PerSubject / PerAction (orthogonal routing-hint over §5 ladder). SignedBundleBody<'a> view omits signature_ed25519 — canonical body bytes for Ed25519 sign+verify; bundle.canonical_signed_body_bytes() returns serde_json deterministic output. #[serde(deny_unknown_fields)] on both PolicyBundle + PolicyRule. New crates/aios-policy/src/bundle_loader.rs (~190 LoC). BundleLoader struct holds HashMap<String, VerifyingKey> trust store + Option<expected_bundle_version>. load_from_bytes(&[u8]) -> Result<PolicyBundle, PolicyError> runs 5 ordered checks: (1) serde_json parse → InvalidPolicyBundle(JSON deserialise: ...); (2) version pin compare → BundleVersionMismatch{expected,found}; (3) authority lookup → BundleUnknownAuthority(name); (4) Ed25519 verify over canonical_signed_body_bytes → BundleSignatureInvalid; (5) per-rule conditions_parser::parse + RuleEffect::Unspecified rejection → InvalidPolicyBundle(rule <id> condition: <err>). load_from_file(&Path) wraps std::fs::read. Constructors: new(trust)/with_version_pin(trust, version)/pin_version/clear_version_pin/expected_bundle_version/trusts_authority. PolicyError +4 variants: InvalidPolicyBundle(String) / BundleSignatureInvalid / BundleVersionMismatch{expected,found} / BundleUnknownAuthority(String) — all with canonical Display strings. Dep additions: aios-policy Cargo.toml +ed25519-dalek (workspace 2.1) +blake3 (workspace) +base64 0.22; dev-dep rand_core (workspace 0.6). Workspace Cargo.toml +base64 0.22 entry. lib.rs re-exports PolicyBundle, PolicyRule, RuleEffect, RuleScope, BundleLoader. Tests tests/bundle.rs (~280 LoC, 15 tests): happy round-trip, malformed JSON, per-rule condition parse failure (or operator triggers DisallowedToken), perturbed-body bad signature (replace human:lucky→human:alice without re-signing), unknown authority (empty trust store), version pin mismatch, version pin match (positive), empty rules, determinism (parse-twice byte-equal + re-serialise round-trip), positive signature material round-trip, RuleScope serde all 5 variants, 50-rule scale smoke, PolicyError Display strings, RuleEffect::Unspecified rejection, load_from_file. Workspace 464 → 479 (+15). All 4 gates PASS: cargo check + test (479) + clippy --workspace --all-targets -D warnings + fmt --all -- --check. Scope guard held: hard_deny_engine.rs / kernel.rs / pipeline.rs / conditions*.rs / constraints.rs / hard_deny.rs / subject.rs / decision.rs UNTOUCHED. Spec deviation report: brief asked for `condition: String` (single) per rule + RuleScope as global/per-subject-type/per-action-target closed enum; S2.3 §11.2 is `conditions[]` (list, multiple ANDed per §9.1 grammar). TRUST SPEC applied — rule keeps `conditions: Vec<String>`; scope kept as orthogonal routing-hint enum with the brief's three values + PerSubject + PerAction (additions consistent with §5 tier matching). RuleEffect::Unspecified rejection is an extra invariant beyond the brief — proto3 zero-value sentinel must not round-trip through the loader (mirrors Decision::Unspecified treatment in §4). M3 next candidate task: T-021 subject hydration + AI self-approval prevention (S2.3 §7 + §17) — fills the §3 step-3 stub + AI-upgrade rule at the §5 step-6 site.",
  "current_step_T019": "T-019 landed. New crates/aios-policy/src/conditions.rs (~700 LoC; 9 inline unit tests). Closed AST: Condition (conjunction of predicates per §9.1 AND-only EBNF — no Or, no Not, no parens variants, per spec deviation from brief — documented in module header), Predicate (Compare / In / Contains / Exists), CompareOp (Eq/Neq/Lt/Lte/Gt/Gte), Namespace (6 closed: Subject/Request/Target/Object/Time/System), ClosedField (48 typed variants enumerating §9.2 base + §26.1 Wave 4 + §26.6 Wave 9 + §27.1 Wave 5 + §28.1 Wave 6 + §29.1 Wave 17, plus TargetAdapterDeclared(String) escape hatch per §9.2 'adapter-declared target fields'), Value (String/Identifier/Int/Bool/Timestamp). ClosedField::resolve(ns, subpath) closes the closed-namespace contract: unknown subpath under subject/request/object/time/system → None; unknown subpath under target → admitted as TargetAdapterDeclared. New crates/aios-policy/src/conditions_parser.rs (~700 LoC; 4 inline unit tests). pub fn parse(&str) -> Result<Condition, ConditionParseError>. Hand-written recursive-descent (no pest/nom/combine added per §21 stack lock). ConditionParseError typed enum: UnexpectedEof / UnexpectedToken / UnknownNamespace / UnknownField / UnknownOperator / DisallowedToken (or/not/parens) / UnterminatedString / InvalidInteger / EmptyValueList. Every variant carries 1-based (line, column). Bare time.recovery_mode boolean sugar per §9.1 last alternative. Determinism: parser is left-to-right single-pass, no global state, no I/O — §13.1 contract pinned by 100-iter round-trip test. New crates/aios-policy/src/conditions_eval.rs (~600 LoC; 3 inline unit tests). EvalContext<'ctx> borrows (HydratedSubject, ActionEnvelope, EnrichmentSnapshot, ClockSnapshot — passed in for replay determinism). ConditionEvalError typed enum: TypeMismatch / UnsupportedOperator / HeterogeneousValueList. pub fn evaluate(&Condition, &EvalContext) -> Result<bool, ConditionEvalError> with left-to-right AND short-circuit on first false. resolve_field_value() maps every ClosedField to FieldValue (Str/StrList/Bool/Int/Absent). Wave 4+ subject fields + Request risk fields + target/object/system namespaces surface as Absent today (T-021/T-022 wire real lookups). target.<subpath> resolved against envelope.request.target serde_json::Value. Adapter-declared target sub-fields likewise — supports the §11.1 YAML sample rule (target.service in [\"nginx\", ...]). PolicyError +2 variants: ConditionParse(String) and ConditionEval(String). lib.rs re-exports: ClosedField, CompareOp, Condition, Namespace, Predicate, Value, evaluate_condition, ClockSnapshot, ConditionEvalError, EvalContext, parse_condition, ConditionParseError. Tests: +57 net (12 inline conditions module + 4 inline parser module + 3 inline eval module + 26 tests/conditions_parser.rs integration + 15 tests/conditions_eval.rs integration — minus 3 inline unit tests that overlap means 16 module + 41 integration = 57). Workspace 407 → 464. All 4 gates: cargo check + test (464) + clippy --workspace --all-targets -D warnings + fmt --all -- --check ALL PASS. Scope guard held: pipeline.rs / kernel.rs / precedence.rs / hard_deny.rs / hard_deny_engine.rs / constraints.rs / subject.rs UNTOUCHED. T-022 (bundle loader) will wire conditions into the bundle rule evaluator. T-021 (subject hydration), T-023 (gRPC), T-024 (cache), T-025 (override boundary) deferred.",
  "T018_carryforward_note": "T-018 closed — HardDenyEngine (S2.3 §6, 10 classes enforced; pipeline step 4 wired).",
  "current_step_T018": "T-018 landed. New crates/aios-policy/src/hard_deny_engine.rs (~620 LoC). HardDenyEngineConfig with 14 closed sets (secret_raw_read_actions, recursive_delete_actions, protected_roots {/, /home, /root, /aios}, recovery_partition_prefixes {/recovery, /boot/recovery, /aios/recovery}, policy_log_mutation_actions, evidence_log_mutation_actions, disable_policy_kernel_actions, disable_recovery_path_actions, modify_boot_chain_actions, untyped_shell_actions, privileged_groups {root, sudo, wheel, privileged, operators}, privileged_capabilities {shell.untyped, system_admin, root_shell}, aiosfs_pointer_rollback_actions, privacy_class_downgrade_actions). HardDenyEngine struct + new(config)/new_with_defaults()/check(envelope, subject) -> Option<HardDenyClass>. check() runs 10 detect_* private methods in §6 table order; first match wins. reason_code_for(HardDenyClass) -> 'HardDeny:<PascalCaseVariant>'. reason_message_for(class) -> human English; for the 2 overridable rows (ModifyBootChain, AiosFsPointerRollbackOnRecovery) appends '(override path: recovery-mode operator approval)' suffix. has_recovery_override_path(class) -> bool const helper for audit. privacy_rank(class) fails closed (unknown class ranks as u8::MAX). Pipeline integration: added step_4_evaluate_hard_denies_with_engine() in pipeline.rs (zero-arg stub kept for T-017 test compat). Added evaluate_with_hard_deny_engine(envelope, ctx, Option<&HardDenyEngine>) driver; evaluate() now thin wrapper delegating with engine=None. InMemoryPolicyKernel gained hard_deny_engine: Option<Arc<HardDenyEngine>> field + new_with_hard_deny(engine) ctor + has_hard_deny_engine() inspector; bare new() is unchanged (T-017 baseline preserved). Dropped Copy from InMemoryPolicyKernel (Arc field; Clone retained). Tests: +24 net (5 inline unit tests in hard_deny_engine module: privacy_rank ladder / normalise_path / reason_code_for variant strings / has_recovery_override_path coverage / overridable reason_message suffix; +19 integration tests in tests/hard_deny.rs covering all 10 §6 rows with positive+negative cases for secret/shell/privacy_class, recursive_delete_root protected-root + prefix + recovery-partition cases, override-path-suffix test, kernel-integration test asserting HardDeny:DisablePolicyKernel reason_code + rules_consulted=1, baseline-kernel-without-engine test asserting stub-pass-through preserved). Workspace 383 → 407 (+24). All gates: cargo check + test (407) + clippy --workspace --all-targets -D warnings + fmt --all -- --check ALL PASS. Scope guard held: kernel.rs only added new_with_hard_deny ctor + Arc field (no logic changes); precedence.rs/constraints.rs/subject.rs/hard_deny.rs ENUM untouched. Conditions parser (T-019), bundle loader (T-022), subject hydration (T-021), gRPC (T-023), cache (T-024), override boundary (T-025) deferred.",
  "current_step_T017": "T-017 landed: crates/aios-policy/src/{kernel.rs,pipeline.rs,precedence.rs} + tests/pipeline.rs. PolicyKernel async trait (evaluate_policy: ActionEnvelope + PolicyContext -> Result<PolicyDecision, PolicyError>), Send+Sync. PolicyContext (subject + EnrichmentSnapshot stub + bundle_version + code_version). DecisionPipeline (Copy, stateless across evals); 12 steps with stubs deliberate for steps 3,4,5,6,7,8,10,12 (each names the task that lands it). Steps 1+2+9+11 REAL. PipelineState enum (Continue / ShortCircuit(Box<PolicyDecision>)); Box because PolicyDecision is 14-field heavy and triggers large_enum_variant otherwise. EmitDecision parameter struct (Copy) keeps emit_decision arity below clippy::too_many_arguments threshold. reason_code module: SCHEMA_INVALID + DEFAULT_DENY constants. RulePrecedence: 7-variant enum (InvalidSubject, HardDeny, EmergencyOverrideDenylist, ExplicitScopedDeny, ExplicitScopedAllow, AiSelfApprovalUpgrade, DefaultDeny); EnumIter+EnumCount; COUNT==7 anchored. InMemoryPolicyKernel harness implements PolicyKernel by running DecisionPipeline. Deps added to Cargo.toml: async-trait, tokio (workspace), ulid (workspace). Tests: +10 (rule_precedence iter order; step 1 schema_invalid short-circuit; default-deny floor reached for well-formed envelope; all 14 PolicyDecision fields populated; bundle_version mirrors context; evaluated_at within 5s; consecutive evaluations mint distinct ULIDs but same request_hash; PipelineState short-circuit prevents later steps; step 9 always short-circuits; stub steps 3-8 return Continue). Workspace 369 -> 379. action_id is fresh ULID per evaluation; deterministic content-addressed binding deferred to Capability Runtime T-002/T-006. All 4 gates: cargo check + test (379) + clippy --workspace --all-targets -- -D warnings + fmt --all -- --check PASS.",
  "current_step": "T-015 closed. M2 (T-007 .. T-015) closed honestly per 'no debt across milestones' discipline. Compaction worker per S3.1 §11.5 / §12 / §13 landed. New crates/aios-evidence/src/compaction.rs (~600 LoC, 19 unit tests). Closed CompactionPolicy (Auto / OperatorApproval default per §11.5 / Disabled — serde wire tokens lowercase exact). CompactionWorker with sync tick(&mut self, backend, now) primitive; approve_segment(&SegmentId) for out-of-band operator authorisation; production wraps in tokio::time::interval task (documented in module header; not shipped). CompactionReport with eligible/compacted/awaiting/receipts_removed/index_entries_pruned counts (Serialize for §20 telemetry). CompactionBackend trait (list_sealed_segments + compact_segment + emit_approval_required) implemented for both backends. InMemoryEvidenceLog: new sealed_segments std::sync::Mutex<SealedStore>; new seal_current_segment(retention_class) method; warm_receipt_count/warm_index_count zero on compact (tombstone preserved §12); emit_approval_required appends through normal signed+chained pipeline via tokio blocking_write. RocksDbEvidenceLog: list scans chain_index→segments CFs (sync DB read); compact_segment one atomic WriteBatch (delete receipts CF rows + record_type_index CF rows; set compacted_at::<seg_id> sentinel in metadata CF — segment row in segments CF untouched so recover_sealed_chain on reopen continues to verify); emit_approval_required uses blocking_write on open-segment state; helper rocksdb_segment_compacted_at(db, seg_id) introspects sentinel. list_sealed_segments in BOTH backends filters out already-compacted segments so the worker does not re-emit approval-required every tick (operator UX + chain noise). RecordType +1 constitutional addition: CompactionApprovalRequired (wire ID 1000 — first entry in reserved Wave 14+ range; retention FOREVER per §11.5). Proto IDL: COMPACTION_APPROVAL_REQUIRED = 1000; reserved 1001 to 9999. Total RecordType variants = 428 (Wave 13 dense 1..=427 + Wave 14+ reserved 1 = 428). 6+ invariant tests updated to split Wave 13 (dense) from Wave 14+ (reserved). New encoding helper metadata_compacted_at_key returning the metadata-CF key; compaction state OUT of segments CF on purpose (preserves §11.4 cryptographic recovery byte-layout). Privacy ceiling: CompactionApprovalRequired added to is_constitutionally_public_record_type — operators always see compaction governance events. lib.rs re-exports: CompactionPolicy, CompactionWorker, CompactionReport, CompactionBackend, COMPACTION_WORKER_SUBJECT, is_compaction_eligible_for. Tests: 19 in compaction.rs + 6 in impl_inmemory.rs + 5 in rocksdb_backend.rs + 3 in tests/compaction_lifecycle.rs = +33 net (some prior tests adjusted for new field). Workspace 330 → 364 (+34). aios-evidence Cargo.toml version 0.0.1 → 0.1.0 (M2 closure marker). All gates: cargo build --workspace + cargo test --workspace (364 PASS) + cargo clippy --workspace --all-targets -- -D warnings (PASS) + cargo fmt --all -- --check (PASS).",
  "completed_steps_M1": ["T-001 .. T-006 — M1 closed at 102 tests"],
  "completed_steps_M2": [
    "T-007 .. T-014 — see prior memory snapshot (330 tests)",
    "T-015: retention-class compaction worker per S3.1 §11.5 / §12 / §13. New crates/aios-evidence/src/compaction.rs (~600 LoC, 19 unit tests). Closed CompactionPolicy + CompactionWorker + CompactionReport + CompactionBackend trait. Both InMemoryEvidenceLog and RocksDbEvidenceLog implement the trait. RecordType +1 (CompactionApprovalRequired, wire ID 1000, FOREVER). Proto reserved range tightened to 1001..9999. Total variants 428 (Wave 13 dense 1..=427 + Wave 14+ reserved 1). 6+ invariant tests updated (split Wave 13 dense from Wave 14+ reserved). +33 tests net. Workspace 330 → 364. aios-evidence Cargo.toml 0.0.1 → 0.1.0. Cold-tier offload + tokio interval task wrapping = carry-forward to M3+ (documented in module header). M2 closed honestly: T-007 .. T-015 = 9 tasks complete."
  ],
  "files_touched_M2_T015": [
    "crates/aios-evidence/Cargo.toml",
    "crates/aios-evidence/proto/aios.evidence.v1alpha1.proto",
    "crates/aios-evidence/src/lib.rs",
    "crates/aios-evidence/src/compaction.rs",
    "crates/aios-evidence/src/record.rs",
    "crates/aios-evidence/src/privacy.rs",
    "crates/aios-evidence/src/persistence/encoding.rs",
    "crates/aios-evidence/src/persistence/rocksdb_backend.rs",
    "crates/aios-evidence/src/service/impl_inmemory.rs",
    "crates/aios-evidence/src/service/conversions.rs",
    "crates/aios-evidence/tests/compaction_lifecycle.rs",
    ".ai/memory.json",
    ".ai/tasks.json",
    ".ai/progress.log",
    ".ai/global_memory.json"
  ],
  "discovered_constraints_T015": [
    "CompactionBackend trait is intentionally synchronous; both backends' impls bridge to tokio-locked state via blocking_write()/blocking_read(). Production drives the worker from a spawn_blocking task; the integration test uses tokio::task::spawn_blocking. Documented in compaction.rs module header.",
    "compact_segment MUST preserve the sealed-segment metadata row in the segments CF — the chain witness (seal hash + Ed25519 signature) is what keeps the cross-segment chain verifiable on reopen. Compaction state lives in a SEPARATE metadata CF sentinel `compacted_at::<segment_id>` so the segment-row byte layout is byte-identical to its pre-compaction form (§11.4 cryptographic recovery is untouched).",
    "list_sealed_segments in both backends filters out already-compacted segments — otherwise OperatorApproval mode would re-emit COMPACTION_APPROVAL_REQUIRED every tick for already-compacted segments (operator UX disaster + chain pollution). The in-memory backend uses warm_receipt_count > 0 as the predicate; the RocksDB backend uses rocksdb_segment_compacted_at(...).is_none().",
    "RecordType vocabulary expansion (Wave 13 dense 1..=427 + Wave 14+ reserved 1000..=9999) required splitting the wire_id_ranges test into two: wave_13_id_range_is_contiguous_and_dense (asserts 1..=427 stays dense) + wave_14_plus_ids_live_in_the_reserved_block (asserts every id > 427 is in 1000..=9999). The TOTAL_RECORD_TYPE_VARIANTS = WAVE_13_VARIANTS + WAVE_14_PLUS_VARIANTS bookkeeping pattern is the constitutional anchor for future Wave 14+ additions.",
    "Proto3 reserved-range tightening: changed `reserved 1000 to 9999` to `COMPACTION_APPROVAL_REQUIRED = 1000` + `reserved 1001 to 9999`. The reserved-range overlap with a defined value is a proto3 compile error; this split is the standard pattern.",
    "Auto-mode test against InMemoryEvidenceLog::seal_current_segment(retention_class) — note that the open-chain receipts (3 segments × N receipts) are sealed INTO three SealedSegment envelopes which carry their own retention_class. The CompactionWorker then sees one FOREVER segment + two STANDARD_24M segments; FOREVER never compacts regardless of wall-clock (is_compaction_eligible enforces this at the eligibility predicate, not at the policy level).",
    "Sealed segment receipts() returns the original appended receipts PLUS the terminal SEGMENT_SEALED witness minted by Segment::seal(). The witness is NOT in the receipts CF (it's only in the SealedSegment envelope on the segments CF). For RocksDB compact_segment, the WriteBatch iterates ALL receipts in the envelope including the witness — the witness's delete is a no-op on the receipts CF but the count returned reflects the iteration. Test assertions skip the witness when verifying ReadReceipt round-trips against appended receipts.",
    "Worker is purely sync + holds no Arc<Backend> reference. Tests construct one InMemoryEvidenceLog::clone() into the spawn_blocking closure and call tick(&mut backend, now) directly. Backend mutations are visible across clones because the underlying sealed_segments is Arc<Mutex<...>> (shared) and the chain state is Arc<RwLock<...>> (shared)."
  ],
  "last_verification_result": "PASS",
  "last_errors": [],
  "current_step_T016": "T-016 closed — aios-policy crate skeleton landed at v0.0.1 with 5 new tests (workspace 364 → 369). Crate structure: src/lib.rs + src/decision.rs (Decision 4-variant + PolicyDecision 14-field per S2.3 §4) + src/hard_deny.rs (HardDenyClass 10-variant w/ EnumIter+EnumCount, serde wire form `hd.<snake>`) + src/subject.rs (SubjectType 7-variant lowercase wire + HydratedSubject 7-field per §7) + src/constraints.rs (Constraints + ApprovalRequirement STUB structs — T-017 fills §10 11-field vocabulary) + src/error.rs (PolicyError: SubjectUnauthenticated/EnrichmentUnavailable/BundleLoad{reason}/SchemaInvalid{detail}). tests/types_roundtrip.rs: 5 tests anchor Decision active=3 + Unspecified sentinel, HardDenyClass::COUNT==10 invariant, PolicyDecision serde round-trip, HydratedSubject serde round-trip, PolicyError Display strings. Cross-crate type linkage anchored: PolicyDecision.action_id is aios_action::ActionId; PolicyDecision.evaluated_at is chrono::DateTime<Utc>. Cargo.toml workspace members updated (aios-policy added). All 4 gates PASS: cargo check + test (369) + clippy --all-targets -D warnings + fmt --check. Scope guard held: no PolicyKernel trait, no EvaluatePolicy, no gRPC, no bundle loading — all deferred to T-017+.",
  "files_touched_M3_T016": [
    "Cargo.toml",
    "Cargo.lock",
    "crates/aios-policy/Cargo.toml",
    "crates/aios-policy/src/lib.rs",
    "crates/aios-policy/src/decision.rs",
    "crates/aios-policy/src/hard_deny.rs",
    "crates/aios-policy/src/subject.rs",
    "crates/aios-policy/src/constraints.rs",
    "crates/aios-policy/src/error.rs",
    "crates/aios-policy/tests/types_roundtrip.rs",
    ".ai/memory.json",
    ".ai/progress.log"
  ],
  "discovered_constraints_T016": [
    "Workspace forbids clippy::expect_used + clippy::panic; integration tests must opt-in with `#![allow(clippy::expect_used, clippy::panic, reason = ...)]` per the established aios-action pattern. Skipped this initially and clippy gate failed loudly with 14 errors before fix.",
    "clippy::doc_markdown is enforced at workspace level; any bare crate identifier in doc-comments (`serde_json`, `PascalCase`, etc.) must be backticked. Caught two violations on first clippy pass.",
    "Decision enum uses #[serde(rename_all = SCREAMING_SNAKE_CASE)] so the wire form matches the proto3 IDL exactly (ALLOW / REQUIRE_APPROVAL / DENY / UNSPECIFIED). The Unspecified variant is the proto3 zero-value sentinel; it never participates in real evaluation per the no-silent-fall-through rule of S2.3 §3.",
    "HardDenyClass serde wire form uses #[serde(rename = \"hd.<snake>\")] so external bundle YAML/proto encoding uses the constitutional spec keys, not the Rust PascalCase variant names. EnumIter + EnumCount derived from strum_macros (already in workspace) gives the COUNT == 10 compile-time invariant anchor."
  ],
  "discovered_constraints_T054": [
    "aios-vault SubjectType::LocalOperator maps to aios_policy::SubjectType::RemoteOperator at the policy boundary because aios-policy T-025 has no LocalOperator variant; recovery_mode plus session_class=RECOVERY preserves the local recovery context.",
    "aios-evidence receipt::tests::t009_verify_signature_rejects_uppercase_hex is flaky: it uppercases the first 4 random signature hex chars, and if those chars are all digits the signature is unchanged. T-054 did not modify aios-evidence due scope guard; targeted rerun and full workspace rerun passed."
  ],
  "notes": "M3 opened with T-016 (aios-policy crate skeleton). 369 workspace tests. Next task T-017 candidate scope: PolicyKernel trait + EvaluatePolicy signature + bundle loading skeleton (signed YAML reader, version pinning, schema validation entry points). Conditions parser (S2.3 §9) and full Constraints vocabulary (S2.3 §10) likely T-018 / T-019."
}
