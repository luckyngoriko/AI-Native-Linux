{
  "backlog": [
    {
      "id": "T-003",
      "title": "aios-action extended ULID prefix registry (S5.3/S5.4/S10.1/S15.2 namespaces)",
      "status": "pending",
      "priority": "P1",
      "dependencies": ["T-001"],
      "acceptance_criteria": [
        "ApprovalRequestId (apprq_), ApprovalBindingId (appb_), OverrideRequestId (ovrq_), OverrideBindingId (ovr_), ActionRuntimeRequestId (actrq_), TransitionPlanId (tplan_)",
        "tplan_ uses 32-hex-char body (BLAKE3 truncation, not ULID) per S0.1 §3.2.2",
        "All round-trip tested, all reject colon-separator form"
      ],
      "blockers": []
    },
    {
      "id": "T-004",
      "title": "aios-action lifecycle FSM with conditions consistency",
      "status": "pending",
      "priority": "P0",
      "dependencies": ["T-001"],
      "acceptance_criteria": [
        "Condition vocabulary enum (PolicyEvaluated, PolicyAccepted, ApprovalRequired, ApprovalGranted, ApprovalExpired, Sandboxed, Executed, Verified, RollbackPossible, RolledBack, Idempotent, Simulated, DeprecatedFieldUsed)",
        "ActionEnvelope::transition_to(phase) method enforcing S0.1 §6.2 valid transitions",
        "S0.1 §6.6 phase ↔ conditions canonicalization function",
        "Monotonicity invariants enforced (phase_changed_at non-decreasing, conditions never removed)"
      ],
      "blockers": []
    },
    {
      "id": "T-005",
      "title": "aios-action full error taxonomy (~30 PascalCase codes)",
      "status": "pending",
      "priority": "P1",
      "dependencies": ["T-001"],
      "acceptance_criteria": [
        "All canonical codes from S0.1 §7.3 (validation, policy, auth, execution, verification, rollback, infrastructure groups)",
        "Each variant has retryable hint per S0.1 §7.4",
        "Error::cause chain bounded at depth 8 per S0.1 §7.5"
      ],
      "blockers": []
    },
    {
      "id": "T-006",
      "title": "aios-action integration tests + golden fixtures",
      "status": "pending",
      "priority": "P1",
      "dependencies": ["T-002", "T-004", "T-005"],
      "acceptance_criteria": [
        "Golden JSON fixtures for representative envelope shapes (successful path, policy denial, rollback)",
        "tests/ directory with integration tests",
        "JCS canonical encoding round-trip golden tests"
      ],
      "blockers": []
    }
  ],
  "completed": [
    {
      "id": "T-001",
      "title": "aios-action crate skeleton + S0.1 core types",
      "completed_at": "2026-05-23",
      "evidence": "cargo build/test/clippy --all-targets -D warnings all PASS; 10 unit tests green; 8 prefix-namespaced ID types + 5-phase FSM + 4-section envelope + 11-variant error subset"
    },
    {
      "id": "T-002",
      "title": "aios-action canonical encoding + BLAKE3 hashing per S0.1 §8.5",
      "completed_at": "2026-05-23",
      "evidence": "cargo build/test/clippy --workspace --all-targets -D warnings all PASS; 24 unit tests green (+14 new); canonical.rs (jcs_canonicalize + blake3_hash + blake3_truncated + CanonicalError); Request::request_hash; ActionEnvelope::idempotency_hash; no new workspace deps; JCS subset documented (lex key sort + no whitespace + array order preserved); ECMA-262 number normalization explicitly deferred to T-006"
    }
  ],
  "blocked": []
}
