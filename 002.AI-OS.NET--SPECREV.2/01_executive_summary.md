# Executive Summary — Rev.2

Status: `PARTIAL` (rev.2 contracts are landing incrementally)

## Purpose of rev.2

Rev.1 established the AIOS vision and the layer model. Rev.2 turns that vision into contract-grade specifications: schemas, state machines, error models, and verification rules that an implementer can build against without further interpretation.

## Rev.1 → Rev.2 delta

To be filled as sub-specs land. Currently tracking:

- **S0.2 (applied):** README's "Self-Evolving Backend" reframed as "Adaptive Backend" — AI proposes; humans approve; Policy Kernel, Evidence Log, Vault Broker, and recovery path are excluded from the proposal pipeline. See [DEC-001](02_design_decisions.md).
- **S0.1 (contract):** Action Envelope and Lifecycle contract. Scope: idempotency, causality, error envelope, lifecycle FSM, versioning, OpenTelemetry hooks, sandbox profile binding, dry-run mode.
- **S1.1 (contract):** Capability Translator. Refined 2026-05-08 with deltas D1–D9: explicit hash encoding (lowercase hex, BLAKE3-256, 32-char truncation aligned with S0.1 §8.5); adversarial-robustness section; statelessness contract; skinny performance contract bridging to S1.2; signed-bundle catalog distribution and trust chain; testable golden fixtures (including Bulgarian utterance and prompt-injection cases); appendix with complete proto IDL; tightened cross-spec field-level mapping; multi-language utterance handling. Refusal taxonomy extended with `TranslationTimeout`. Scope unchanged: capability catalog, RAG-over-capabilities, target binding, ambiguity handling, refusal rules, conservative risk hints, verification intent generation, and translation evidence.
- **S1.2 (contract):** Latency Tiering. Refined 2026-05-08 with deltas D1–D12: typed `LatencyRouter` service with proto IDL; quantitative per-tier latency budgets (T0–T4 p50/p95/p99 + hard timeouts); deterministic priority-ordered routing algorithm replacing the rev.1 condition table; closed `PrivacyClass` enum with per-class tier ceilings; tier upgrade/downgrade dynamics with single-upgrade cap and explicit timeout (no silent fallback); T0 caching semantics with cache key, TTL, and invalidation triggers; adversarial protection (per-subject rate limits, external-model budget guards, defense delegation); statelessness contract; user routing preferences as hints (never enforcement); telemetry contract with bounded label cardinality; full evidence chain (`routing_id` → `translation_id` → `action_id` → evidence receipts); golden routing fixtures including Bulgarian-utterance, secret-bearing, classified, and adversarial cases. Scope unchanged: deterministic direct path, local model path, powerful reasoning path, degradation, and routing evidence.
- **S1.3 (contract):** AIOS-FS object model and conflict resolution. Refined 2026-05-08 with deltas D1–D12: explicit hash encoding (full BLAKE3-256 lowercase hex for chunk IDs, no truncation; metadata digests follow S0.1 §8.5 truncation rule); proto IDL with `aios.fs.v1alpha1` package and `AIOSFSObjects` gRPC service; transaction record with multi-pointer atomicity (all-or-nothing); FastCDC chunking defaults (64KB/256KB/1MB) with fixed-size fallback; orphan chunk GC with 24h TTL and evidence-logged reaping; `PrivacyClass` field on objects (mirrors S1.2 §5); object lifecycle states (`ACTIVE`/`RETIRED`/`PURGED`) with retention semantics; pointer move CAS protocol with `ConflictDetected` error; three read consistency modes (`SNAPSHOT`/`LINEARIZABLE`/`EVENTUAL`); quarantine triggers/exits with evidence trail; closed CRDT vocabulary (`G_COUNTER`/`PN_COUNTER`/`OR_SET`/`LWW_REGISTER`/`OR_MAP`/`RGA_TEXT`); 30-day default conflict TTL with abandonment evidence; conflict notification (push via L9 stream + pull via `ListConflicts`); resolution authority enforced via L4 capabilities; AI merge proposal validation rules in code (not just discipline); golden fixtures and telemetry contracts for both files. Scope unchanged: immutable versions, content-addressed chunks, pointer promotion, optimistic concurrency, explicit conflict records, and staged AI merge proposals.
- **S2.1 (contract):** AIOS-FS query/view language. Refined 2026-05-08 with deltas D1–D8: formal EBNF grammar with closed operator vocabulary; proto IDL and `AIOSFSQuery` gRPC service; materialization model (virtual vs materialized; ON_DEMAND/ON_WRITE/SCHEDULED/MANUAL refresh strategies); privacy-class filter that silently excludes objects above caller's ceiling; time-travel queries (`AS OF version_id` or `AS OF timestamp`); pagination (offset and cursor) with query budget and timeout; explicit NL→query bridge to S1.1 with evidence linkage; nine golden fixtures and bounded-cardinality telemetry. Scope unchanged: semantic views, constrained DSL, projections, and natural-language-to-query boundaries.
- **S2.2 (contract):** AIOS-FS implementation space. Refined 2026-05-08 with deltas D9–D12: backing-storage choice (RocksDB primary for chunks/objects/versions/pointers/transactions, SQLite WAL mode for metadata catalog, Tantivy for lexical index, embedding store deferred to L5); RocksDB column family layout with CAS write batches; crash consistency (WAL fsync per commit, replay on startup, periodic checkpoints); snapshot/backup tiers (ZFS/Btrfs preferred, LVM fallback, logical export tarball format); encryption at rest delegated to LUKS/dm-crypt/ZFS native (per-object encryption deferred to L4); performance targets per operation; resource budgets with backpressure; POSIX↔AIOS-FS migration tooling; seven golden fixtures and telemetry. Decision unchanged: userspace authoritative object store with FUSE/portal projections first; kernel work deferred.
- **S2.3 (contract):** Policy Kernel. Refined 2026-05-08 with deltas D1–D12: proto IDL with `aios.policy.v1alpha1` package and `PolicyKernel` gRPC service (`EvaluatePolicy`/`SimulatePolicy`/`LoadBundle`/`RollbackBundle`/`ExplainDecision`/`GetPolicyEngineInfo`); subject normalization stage with L4 identity hydration (canonical*subject_id, groups, capabilities, session_class, recovery_mode, is_ai); resource enrichment using AIOS-FS SNAPSHOT consistency; closed conditions vocabulary with EBNF grammar (six namespaces: subject/request/target/object/time/system; `and` only); closed constraints vocabulary (sandbox_profile_id, max_runtime_seconds, verification_required, dry_run_only, require_evidence_grade, require_human_co_signer, network_policy, max_concurrent_per_subject, min_subject_session_class, vault_capability_required, ttl_seconds); signed policy bundle distribution (AIOS root → publisher → bundle trust chain) with content-addressed `polb*<hex>`versions, hot reload, operator-only rollback, degraded mode on signature failure; decision determinism guarantee`(request_hash, bundle_version, enrichment_snapshot_id) → identical PolicyDecision`; per-`(request_hash, bundle_version)` decision cache with bounded TTL; performance budgets (p95 < 5 ms cached, < 25 ms with enrichment, < 50 ms simulation); emergency override boundary (scoped, time-bounded ≤ 24 h, human-only, evidence-linked, cannot bypass hard denies or AI-self-approval); **AI subject self-approval prevention as a hard-coded invariant** (AI subject + any true risk flag ⇒ REQUIRE_APPROVAL with human-only approver); adversarial robustness (cycle detection at bundle load, per-evaluation rule budget 1000, rate limits, fail-closed on internal errors); ten golden decision fixtures and bounded-cardinality telemetry. Scope unchanged: default deny, hard denies, request-hash-bound approvals, policy schema, simulation, and decision evidence.
- **S2.4 (contract):** Verification grammar. Refined 2026-05-08 with deltas D1–D12: proto IDL with `aios.verification.v1alpha1` package and `VerificationEngine` gRPC service (`RunVerification`/`ExplainResult`/`GetEngineInfo`); closed primitive vocabulary as `oneof primitive` with twelve initial primitives (`service_active`, `service_inactive`, `package_installed`, `port_open`, `port_closed`, `http_ok`, `file_exists`, `file_hash`, `repo_exists`, `aiosfs_pointer`, `policy_decision`, `evidence_exists`); composition grammar (`AllOf`/`AnyOf`/`NotOf`/`Eventually`) with bounded recursion depth 8 and cycle detection; five-state `VerificationStatus` enum (`PASSED`/`FAILED`/`TIMEOUT`/`PROBE_ERROR`/`SKIPPED`) distinguishing probe failure from predicate failure; closed `PropertyType` enum with nine constitutional invariants (`EVIDENCE_LOG_APPEND_ONLY`, `EVIDENCE_HASH_CHAIN_INTACT`, `AIOSFS_POINTER_HISTORY_ACYCLIC`, `POLICY_DEFAULT_DENY_HOLDS`, `POLICY_AI_SELF_APPROVAL_BLOCKED`, `RECOVERY_PATH_INDEPENDENT_OF_L5`, `VAULT_NO_RAW_SECRET_LEAK`, `SANDBOX_PROFILE_MOST_RESTRICTIVE`, `EVIDENCE_RETENTION_RESPECTED`); read-only probe discipline (no side effects, no L4 capability invocation, no AIOS-FS writes, no external network without explicit `network_policy`); per-primitive timeouts (default 5 s, max 30 s); receipt linkage to S3.1 via evidence receipt id; eight golden fixtures and bounded-cardinality telemetry. Scope unchanged: typed verification primitives, composition, result shape, timeout/skipped semantics, and property checks.
- **S3.1 (contract):** Evidence log architecture. Refined 2026-05-08 with deltas D1–D12: proto IDL with `aios.evidence.v1alpha1` package and `EvidenceLog` gRPC service (`Append`/`ReadReceipt`/`Subscribe`/`Query`/`VerifyChain`/`RebuildIndex`/`GetLogInfo`); 22 record types with discriminated `RecordPayload` oneof spanning lifecycle, governance, L9 ops, and adapter events; hash chain algorithm `previous_receipt_hash = hex_lower(BLAKE3(jcs_canonical(prev_receipt)))[:32]` with 64-zero-char genesis sentinel; per-segment Ed25519 signature over (header || record_hashes || sealed_at) with `VerifyChain` emitting `TAMPER_DETECTED` evidence on mismatch; segment lifecycle (WAL → active → sealed at 64 MB or 1 h → warm ≤ 90 d → cold > 90 d) with auto-rebuild of cold indexes; `RetentionClass` enum (`STANDARD_24M`/`EXTENDED_60M`/`FOREVER`) with forever-retention for `POLICY_DECISION` denials, `EXECUTION` failures, `EMERGENCY_OVERRIDE_GRANT`, `TAMPER_DETECTED`, `RECOVERY_EVENT`, `CHAIN_CHECKPOINT`, `SEGMENT_SEALED`, `POLICY_BUNDLE_LOAD`, `CAPABILITY_CATALOG_LOAD`; Subscribe with bookmark replay and 5 s debouncing; Query with privacy ceiling silently excluding records above caller's class and redacted projections for partial visibility; adversarial robustness (per-subject append rate limits, ±5 s timestamp clock-skew window, replay protection via receipt-id uniqueness, segment integrity check on read with auto-quarantine on mismatch); nine golden fixtures and bounded-cardinality telemetry. Scope unchanged: append-only WAL, sealed segments, hash chain, indexes, compaction boundaries, and redaction.
- **S13.1 (contract):** L5 Cognitive Core Model. Initial contract written 2026-05-11 (1272 lines) — closes the AI-side hole. Until S13.1 the runtime / network / distribution layers were guards around an empty center: S1.1 translator and S1.2 latency tiering existed but the foundational cognitive contract — what an AI agent IS in AIOS terms, how it perceives, plans, remembers, coordinates, and how its proposing pipeline mechanically respects INV-002 — was unspecified. Proto IDL `aios.cognitive.v1alpha1`. **9 closed enums** (each + UNSPECIFIED): `CognitiveTaskKind` (10 — INTENT_PERCEPTION / PLANNING / ACTION_PROPOSAL_DRAFTING / MEMORY_RECALL / MEMORY_WRITE / INTER_AGENT_COORDINATION / VERIFICATION_REASONING / EXTERNAL_MODEL_CALL / TRANSLATION / LATENCY_ROUTING), `AgentKind` (6 — ASSISTANT / WORKER / DAEMON / COORDINATOR / TRANSLATOR / ROUTER), `AgentLifecycleState` (9 with BLOCKED_AWAITING_APPROVAL / BLOCKED_AWAITING_DEPENDENCY / BLOCKED_AWAITING_VAULT / DEGRADED / RETIRING / RETIRED), `MemoryClass` (5 — EPHEMERAL / EPISODIC / SEMANTIC / WORKING / SHARED), `MemoryPrivacyClass` (5 — PUBLIC_TO_GROUP / PRIVATE_TO_USER / PRIVATE_TO_AGENT / SYSTEM_INTERNAL / TRANSIENT), `PlanState` (7-state FSM), `InterAgentMessageKind` (8 — WORK_REQUEST / WORK_RESPONSE / COORDINATION_QUERY / COORDINATION_REPLY / HANDOFF / ESCALATION / BROADCAST / SHUTDOWN_REQUEST), `CognitiveErrorCode` (16), plus `ApprovalGranularity` helper. **Mechanical INV-002 enforcement** via the proposing pipeline: intent observed → INTENT_PERCEPTION → PLANNING → ACTION_PROPOSAL_DRAFTING → S0.1 ActionEnvelope emitted → handed to L3 Capability Runtime → agent transitions BLOCKED_AWAITING_APPROVAL — at no point in the pipeline does the agent execute the action; the agent's role ends at "envelope emitted" and resumes at "result available". This is FSM structure, not policy. **Memory write is itself a typed action** (`agent.memory.write`) gated by L3 — even agent self-memory cannot bypass policy; prevents covert communication via memory. Inter-agent coordination is **always typed-action-mediated** (`agent.coordinate.send` with closed `InterAgentMessageKind`); direct memory access between agents is forbidden (no shared FS write); cross-group coordination forbidden by default (binds INV-011). **External model calls flow through S8.1 §J canonical pattern** (vault-brokered only; `AICrossOriginPosture = AI_VAULT_BROKERED_ONLY`); agent NEVER sees the API key. **Self-grading prohibition** (binds INV-016): `agent.grade.attempt` is REJECTED CLOSED if `subject.is_ai = true AND subject_canonical_id == graded_capability.author_subject_id`. Plan FSM supports per-action approval (default; safest) or bundled approval (STRONG strength + no hard-denied actions in plan; refused otherwise). **Recovery-mode behavior**: cite S9.1 — agents in BLOCKED_AWAITING_APPROVAL at recovery-entry transition to RETIRING with FOREVER `AGENT_INTERRUPTED_BY_RECOVERY` evidence; cannot resume after recovery. Memory privacy enforcement at vault + AIOS-FS: PRIVATE_TO_USER cannot be read across users even within same group; agent attempts emit FOREVER `AGENT_MEMORY_CROSS_USER_DENIED`. Cognitive backends are adapters (cite S10.1 AdapterManifest): LangGraph, raw Anthropic API (vault-brokered), local Ollama — operator chooses; per-agent adapter selection. Adversarial coverage: agent direct FS write blocked (FOREVER), cross-agent memory read denied at S4.1 namespace, self-grading blocked (INV-016), SECRET_GET hard-denied at request entry (S5.2), AI_DIRECT_INTERNET_DENIED FOREVER (S8.1), prompt injection detection in INTENT_PERCEPTION layer, cross-group coordination blocked (INV-011 hard-deny). 18 record types queued for next-Wave consolidation (FOREVER for INV-enforcement events: AGENT_INTERRUPTED_BY_RECOVERY, AGENT_MEMORY_CROSS_USER_DENIED, AGENT_SELF_GRADING_BLOCKED, AGENT_DIRECT_FS_WRITE_BLOCKED, AGENT_CROSS_GROUP_COORDINATION_BLOCKED, AGENT_PROMPT_INJECTION_DETECTED). 9 typed actions referenced (existing S0.1 envelope + `external_model_call` from S8.1; new for S1.1 catalog: `agent.memory.read`, `agent.memory.write`, `agent.coordinate.send`, `agent.grade.attempt`, `agent.plan.submit`, `agent.plan.abandon`, `agent.register`, `agent.retire`). 1 S2.4 property queued: `AI_PROPOSAL_PIPELINE_INTACT`. Three concrete worked examples (family ASSISTANT proposes calendar event, multi-agent COORDINATOR decomposes "build the website" into WORKER agents with WORK_REQUEST/WORK_RESPONSE messages, adversarial cross-user memory access denial). Cites INV-002/010/011/013/016/017/018. After this contract: L5 has 3 of its planned sub-specs CONTRACT (S13.1 cognitive core model + S1.1 translator + S1.2 latency); model_router stays SHELL for future depth. See [DEC-038](02_design_decisions.md).
- **Wave 7 cross-spec consolidation (applied 2026-05-11):** Single addendum sweep applying queued touch-ups from S11.1 + S9.3 + S12.1 into two existing contracts. Each touched contract gained an addendum section (§26 in S3.1, §20 in S3.2). Per file: **S3.1 Evidence Log §26** (+104 lines) — 46 new RecordType narrative entries: 19 from S11.1 distribution (4 STANDARD_24M + 4 EXTENDED_60M + 11 FOREVER for trust-violation surface), 13 from S9.3 dedicated kernel pipeline (5 STANDARD_24M + 2 EXTENDED_60M + 5 FOREVER for promotion/rollback/drift), 14 from S12.1 app runtime model (6 STANDARD_24M + 4 EXTENDED_60M + 4 FOREVER for trust violations). Truthful retention distribution across 46: **20 STANDARD_24M + 8 EXTENDED_60M + 18 FOREVER** (the §26.4 reconciliation note records the truthful arithmetic which differs slightly from the orchestrator's expected 18/7/21 — orchestrator honored agent's truthful count). Cumulative narrative RecordType total: 159 (Wave 6) + 46 (Wave 7) = **205 entries**. Honors the §23/§24/§25 narrative-only declaration pattern: Appendix A IDL block NOT updated; full IDL reconciliation deferred. **S3.2 Sandbox Composition §20** (+58 lines) — new typed field on `SandboxProfile`: `ecosystem_runtime: aios.appcompat.v1alpha1.EcosystemRuntime` (closed enum from S12.1, 12 entries). Composition merges per the existing most-restrictive-wins discipline; mismatch across the 5 sources → fail closed with `EcosystemRuntimeMismatch`, NEVER silently pick one. Apply-time invariant: when action target carries `app_id`, the resolved package's manifest `ecosystem_runtime` MUST match the composed SandboxProfile's `ecosystem_runtime`; drift fails the action with `APP_ECOSYSTEM_RUNTIME_BREAKOUT_ATTEMPTED` FOREVER (already in S3.1 §26.3). Recovery-mode floor restricts to `RUNTIME_LINUX_NATIVE` only (binds S9.1 RecoveryDeniedClass = THIRD_PARTY_BINARY_RUN). Three queued items NOT applied in Wave 7 (held for separate consolidations): 6 new typed actions queued for S10.1 catalog (S9.3's `kernel.build`, `kernel.refresh`; S12.1's `app.observe_in_sandbox`, `app.translate_manifest`, `app.propose_manifest_delta`, `app.contribute_recipe`); 3 candidate L0 invariants (`NETWORK_DEFAULT_DENY_OUTBOUND`, `PACKAGE_TRUST_CHAIN_BOUND`, `ECOSYSTEM_HONESTY_DISCLOSURE`) held for audit-phase L0 sweep per the project owner's "deliberate single-purpose constitutional act" pattern. Total addition across 2 files: 162 lines. See [DEC-037](02_design_decisions.md).
- **S12.1 (contract):** L6 App Runtime Model + Cross-Ecosystem Compatibility. Initial contract written 2026-05-11 (1104 lines) — captures the smart mechanism deliberated explicitly with the project owner for running Android, Apple, Windows, and Linux apps in AIOS. Proto IDL `aios.appcompat.v1alpha1`. **Closed `EcosystemRuntime` enum (12 entries):** RUNTIME_LINUX_NATIVE / RUNTIME_FLATPAK / RUNTIME_APPIMAGE / RUNTIME_SNAP / RUNTIME_DISTROBOX / RUNTIME_WINDOWS_PROTON / RUNTIME_WINDOWS_VM / RUNTIME_ANDROID_WAYDROID / RUNTIME_ANDROID_VM_WITH_GMS / RUNTIME_MACOS_DARLING / RUNTIME_MACOS_VM / RUNTIME_REMOTE_APPLE_BRIDGE — each runtime is itself an AIOS package (`PackageKind = ADAPTER` per S11.1) with its own SandboxProfile and capability declarations. **Honesty principle (constitutional in spirit; queued as candidate L0 invariant `ECOSYSTEM_HONESTY_DISCLOSURE`):** AIOS does not promise capabilities it does not have. iOS apps cannot run on non-Apple hardware because Apple actively prevents it (secure enclave + entitlements + hardware attestation); AIOS does not pretend otherwise — RUNTIME_REMOTE_APPLE_BRIDGE bridges to user's actual Apple device instead. Closed `EcosystemHonestyClass` (4: FULLY_SUPPORTED / PARTIALLY_SUPPORTED / REQUIRES_VM / NOT_RUNNABLE_ON_NON_NATIVE) — required disclosure on every install. Closed `ManifestTranslationStrategy` (8: ANDROID_MANIFEST_XML / FLATPAK_MANIFEST_JSON / SNAPCRAFT_YAML / PROTON_RECIPE / WINE_PREFIX_PROBE / APPIMAGE_BEHAVIORAL / MAC_INFO_PLIST / IOS_REMOTE_BRIDGE). Closed `SyscallClass` (11), `RecipeTrustClass` (4), `ManifestDeltaOutcome` (4). **4-phase AI-assisted setup mechanism (the killer feature):** Phase A pre-flight observation in maximally-restricted sandbox (loopback only, ephemeral FS, no audio, no GPU accel, narrow seccomp, syscall trace) — typed action `app.observe_in_sandbox` under `_system:service:app-observer` subject, NEVER as operator; Phase B AI proposes signed manifest (SandboxProfile + NetworkOutboundManifest + capability list + EcosystemHonestyClass disclosure + EcosystemRuntime selection) — typed action `app.translate_manifest`; HUMAN_USER approval required (binds INV-002); Phase C 60s first-run capability lie audit (cite S11.1 §G); Phase D continuous refinement via `app.propose_manifest_delta` typed action. **Community Recipe Registry** (cite S11.1 AIOS_COMMUNITY_REPO): operators may `contribute_recipe` (typed action) signed; reputation evidence accumulates ("215 operators have used this manifest, 0 capability lies, 3 reported breakages with deltas"); imports from ProtonDB / WineHQ AppDB / Flathub / AUR / Snapcraft as metadata-only with attribution; **local Phase A pre-flight observation always wins** over imported reputation. Adversarial robustness covers: app modifying behavior post-audit (continuous monitoring extends past first 60s); Wine prefix breakout (S3.2 sandbox floor cannot be loosened); Waydroid container breakout (per-group AIOS namespace + S8.2 per-group VkDevice; binds INV-011); malicious recipe (caught at local first-run audit; reputation downgrades on capability lie); iOS-claim attack (any recipe claiming direct iOS execution rejected at registry ingest); anti-cheat fingerprint forging (AIOS reports honestly per-game, doesn't falsify Windows-native fingerprint). **AI subjects can REQUEST app installs but never directly install** — install action requires HUMAN_USER subject in approval (INV-002 mechanical at app-runtime layer; FOREVER `APP_AI_DIRECT_INSTALL_ATTEMPTED_BLOCKED` evidence on attempts). Three concrete worked examples (Steam game with community recipe; Android note-taking app with no recipe — full Phase A/B/C cycle; iOS app honestly declined with bridge to user's Apple device). 14 record types queued for S3.1 Wave 7 (FOREVER for the four trust-violation events: APP_HONESTY_CLASS_VIOLATION, APP_ECOSYSTEM_RUNTIME_BREAKOUT_ATTEMPTED, APP_AI_DIRECT_INSTALL_ATTEMPTED_BLOCKED, APP_RECIPE_DECEPTIVE_REJECTED_AT_INGEST). 4 new typed actions queued for S10.1 Wave 7 (`app.observe_in_sandbox`, `app.translate_manifest`, `app.propose_manifest_delta`, `app.contribute_recipe`). 1 S3.2 SandboxProfile field queued for Wave 7: `ecosystem_runtime: EcosystemRuntime`. 1 candidate L0 invariant queued: `ECOSYSTEM_HONESTY_DISCLOSURE`. See [DEC-036](02_design_decisions.md).
- **S9.3 (contract):** L1 Dedicated Kernel Pipeline (smart mechanism). Initial contract written 2026-05-11 (1161 lines) — captures the smart mechanism deliberated explicitly with the project owner. The mechanism has eight tightly-coupled structural elements; if any is missing the mechanism is stupid regardless of context. Proto IDL `aios.kernel.v1alpha1.KernelBuilder`. **The 8 elements:** §4 Build is a typed deterministic function (`KernelImage = Build(hardware_graph, security_target, aios_invariant_bundle_id, upstream_kernel_version, module_set, firmware_blob_set, default_seccomp, lsm_config)` — identical inputs → bit-identical KernelImage; pipeline-definition signed by AIOS root, recovery-mode-only replacement); §5 **Convergence as fixed-point** (closed `ConvergenceState` enum with ITERATING / CONVERGED / DIVERGED / ABANDONED; convergence = hash of input+output across iterations stops changing; **monotonicity rule** — each iteration's gate score must be ≥ previous, regression blocks further iteration); §6 **Six machine-checkable acceptance gates, all-of** (closed `GateName` 6 + `GateResult` 4: STABILITY = N=10 successive boots + 24h KASAN clean + zero panics; SECURITY = kspp ≥ 95% + lockdown=confidentiality + signed-only modules + kexec revoked + BPF/ftrace restricted; HARDWARE_FIT = zero unbound devices vs L8 HDM + every firmware blob present and signed; PERFORMANCE = ≤ 10% regression vs generic baseline; REPRODUCIBILITY = bit-identical bzImage over 3 rebuilds; RECOVERY_REHEARSAL = 5/5 successful A/B fallbacks); §7 **3-AND filter for kernel additions** (closed `AdditionFilterResult` enum) — feature accepted iff (1) mechanically enforces an existing AIOS L0 invariant + (2) exists in upstream Linux mainline + (3) has a measurable acceptance gate; canonical accepted table includes dm-verity (INV-004), IMA+EVM (INV-014), Lockdown=confidentiality (INV-013), signed-only modules + revoke kexec (INV-013), TPM measured boot + sealed vault root (INV-018), pinned eBPF correlator (INV-002+INV-011), kernel-side evidence netlink emission (INV-005); rejected table includes custom scheduler, custom LSM, custom syscall, out-of-tree modules; closed `SecurityTarget` enum (3: KSPP_BASELINE / KSPP_STRICT / KSPP_PARANOID); §8 Build runs as typed AIOS action through L3 Capability Runtime (S10.1) — ISOLATED_SANDBOX dispatch, TYPED_PARAMETERS_ONLY io_mode, signed by `_system:service:kernel-builder` subject; this **closes the recursion**: AIOS builds AIOS using AIOS — same governance plane for runtime and build; §9 **A/B promotion FSM** (closed `KernelImageState` enum with 9 states: BUILDING / BUILT / GATING / GATE_PASSED / GATE_FAILED / A_PROMOTED / B_DEMOTED_TO_A / ROLLBACK / RETIRED) — promotion to A requires recovery-mode (cite S9.1 RecoveryMutableScope = DEDICATED_KERNEL_PROMOTION); rollback is automatic, not human-driven (A failed N=2 boots → B autopromoted, A demoted, FOREVER evidence); §10 **Drift detection: kernel image hash is evidence subject** — every boot records the running kernel's BLAKE3 image hash; expected hash loaded from L4.2 vault; mismatch → FOREVER `KERNEL_IMAGE_DRIFT_DETECTED` + immediate drop into recovery; the kernel itself participates in the evidence chain it hosts (recursive but constructive); §11 **Maintenance is structural property** — typed `kernel.refresh` scheduled action via L3 SGR (default cadence: every upstream stable release tag); pulls upstream stable + re-runs full pipeline; promotes A only if all 6 gates pass; pipeline failure → system continues on previous A slot; eliminates the "do I have time to maintain a custom kernel" question (closed `KernelMaintenanceResult` enum 4 values). 13 record types queued for S3.1 Wave 7 (5 FOREVER: KERNEL_PROMOTED_TO_A, KERNEL_PROMOTED_TO_B, KERNEL_ROLLBACK_PERFORMED, KERNEL_IMAGE_DRIFT_DETECTED, PIPELINE_DEFINITION_REPLACED). Adversarial robustness covers pipeline tampering (signed pipeline-definition rejects), build determinism break (3-rebuild reproducibility gate), gate-result forgery (gate results signed by kernel-builder subject + recorded in evidence), hostile mirror returning tampered upstream tarball (content-hash check on upstream tarball before Build), evil-maid kernel swap (kernel-image hash drift detection at boot), insider with kernel-builder credential (still gated by recovery-mode for promotion). The deliberation was framed explicitly without time-frame thinking ("v1/v2") and without feasibility thinking ("realistic for solo founder") — the criterion was smart vs stupid; if all 8 elements are in place, dedicated kernel is not maintenance burden but passive structural property. See [DEC-035](02_design_decisions.md).
- **S11.1 (contract):** L10 Repository Model + Trust Roots. Initial contract written 2026-05-11 (1027 lines) — closes the **last SHELL layer** in the AIOS spec; after this all 11 layers are `PARTIAL+`. Proto IDL `aios.distribution.v1alpha1`. **Three-tier signing chain** (closed): AIOS root key (firmware-pinned, vault-protected) → publisher root keys (signed by AIOS root) → package signing keys (signed by publisher root); chain depth ≤ 3 hops, deeper rejected with `TRUST_CHAIN_TOO_DEEP` FOREVER. 9 closed enums: `PublisherTrustLevel` (5 — AIOS_ROOT/VERIFIED/COMMUNITY/DEPRECATED/DEPLATFORMED), `RepositoryKind` (5 — AIOS_ROOT_REPO/AIOS_VERIFIED_REPO/AIOS_COMMUNITY_REPO/AIOS_RECOVERY_REPO/EXTERNAL_BRIDGE), `UpdateChannel` (4 — STABLE/BETA/RECOVERY_CRITICAL/DEPRECATED_RETENTION), `PackageKind` (9 — APP/AGENT/THEME/INVARIANT_BUNDLE/POLICY_BUNDLE/IDENTITY_BUNDLE/KERNEL_CANDIDATE/ADAPTER/CAPABILITY_CATALOG_DELTA), `InstallScope` (4 — SYSTEM_ONLY/GROUP_SCOPED/USER_SCOPED/EITHER), `PackageInstallState` (10-state FSM), `PackageVerificationResult` (10), `MirrorSemantic` (3 — ORIGIN/CACHED/LOCAL; mirrors NEVER re-sign — content-hash check on host detects tampering), `TakedownReason` (7 — MALICIOUS_BEHAVIOR_DETECTED/SUPPLY_CHAIN_COMPROMISE/CAPABILITY_LIE_DETECTED/LEGAL_REQUIREMENT/PUBLISHER_REQUEST/KEY_COMPROMISE/ABANDONED_AFTER_INACTIVE_TTL). 17-step strictly-ordered install pipeline (Fetch → Signature verify → Trust chain verify → Publisher state check → Content hash verify → Manifest field validation → Sandbox profile validation → Capability declaration vs catalog → Network manifest validation → Policy decision → Approval → Recovery-mode gate → APPROVED → Atomic install → Capability binding issue → ACTIVE → 60s capability lie audit). **Recovery-only package classes** (INVARIANT_BUNDLE / POLICY_BUNDLE / IDENTITY_BUNDLE / KERNEL_CANDIDATE / CAPABILITY_CATALOG_DELTA) require recovery mode (binds INV-012 + L1.1 RecoveryMutableScope); outside recovery fail closed with `RecoveryRequiredForPackageKind`. **AI subjects can REQUEST package install but never directly install** — install action requires HUMAN_USER subject in approval (binds INV-002 + INV-013; package-distribution analog of "AI proposes never executes"). First-run capability lie audit: 60s observation; observed capability not in declared set → quarantine + FOREVER `CAPABILITY_LIE_DETECTED`. Mirror tampering: content-hash check on host; suspicious mirror auto-blacklisted with FOREVER `MIRROR_HASH_MISMATCH_BLACKLISTED`. Publisher rotation: old root signs new root + AIOS root co-signs + recovery-mode + FOREVER `PUBLISHER_KEY_ROTATED`. Deplatforming: AIOS root co-signs takedown + all packages QUARANTINED on next health check + 30-day grace period for existing installs + FOREVER `PUBLISHER_DEPLATFORMED`. External bridges (Flathub/OCI/distro): NEVER admit to AIOS_ROOT or AIOS_VERIFIED — COMMUNITY at best; bridge re-packages with AIOS bridge-signing key after upstream signature validation. 19 record types queued for S3.1 follow-up (8 FOREVER, 4 EXTENDED_60M, 7 STANDARD_24M). 1 candidate L0 invariant queued narrative-only: `PACKAGE_TRUST_CHAIN_BOUND`. Three worked examples (VERIFIED-publisher app happy path, AI-requested install routes to operator approval, KEY_COMPROMISE deplatform with 30-day grace). After this contract: 10 of 11 layers PARTIAL+; only some sub-specs within layers stay SHELL. See [DEC-034](02_design_decisions.md).
- **Wave 6 cross-spec consolidation (applied 2026-05-11):** Single addendum sweep applying queued touch-ups from S5.2 + S5.3 + S5.4 + S9.1 + S10.1 + S8.1 + DEC-026 (L0 INV-023/024 promotion) into three existing contracts. Each touched contract gained an addendum section (§25 in S3.1, §19 in S2.4, §28 in S2.3) cataloging additions; existing numbered sections were not edited in place — same additive discipline as Wave 5. Per file: **S3.1 Evidence Log §25** (+163 lines) — 72 new unique RecordType narrative additions: 8 from S5.2 vault, 8 from S5.3 approval (reconciles APPROVAL_BINDING_VOIDED with S10.1's BINDING_VOIDED_ACTION_REVISED into one canonical record name), 8 FOREVER from S5.4 emergency override, 10 FOREVER from S9.1 recovery (+ 3 deferred narrative names), 20 from S10.1 capability runtime, 18 from S8.1 network. Retention class distribution across 72: 25 STANDARD_24M + 14 EXTENDED_60M + 33 FOREVER. Cumulative narrative RecordType total 87 (Wave 5 baseline) + 72 = **159 entries**. **S2.4 Verification Grammar §19** (+118 lines) — 2 new closed `PropertyType` entries verifying L0 invariants INV-023 and INV-024 (`CHROME_ZONE_RESERVED`, `GPU_COMPUTE_GATED`) — closed enum 14 → **16 entries**; 3 new closed primitives from S8.1 (`network_subject_outbound_class`, `network_active_exposure_class`, `network_external_model_call_brokered_only`) — closed primitive vocabulary 21 → **24 entries**. **S2.3 Policy Kernel §28** (+88 lines) — 3 new closed condition fields from S8.1 (`subject.network_outbound_directive`, `subject.ai_external_posture`, `target.exposure_class`) — conditions vocabulary 23 → **26 fields**. No new constitutional hard-denies added in this Wave (binds to the discipline established in DEC-025/DEC-026: each L0 INV addition is a deliberate single-purpose act). The candidate L0 invariant `NETWORK_DEFAULT_DENY_OUTBOUND` queued by S8.1 stays deferred for a separate L0 sweep. Total addition across 3 files: 369 lines. Every queued touch-up from the post-Wave 5 sub-spec landings is now in the contract it applies to, not just enumerated in the originating queue. After Wave 6 the L4/L1/L3/L8 sub-spec touch-up loops are closed-loop. See [DEC-033](02_design_decisions.md).
- **S8.1 (contract):** L8 Network Policy. Initial contract written 2026-05-11 (864 lines) — the L8 enforcement plane under S7.5 web renderer's `WebExposureState` and S3.2 sandbox `NetworkMode`. Proto IDL `aios.network.v1alpha1` with `NetworkPolicyService` gRPC (`GetNetworkPosture`/`RequestExposure`/`RevokeExposure`/`GrantOutbound`/`RevokeOutbound`/`EvaluateConnection`/`ListActiveExposures`/`ListActiveOutbound`/`GetNetworkPolicyInfo`). 9 closed enums: `NetworkPosture` (5: AIRGAP/LOOPBACK_ONLY/LAN_LOCAL/LAN_AND_INTERNET/PUBLIC_ROUTABLE), `OutboundDirective` (5), `InboundExposureClass` (4: LOOPBACK/LAN/PUBLIC/RECOVERY), `ProtocolFamily` (7), `AllowlistEntryKind` (8), `PortPolicy` (5), `ExposureApprovalState` (8 FSM states), `NetworkPolicyErrorCode` (16), `AICrossOriginPosture` (3 — AI_NO_EXTERNAL / AI_VAULT_BROKERED_ONLY / AI_LOOPBACK_ONLY). **AI subjects are NEVER granted ALLOW_INTERNET / ALLOW_LIST_ONLY for arbitrary destinations** — their network access flows only through L4.2 vault-brokered capability handles (network analog of vault use-without-reveal). Public exposure constitutional discipline: HUMAN_USER + recovery_mode + STRONG approval + co-signer + FOREVER evidence + 4 h auto-termination + 5-minute heartbeat. **Forbidden direct LAN→PUBLIC transition** enforced at L8 (must downgrade to LOOPBACK first); already constitutional in S7.5. Allowlist composition: HOST_FQDN entries resolved with bounded fan-out (max 16 IPs per FQDN); LAN_SUBNET pinned at activation; only AIOS-root-signed DoT resolvers permitted. Per-app outbound discipline: every app/service/agent declares signed `network_outbound_manifest`; runtime breach degrades posture to AI_LOOPBACK_ONLY with FOREVER evidence. AI external-model-call canonical pattern: action → policy → vault broker holds API key as `KEY_ENCRYPT`/`MAC_GENERATE` → L8 brokers connection → `EXTERNAL_MODEL_CALL_BROKERED` evidence (no payload, no key). Backend stack: nftables primary, iptables degraded fallback, WireGuard for VPN tunnels, Linux network namespaces per group (binds INV-011). Adversarial coverage: allowlist Ed25519 signature, PID→subject_id correlator via netlink/eBPF, dual-stack IPv4/IPv6 evaluation, DNS rebinding pinning, ARP spoofing detection (MAC+IP pin at activation), mid-flight grant revocation < 250 ms. 18 record types queued for S3.1 (7 FOREVER, 5 EXTENDED_60M, 6 STANDARD_24M). Other queues: 3 S2.4 verification primitives (`network_subject_outbound_class`, `network_active_exposure_class`, `network_external_model_call_brokered_only`), 3 new S2.3 closed condition fields (`subject.network_outbound_directive`, `subject.ai_external_posture`, `target.exposure_class`), 1 candidate L0 invariant (`NETWORK_DEFAULT_DENY_OUTBOUND`). L8 layer headline updated: 02_network_policy + 05_gpu_resource_model both `CONTRACT`. See [DEC-032](02_design_decisions.md).
- **S10.1 (contract):** L3 Capability Runtime gRPC. Initial contract written 2026-05-11 (1197 lines) — closes the long-standing dangling reference: every layer in AIOS cites the Capability Runtime as the executor of typed actions but the L3 contract was `SHELL` until now. Proto IDL `aios.runtime.v1alpha1.CapabilityRuntime` service with 9 RPCs (`ValidateAction`/`EvaluatePolicyForAction`/`RequestApprovalForAction`/`ExecuteAction`/`VerifyAction`/`RollbackAction`/`GetActionStatus`/`ListAdapters`/`GetAdapterCapabilities` + `GetCapabilityRuntimeInfo`). 11 closed enums: `ActionLifecycleState` (14 states formalising Rev.1 §13: CREATED/POLICY_PENDING/APPROVED/APPROVAL_PENDING/POLICY_DENIED/QUEUED/EXECUTING/VERIFYING/SUCCEEDED/FAILED/ROLLED_BACK/ROLLBACK_FAILED terminal/OVERRIDE_PENDING/OVERRIDE_DENIED), `ActionDispatchKind` (4: IN_PROCESS_RPC/SUBPROCESS_FORK/ISOLATED_SANDBOX required for AI-origin/DRY_RUN), `AdapterIOMode` (2 — TYPED_PARAMETERS_ONLY default; TEMPLATE_PARAMETERS with closed substitution; **free-form shell input is NOT a supported AdapterIOMode**), `AdapterStability` (5 — REGISTERED/EXPERIMENTAL dry-run-only-by-default/STABLE/DEPRECATED/RETIRED), `QueueClass` (4 — INTERACTIVE p95 < 200 ms/AGENT_PROPOSAL fairness-bounded/BACKGROUND/RECOVERY_PRIORITY preempt-all), `ExecutionFailureReason` (12), `RollbackOutcome` (4), `RuntimeErrorCode` (20), `RollbackStrategy` (4 — NONE/IDEMPOTENT_REVERSE/CHECKPOINT_BASED/EXTERNAL_REQUIRED), `AdapterStatus` (5), `AdapterHealth` (3). 21-row exhaustive lifecycle FSM transition table (T1-T21); illegal transitions denied with `IllegalStateTransition`. Mandatory 8-step pre-dispatch sequence: re-validate canonical hash → re-evaluate policy under current bundle → re-check approval binding state → re-check vault capability → re-compose sandbox profile under current host capability snapshot → re-check QUEUED state → mark CONSUMED on binding (single-use enforcement) → transition EXECUTING. Adapter manifest: signed Ed25519 over JCS, declared action_kinds must exist in L5 capability catalog, declared invariants_supported list, registration rejection on signature failure → FOREVER evidence. **AI-origin actions cannot exceed 50% of total queue capacity** (default fairness floor); attempted INTERACTIVE queue class on AI subject silently downgrades to AGENT_PROPOSAL with telemetry record. Hard-deny rescue path: POLICY_DENIED can transition OVERRIDE_PENDING → APPROVED via S5.4 emergency override binding (action_canonical_hash anchored). Verification path: VERIFYING calls L9.2 VerificationEngine using S0.1 verification_intent; status PASSED → SUCCEEDED; FAILED/TIMEOUT/PROBE_ERROR/SKIPPED → FAILED + rollback FSM. Rollback FSM is closed; ROLLBACK_FAILED is **terminal** with FOREVER `ROLLBACK_FAILED_REQUIRES_OPERATOR` evidence + affected_resources list (no automatic retry — operator intervention required). Performance contract: ValidateAction p95 < 5 ms; ExecuteAction synchronous overhead p95 < 25 ms (excluding adapter execution); VerifyAction overhead p95 < 50 ms; queue depth 1000 before backpressure; adapter timeout 30 s default / 300 s max. Three worked examples (operator approves `aios.fs.write` happy path, AI hard-denied `policy_bundle.update` rescued by override, destructive action with verification failure → checkpoint rollback). 20 record types queued for S3.1 (4 FOREVER: ROLLBACK_FAILED_REQUIRES_OPERATOR, ADAPTER_REGISTRATION_REJECTED, BINDING_VOIDED_ACTION_REVISED). Cites INV-002/005/007/009/013/014. See [DEC-031](02_design_decisions.md).
- **S9.1 (contract):** L1 Recovery Boundary. Initial contract written 2026-05-11 (735 lines) — closes the most-cited dangling reference in the AIOS constitution. Recovery is invoked by INV-001 (recovery independent of L5), INV-004 (recovery boundary preserved), INV-012 (recovery required for system mutation), INV-022 (recovery aesthetically distinct), L4.1 hard-deny `RecoveryRequiredForSystemMutation`, S5.2 vault `RevealSecret`, S5.4 STRONG_SOLO override only-in-recovery, S7.1 recovery surface stack, S7.5 RECOVERY exposure state, L4.3 `RECOVERY` SessionClass — but until now there was no contract specifying what recovery actually IS. Proto IDL `aios.recovery.v1alpha1`. 8 closed enums: `RecoveryMode` (3: NORMAL/RECOVERY/DEGRADED), `RecoveryEntryReason` (8: OPERATOR_INITIATED, BOOT_FAILURE_AUTO N consecutive, INVARIANT_BUNDLE_SIGNATURE_FAILURE, POLICY_BUNDLE_CORRUPTION, AIOSFS_ROOT_UNRESOLVABLE, VAULT_ROOT_KEY_UNAVAILABLE, IDENTITY_BUNDLE_FAILURE, EVIDENCE_LOG_TAMPER_DETECTED), `RecoveryExitReason` (4 — REBOOT_TO_NORMAL **the only normal exit**, TTL_EXPIRED, OPERATOR_TERMINATED, UNRECOVERABLE_FAILURE), `RecoveryStage` (9-state linear FSM from STAGE_PRE_KERNEL through STAGE_RECOVERY_ACTIVE to STAGE_REBOOTING), `RecoveryMutableScope` (8: POLICY_BUNDLE, CAPABILITY_CATALOG, VAULT_ROOT_MATERIAL, INVARIANT_BUNDLE, IDENTITY_BUNDLE, RECOVERY_OPERATOR_REGISTRATION, DEDICATED_KERNEL_PROMOTION, L1_BOOT_PARAMETERS), `RecoveryReadOnlyScope` (2: AIOSFS_GROUP_DATA forensic-only, EVIDENCE_LOG_TAIL no-rewrite), `RecoveryDeniedClass` (5: AI_AGENT_EXECUTION, WEB_PUBLIC_EXPOSURE, LAN_NETWORK_OPEN, THIRD_PARTY_BINARY_RUN, EVIDENCE_LOG_REWRITE), `RecoveryNetworkPosture` (3: LOOPBACK_ONLY default / LAN_FOR_PROVISIONING time-boxed evidence-bound / AIRGAP). Constitutional 8-hour hard cap on recovery session with auto-reboot at TTL_EXPIRED (cannot be loosened by any policy bundle — bounded blast radius). **Exit-by-reboot only** — no in-place RECOVERY → NORMAL transition (eliminates "did we really clean up state?" ambiguity, forces deterministic kernel command line change, prevents drift). **No-L5-in-recovery contract**: L5 systemd units masked in recovery boot path; any start attempt fails closed `L5StartProhibitedInRecovery` + FOREVER `RECOVERY_L5_START_BLOCKED` evidence. Concrete mount discipline table: `/aios` NOT MOUNTED in recovery; group data NOT MOUNTED (read-only forensic attach optional with FOREVER evidence). Operator authentication: dedicated recovery prompt under `_recovery` KWin session, no remote authentication accepted in recovery, hardware key preferred / passphrase fallback emits `HEAVY_AUTH_FALLBACK_USED`. 10 FOREVER record types queued for S3.1 (RECOVERY_BOOT_ENTERED, RECOVERY_OPERATOR_AUTHENTICATED, RECOVERY_OPERATION_PERFORMED, RECOVERY_TTL_EXPIRED_AUTO_REBOOT, RECOVERY_BOOT_EXITED, RECOVERY_L5_START_BLOCKED, RECOVERY_NETWORK_LAN_ENABLED, RECOVERY_NETWORK_LAN_DISABLED, RECOVERY_FORENSIC_ATTACH_PERFORMED, BOOT_FAILURE_AUTO_RECOVERY_TRIGGERED). Three worked examples (normal boot, operator-initiated recovery for policy bundle update, boot-failure auto-recovery with forensic attach). L1 layer headline updated SHELL → PARTIAL. See [DEC-030](02_design_decisions.md).
- **S5.4 (contract):** L4 Emergency Override. Initial contract written 2026-05-11 (701 lines). Disjoint from approval — approval rescues `policy_pending`; override rescues `hard_deny`. Proto IDL with `aios.override.v1alpha1` package and `EmergencyOverrideService` gRPC. Closed `OverrideState` (7: REQUESTED/AWAITING_DUAL_CONFIRM/ACTIVE/CONSUMED/EXPIRED/REVOKED/DENIED), closed `OverrideStrength` with **WEAK forbidden** (3: STRONG_SOLO recovery-only, DUAL_HUMAN default, TRIPLE_HUMAN for deepest hard-denies), closed `OverrideScope` (2: ONE_ACTION single-spend / ONE_SUBJECT_TTL ≤ 5 min, no OPEN_SCOPE / ALL_ACTIONS / INDEFINITE), closed `OverrideTtlClass` (3 — TTL_OVERRIDE_INSTANT ≤ 60 s / TTL_OVERRIDE_SHORT ≤ 5 min / TTL_OVERRIDE_RECOVERY ≤ 15 min; **no infinite TTL, no tier > 15 minutes**), closed `OverrideDenialReason` (8), closed `OverrideRateClass` (3 — LIMITED_LOW ≤ 3/month default, LIMITED_MED ≤ 10/month, RECOVERY_OPERATOR ≤ 30/month). Closed `NonOverridableClass` (6 — **AIOS_RECOVERY_BREAK / EVIDENCE_LOG_REWRITE / VAULT_RAW_REVEAL_BYPASS / IDENTITY_KEY_FORGE / POLICY_DENY_LIST_DELETE / AIOS_CHROME_REPLACEMENT**) — these constitutional hard-denies cannot be rescued by ANY override; quorum/TTL/recovery are all irrelevant; the only path to change them is rebuilding the system with a new policy bundle and a new evidence chain root. `OverrideBinding` record with target_action_request_id + canonical hash anchored in evidence chain root at request and grant time, requesting and confirming subject ids, justification text (≥ 32 chars, sanitized), per-confirmer Ed25519 signatures over canonical JCS. **Channel separation invariant**: each confirming subject's grant must arrive on a distinct ApprovalChannel (defends against single-machine compromise — two grants from the same channel/session denied as CO_SUBJECT_IDENTICAL). STRONG_SOLO permitted **only** during recovery boot (cite L1 boundary); outside recovery the minimum is DUAL_HUMAN. Constitutional cooldown after consumed override (≥ 5 minutes per granting subject) plus per-subject monthly cap. Reversibility: a CONSUMED override cannot be unwound (physical-world effects); the override RECORD is immutable FOREVER; later overrides change behavior going forward; post-hoc forensic findings recorded as separate `OVERRIDE_REVIEW` records (never as modifications). Eight FOREVER record types queued for S3.1 follow-up: `OVERRIDE_REQUESTED`, `OVERRIDE_QUORUM_RECEIVED`, `OVERRIDE_GRANTED`, `OVERRIDE_CONSUMED`, `OVERRIDE_DENIED`, `OVERRIDE_EXPIRED`, `OVERRIDE_REVOKED`, `OVERRIDE_REVIEW`. L7.2 NodeKind addition queued: `OVERRIDE_PROMPT` (visually distinct per L7.3 ΔE ≥ 25 HARMFUL state distinctness rule); until the L7.2 amendment lands, fall back to APPROVAL_PROMPT NodeKind with `is_override = TRUE` ARIA-grade flag. Three concrete worked examples: recovery operator unbricks policy DB after bad config push (STRONG_SOLO during recovery, ONE_ACTION, TTL_OVERRIDE_RECOVERY); two human admins jointly grant a one-time DUAL_HUMAN override deleting a hard-denied old encrypted backup; attempted EVIDENCE_LOG_REWRITE override → denied as TARGET_NOT_OVERRIDABLE with FOREVER `OVERRIDE_DENIED` and triggered `OVERRIDE_REVIEW`. See [DEC-029](02_design_decisions.md).
- **S5.3 (contract):** L4 Approval Mechanics. Initial contract written 2026-05-11 (745 lines). Proto IDL with `aios.approval.v1alpha1` package and `ApprovalService` gRPC. Closed `ApprovalRequestState` (8: DRAFT / AWAITING_OPERATOR / GRANTED / DENIED / EXPIRED / REVOKED / CONSUMED / FAILED_DELIVERY), closed `ApprovalChannel` (7: KDE_NATIVE_PROMPT / WEB_LOOPBACK_PROMPT / WEB_LAN_PROMPT / CLI_TTY_PROMPT / MOBILE_PUSH deferred / VOICE_CHALLENGE deferred / RECOVERY_CONSOLE), closed `ApprovalStrength` (4 — WEAK / STRONG / DUAL / BIOMETRIC), closed `ApprovalDenialReason` (7), closed `ApprovalBindingScope` (3 — EXACT_ACTION / ACTION_FAMILY / SESSION_SCOPED reserved), closed `ApprovalTtlClass` (5 — TTL_INSTANT ≤ 60 s / TTL_SHORT ≤ 5 min / TTL_MEDIUM ≤ 30 min / TTL_LONG ≤ 4 h / TTL_RECOVERY ≤ 30 min recovery-only; **TTL_INFINITE explicitly forbidden**). The `ApprovalBinding` record anchors the binding to one exact ActionRequest and to the action's canonical hash (`hex_lower(BLAKE3(JCS(action)))[:32]`) — if the action's canonical form changes by even one byte between grant and execute, the binding voids with `ACTION_REVISED` (Capability Runtime re-validates at ExecuteAction). Deterministic channel selection order (recovery→RECOVERY_CONSOLE; local console→KDE_NATIVE_PROMPT; loopback session→WEB_LOOPBACK_PROMPT; TTY→CLI_TTY_PROMPT; bound mobile→MOBILE_PUSH else FAILED_DELIVERY). Trust-bearing surface contract: APPROVAL_PROMPT renders only in CHROME composition zone authored by the AIOS chrome system identity (binds INV-023 CHROME_ZONE_RESERVED + INV-020 + INV-021); AI agents constitutionally cannot author the approval prompt. Hard-denied actions cannot be rescued by approval — they require Emergency Override (S5.4). Dual-control: DUAL strength requires two distinct subject ids both `HUMAN_USER`-kind with both Ed25519 signatures present. Anti-replay: EXACT_ACTION bindings are single-use (CONSUMED is terminal); re-presenting fails closed. Nine record types queued for S3.1 with LONG retention floor: `APPROVAL_REQUESTED`, `APPROVAL_DELIVERED`, `APPROVAL_GRANTED`, `APPROVAL_DENIED`, `APPROVAL_EXPIRED`, `APPROVAL_CONSUMED`, `APPROVAL_REVOKED`, `APPROVAL_DELIVERY_FAILED`, `APPROVAL_BINDING_VOIDED` (action-revision case). Three worked examples (operator approves `aios.fs.write` from KDE happy path, action revision case, dual-control delete on production data). See [DEC-028](02_design_decisions.md).
- **S5.2 (contract):** L4 Vault Broker. Initial contract written 2026-05-11 (1000 lines), promoting from `SHELL`. Implements INV-003 (secrets are capabilities) and INV-018 (vault never leaks raw secrets). Proto IDL with `aios.vault.v1alpha1` package and `VaultBroker` gRPC service (`SignBlob` / `VerifyBlob` / `EncryptBlob` / `DecryptBlob` / `GenerateMac` / `VerifyMac` / `GenerateRandom` / `RevealSecret`); use-without-reveal contract — AI subjects perform cryptographic operations via capability handle, never receiving raw key material. Closed `VaultCapabilityClass` enum (8 + UNSPECIFIED): KEY_SIGN, KEY_VERIFY, KEY_ENCRYPT, KEY_DECRYPT, MAC_GENERATE, MAC_VERIFY, RANDOM_GENERATE, SECRET_GET. Closed `VaultMaterialKind` (11 + UNSPECIFIED): Ed25519/RSA/X25519 keys, AES-256-GCM, ChaCha20-Poly1305, HMAC-SHA256, BLAKE3 keyed, password/token/cert blobs. Closed `CapabilityState` (6 — DRAFT / ACTIVE / EXPIRED / REVOKED / ROTATED distinct from REVOKED for material rollover with binding kept usable / DISCARDED), closed `RotationReason` (4), closed `RevocationReason` (7), closed `VaultErrorCode` (21). **AI subjects hard-denied from `SECRET_GET` at request entry regardless of capability state** — capability is rejected, not revoked, since the same capability could be valid for a HUMAN_USER. `RevealSecret` (the human-only escape hatch) requires HUMAN_USER + STRONG session class + recovery_mode + co-signer approval (per S5.3) + one-shot capability + 100 ms minimum-latency floor against timing brute-force on co-signer approval ids; emits `VAULT_RAW_REVEAL` FOREVER. KEY_VERIFY requires a separate public-key capability even when the private counterpart is held (defense-in-depth boundary). Eight new record types queued for S3.1 follow-up: `VAULT_CAPABILITY_ISSUED` STANDARD_24M, `VAULT_CAPABILITY_ROTATED` STANDARD_24M, `VAULT_CAPABILITY_REVOKED` EXTENDED_60M, `VAULT_OPERATION` STANDARD_24M, `VAULT_RAW_REVEAL` FOREVER, `VAULT_CAPABILITY_FORGERY` FOREVER, `SUBJECT_KIND_REJECTED_FOR_VAULT` FOREVER, `VAULT_RECOVERY_SNAPSHOT_LOADED` FOREVER. See [DEC-027](02_design_decisions.md).
- **L0 invariants promotion (applied 2026-05-11):** The two constitutional hard-deny candidates queued by Wave 5 §27.2 — `CompositionZoneForbidden` and `GpuComputeOutsideAuthorisedClass` — have been promoted into the L0 INV catalog as formal invariants `INV-023 CHROME_ZONE_RESERVED` and `INV-024 GPU_COMPUTE_GATED`. Each entry follows the canonical Statement / Why / Enforced by / Verified by / Cannot be loosened by structure. Closed `InvariantId` enum grows from 22 to 24 entries; telemetry label cardinality on `governance_invariant_violation_total` updated to 24; acceptance criteria updated; cross-spec dependency table extended with a new L8 row (L8.2 enforces INV-024). Two new S2.4 properties queued for the next S2.4 consolidation: `CHROME_ZONE_RESERVED` and `GPU_COMPUTE_GATED` — adding to the 14 already in the closed `PropertyType` enum after Wave 5 will bring it to 16. Reciprocal narrative in S2.3 §27.2 amended from "promotion queued" to "promoted as INV-023/INV-024". See [DEC-026](02_design_decisions.md).
- **Wave 5 cross-spec consolidation (applied 2026-05-10):** Single addendum sweep applying queued touch-ups from S7.1 + S7.2 + S7.3 + S7.4 + S7.5 + S8.2 + L0 INV-019..022 into five existing contracts. Each touched contract gained an addendum section near its end (before "See also") cataloging additions; existing numbered sections were not edited in place — preserves prior commit content and keeps diffs reviewable. Per file: **S3.1 Evidence Log §24** (+126 lines) — 58 new RecordType narrative additions distributed by source spec (7 from S7.1, 3 from S7.2, 4 from S7.3, 11 from S7.4, 17 from S7.5, 16 from S8.2); retention class distribution 37 STANDARD_24M + 7 EXTENDED_60M + 14 FOREVER; declared narrative total 87 (the IDL appendix block stays at the prior state per the §23 pattern; full IDL reconciliation is a separate sweep). **S2.4 Verification Grammar §18** (+128 lines) — 4 new `PropertyType` enum entries enforcing INV-019..022 (`RENDERER_VISUAL_IDENTITY_PRESERVED`, `TRUST_INDICATORS_ALWAYS_VISIBLE`, `AI_HUMAN_VISUAL_DISTINCTION`, `RECOVERY_AESTHETIC_DISTINCT` — closed enum now 14 entries) plus 8 new primitives (`surface_in_zone`, `tree_contains_kind`, `tree_max_depth`, `theme_satisfies_invariants`, `theme_constitutional_icons_intact`, `gpu_binding_class`, `web_renderer_bound_to`, `web_chrome_z_index_at_least` — primitive vocabulary now 21 entries). **S2.3 Policy Kernel §27** (+77 lines) — 6 new closed condition fields (`target.surface_kind`, `target.composition_zone`, `target.gpu_capability_class`, `target.gpu_device_kind`, `target.theme_kind`, `target.theme_id`) plus 2 new constitutional hard-deny candidates (`CompositionZoneForbidden` binding INV-020 — AI subjects and APP/STREAM surfaces cannot reach CHROME zone; `GpuComputeOutsideAuthorisedClass` — GPU_COMPUTE_HEAVY requires explicit `gpu.compute_heavy` capability); promotion of these candidates to constitutional invariants in L0 catalog is queued for next revision (current INV catalog stays at 22). **S2.1 Query/View Language §19** (+50 lines) — 5 new closed query fields (`target.surface_kind`, `target.composition_zone`, `target.theme_kind`, `target.theme_id`, `target.gpu_capability_class`); illustrative example queries provided. **S3.2 Sandbox Composition §19** (+83 lines) — new `SandboxProfile.gpu_policy: GpuPolicy` typed field with composition merge rules (most-restrictive wins; runtime safety floor cannot be loosened) plus apply-time invariant `SurfaceGroupOwnershipMismatch` check (when action target has surface_id, surface group_owner must match subject primary_group_id) — defense-in-depth over S7.1 / S2.3. Total addition across 5 files: 464 lines. The namespace contract from S4.1 plus the L7 + L8 rendering architecture is now closed-loop: every queued touch-up is in the contract it applies to, not just enumerated in the originating spec.
- **S7.5 (contract):** L7 Web Renderer. Initial contract written 2026-05-10 (914 lines) as the second concrete renderer alongside S7.4 KDE — both consume the L7 foundation triple (S7.1 + S7.2 + S7.3) plus L8 GPU model. Proto IDL with `aios.renderer.web.v1alpha1` package and `WebRenderer` gRPC service (`RenderTree`, `OpenRecoveryPage`, `RegisterCustomElement`, `GrantExposure`, `RevokeExposure`, `GetRendererInfo`). Closed `WebExposureState` (LOOPBACK / LAN / PUBLIC / RECOVERY), closed `BrowserKind` (CHROMIUM / FIREFOX / SAFARI / OTHER), closed `WebRenderMode` (FULL / DEGRADED / RECOVERY), closed `WebRenderTreeErrorCode` (11 values), closed `ExposureGrantErrorCode` (6 values). Comprehensive compilation table for all 19 S7.2 NodeKinds to DOM/Web Components: VISUALIZATION/STREAM/SURFACE_EMBED hosted via WebGPU `<canvas>` + `wgpu` compiled to wasm; SECURITY_INDICATOR/APPROVAL_PROMPT/EVIDENCE_LINK/AGENT_MESSAGE as Web Components in CHROME shadow root at z-index 9999 (binding INV-020 — survives page-level Fullscreen API). Visual token compilation per S7.3 §8.2: Color → CSS custom properties on `:root` overridden in shadow roots; Typography → CSS @font-face + custom property aliases; Spacing → CSS variables; Motion → CSS transition/animation duration + easing variables with `MOTION_DURATION_INSTANT` hard-fixed to 0 ms even against malicious theme values via `!important` override; Icons → SVG sprite from `/aios/system/themes/<theme_id>/icons/sprite.svg`; constitutional icons reference root-signed asset only. **Subdomain-based per-group origins** `<group_id>.aios.localhost` chosen for browser same-origin GPUAdapter delivery per S8.2 §I4 — each group gets a separate browser origin so per-origin GPUAdapter sandboxing actually isolates groups. **Closed shadow root** (`mode: "closed"`) for chrome blocks extension and page DOM access; coupled with periodic integrity check (renderer's tree-signing service rejects DOM nodes added outside its construction path; mismatch raises `WEB_EXTENSION_INTERFERENCE` evidence) rather than relying on browser containment alone. **Recovery served from separate origin `recovery.localhost`** rather than just a route — leverages browser-level isolation of cookies / IndexedDB / Service Worker scope; AIOS_RECOVERY theme auto-active; APP_SURFACE/STREAM_SURFACE creation rejected with `WEB_RECOVERY_KIND_REJECTED` FOREVER evidence. Localhost-default exposure model binds INV-006: default bind to 127.0.0.1 + ::1; LAN exposure requires policy approval + WEB_LAN_EXPOSURE_GRANTED FOREVER + heartbeat WEB_LAN_EXPOSURE_ACTIVE STANDARD_24M; public exposure requires recovery-mode approval + WEB_PUBLIC_EXPOSURE_GRANTED FOREVER + WEB_PUBLIC_EXPOSURE_FIREWALL_RECORDED FOREVER; **forbidden direct `LAN → PUBLIC` FSM transition** — must downgrade to LOOPBACK first to force constitutional ground state per escalation. HTTPS with self-signed cert for loopback; LAN/public cert from L4 vault broker (deferred). Performance contract: FCP p75 < 1.5 s on localhost; LCP p75 < 2.0 s; INP p75 < 200 ms; CLS < 0.1 constitutional (chrome must not shift, breach emits WEB_RENDERER_CLS_BREACH); WebGPU init p95 < 100 ms; schema compilation p95 < 50 ms per 1000 nodes. Adversarial robustness: page-level Fullscreen API request → AIOS chrome remains visible above fullscreen via shadow root z-index 9999 + `position: fixed`; clickjacking blocked by CSP `frame-ancestors 'self'` + X-Frame-Options DENY; `<iframe>` SURFACE_EMBED restricted by sandbox attribute; theme injection via custom CSS rejected; constitutional icons can only load from AIOS root-signed asset URL with CSP enforcement; Service Worker disabled in recovery mode; per-origin GPUAdapter ensures cross-group GPU isolation; browser extension DOM injection raises evidence and removes offending nodes. Browser support matrix: Chromium/Firefox/Safari current with WebGPU; pre-WebGPU browsers receive 426 Upgrade Required; degraded mode for missing WebGPU still renders structural DOM nodes with placeholder for GPU-bearing kinds. Eight golden fixtures + thirteen metrics with bounded label cardinality (node_id/surface_id/subject_id/group_id/user_id NEVER as labels). Seventeen new evidence record types queued for S3.1 follow-up: 4 FOREVER (WEB_LAN_EXPOSURE_GRANTED, WEB_PUBLIC_EXPOSURE_GRANTED, WEB_RECOVERY_KIND_REJECTED, WEB_PUBLIC_EXPOSURE_FIREWALL_RECORDED), 2 EXTENDED_60M (WEB_RECOVERY_PAGE_LOADED, WEB_RECOVERY_PAGE_EXITED), 11 STANDARD_24M (WEB_RENDERER_STARTED, WEB_RENDERER_DEGRADED, WEB_LAN_EXPOSURE_ACTIVE heartbeat, WEB_EXPOSURE_REVOKED, WEB_EXTENSION_INTERFERENCE, WEB_FULLSCREEN_REQUESTED, WEB_THEME_INJECTION_BLOCKED, WEB_THEME_FALLBACK_USED, WEB_CLIENT_STORAGE_QUOTA_BREACH, WEB_RENDERER_CLS_BREACH, WEB_CONSTITUTIONAL_ELEMENT_REREGISTER_BLOCKED). Two new S2.4 verification primitives queued: `web_renderer_bound_to(host, port)`, `web_chrome_z_index_at_least(value)`.
- **S7.4 (contract):** L7 KDE Plasma Renderer. Initial contract written 2026-05-10 (767 lines) as the first concrete renderer implementation of the L7 foundation triple (S7.1 + S7.2 + S7.3). Proto IDL with `aios.renderer.kde.v1alpha1` package and `KdeRenderer` gRPC service (`RenderTree`, `OpenRecoveryShell`, `RegisterCompositionExtension`, `GetRendererInfo`). Closed `KdeCompilationResultCode` (11 values), closed `KdeRendererMode` (NORMAL/RECOVERY/DEGRADED), closed `KdeWaylandLayer` (BACKGROUND/BOTTOM/TOP/OVERLAY/XDG_SHELL/XDG_SUBSURFACE), closed `KdeDegradationReason` (5 values), closed `RecoveryShellErrorCode` (5 values). Wayland protocol matrix locked: wl_compositor + xdg-shell + wlr-layer-shell + wp-presentation-time + linux-dmabuf-v1 + xdg-output + wlr-foreign-toplevel-management; CompositionZone-to-layer-shell mapping per S7.1 §4.3 (BACKGROUND→layer-shell `background`; CONTENT→`xdg_shell`; OVERLAY→`top`; CHROME→`overlay` which survives fullscreen, binding INV-020). Comprehensive compilation table for all 19 S7.2 NodeKinds to Qt/QML primitives with VISUALIZATION/STREAM/SURFACE_EMBED routing through `wgpu` against per-group `VkDevice` (dmabuf-backed wl_subsurfaces; QQuickItem with embedded QRhiTexture handoff). Visual token compilation per S7.3 §8.1: Color tokens → QPalette + QML PropertyChanges; Typography → QFont; Spacing → QML Layout.margins/spacing; Motion → QPropertyAnimation; Icons → KIconLoader with theme-bound icon set, constitutional icons from AIOS root signed bundle and runtime-re-verified per icon load (catches mid-session disk tampering). KDE Plasma platform integration: KRunner Alt+Space for AIOS goal entry; Plasma widget for dashboard; KDE notifications via org.freedesktop.Notifications + KNotification; Activities support; system tray indicator (always present per INV-020); AIOS theme override scoped to AIOSWindow QML root (Plasma's own surfaces unaltered — coexistence rather than full takeover). Recovery shell as **separate KWin session under `_recovery` user** (not just a layer-shell namespace inside operator session — defense-in-depth around recovery boundary); AIOS_RECOVERY theme auto-active; no APP_SURFACE/STREAM_SURFACE accepted; operator credential entry via dedicated dialog with no Plasma session reachable. KWin scripts loaded **only from AIOS root-signed bundle**, never from user/system KWin script directories — closes major social-engineering surface. Performance: 60Hz frame budget p99 < 16 ms; schema-tree-to-Qt compilation p95 < 50 ms per 1000 nodes; KRunner search response p95 < 100 ms; recovery shell boot rendering p95 < 2 s; degraded fallback Qt-only path when KWin/Wayland unavailable. Eight golden fixtures (normal AIOS evidence viewer rendering, Bevy fullscreen game with chrome above, cross-group VkDevice isolation, recovery aesthetic distinct, KRunner Alt+Space, notifications with constitutional icons, layer-shell rejection of app surfaces, KWin disconnect → degraded fallback). Twelve metrics with bounded label cardinality (zone/kind/result/error_code/layer/dropped_frame_severity as labels; window_id/surface_id/subject_id/group_id NEVER labels). Eleven new evidence record types queued for S3.1 follow-up: `KDE_RENDERER_STARTED` STANDARD_24M, `KDE_RENDERER_DEGRADED` FOREVER, `KDE_FRAME_DROPPED` STANDARD_24M, `KDE_LAYER_SHELL_REJECTED` FOREVER, `KDE_KWIN_SCRIPT_LOADED` STANDARD_24M, `KDE_KWIN_SCRIPT_REJECTED` FOREVER, `KDE_RECOVERY_SHELL_STARTED` FOREVER, `KDE_RECOVERY_KIND_REJECTED_AT_RENDERER` FOREVER, `KDE_PLASMA_THEME_OVERRIDDEN` STANDARD_24M, `KDE_RENDER_FAILED` EXTENDED_60M, `KDE_TOKEN_FALLBACK_USED` STANDARD_24M.
- **S7.3 (contract):** L7 Visual Language. Initial contract written 2026-05-10 (1100 lines), stage 2 of the three-stage visual plan: stage 1 was the four constitutional invariants in L0 (INV-019..INV-022), stage 2 is this token-level taxonomy + theme contract, stage 3 is the concrete theme bundle (specific hex codes, typeface family, icon glyphs) deferred until first renderer prototype exists. Proto IDL with `aios.visual.v1alpha1` package and `VisualLanguageService` gRPC; closed token-name enums covering color (31 slots — action provenance, surface zones, text, semantic state, trust posture, evidence retention class, group/scope/recovery boundaries, borders), typography (17 roles including `TYPOGRAPHY_RECOVERY` and `TYPOGRAPHY_AI_ORIGIN`), spacing (9 slots on theme-supplied base unit + multiplier), motion (11 slots — durations including constitutionally-pinned `MOTION_DURATION_INSTANT = 0 ms` for security indicators, easings including `MOTION_EASING_CRITICAL`), icons (24 slots in three categories: 8 constitutional non-customizable, 4 semantic constrained-customizable, 7 fully customizable action), components (14 distinctive AIOS recipes — 7 constitutional including `COMPONENT_SECURITY_BANNER`/`COMPONENT_RECOVERY_SHIELD`/`COMPONENT_AI_SUBJECT_BADGE`/`COMPONENT_TRUST_INDICATOR`, 6 recognizable adjustable including `COMPONENT_AGENT_MESSAGE`/`COMPONENT_APPROVAL_PROMPT`/`COMPONENT_AUDIT_TRAIL`); closed `DistinctionAxis` enum with 6 axes (HUE/PATTERN/TYPOGRAPHY/OUTLINE/OPACITY/POSITION) for multi-axis distinction model; closed `ThemeKind` enum (5 values — AIOS*FALLBACK/AIOS_DEFAULT/AIOS_RECOVERY/USER_THEME/PUBLISHER_THEME); signed `Theme` bundle `theme*<hex_lower(BLAKE3(jcs(theme)))[:32]>` with constitutional theme validation rules — ΔE distinctness ≥ 25 in CIEDE2000 for AI/human, recovery/normal, trust verified/unverified, evidence permanent/standard pairs; multi-axis distinction (≥ 2 axes for AI/human and trust; ≥ 3 axes for recovery/normal); recovery cross-theme distinctness (`COLOR_BOUNDARY_RECOVERY`ΔE ≥ 30 from every other theme;`TYPOGRAPHY_RECOVERY`distinct family); constitutional icons cannot be replaced (canonical hash check); constitutional component recipes cannot be overridden by themes;`MOTION_DURATION_INSTANT`constitutionally pinned at 0 ms (themes attempting non-zero rejected with`ThemeInvariantViolated`); recovery theme auto-activates in recovery mode and is not user-selectable (`RecoveryThemeImmutable`rejection on user switch attempts); cross-renderer mapping locked — KDE compiles tokens to Qt palette + QML + wgpu shader uniforms, Web compiles to CSS custom properties + WebGPU uniforms, CLI compiles to ANSI + box-drawing + glyphs (constitutional distinctions remain enforceable on monochrome terminals via pattern + typography axes); eight golden fixtures (valid normal-mode theme, theme rejected for indistinct AI/human, colorblind-resilient theme with 3-axis distinction, recovery theme constitutional, constitutional icon substitution rejected, animation timing attempt against INV-020, recovery-namespace squatting rejected, constitutional component override attempt rejected); eight metrics with bounded label cardinality (theme_id/owner/group ids NEVER as labels). Cross-spec touch-ups queued for follow-up consolidation: S2.1 (new`target.theme_kind`and`target.theme_id`query fields), S3.1 (4 new record types`THEME_LOADED`STANDARD_24M,`THEME_REJECTED`EXTENDED_60M,`THEME_SWITCHED`STANDARD_24M,`THEME_INVARIANT_VIOLATED`FOREVER), S2.4 (2 new primitives`theme_satisfies_invariants(theme_id)`, `theme_constitutional_icons_intact(theme_id)`). L7 layer remains `PARTIAL`; foundation triple S7.1 + S7.2 + S7.3 now all `CONTRACT`.
- **S8.2 (contract):** L8 GPU Resource Model. Initial contract written 2026-05-10 (803 lines) as the hardware peer of S7.1 — closes the dangling `gpu_capability_class` reference and gives S3.2 sandbox composition a concrete GPU policy surface to bind to. Proto IDL with `aios.gpu.v1alpha1` package and `GpuResourceService` gRPC (`EnumerateDevices`/`IssueCapabilityBinding`/`RevokeCapabilityBinding`/`AuthorizeDmabufPeer`/`GetGpuResourceInfo`); closed `GpuCapabilityClass` enum (5 values: GPU_PASSIVE_DISPLAY, GPU_BASIC_2D, GPU_RICH_2D, GPU_FULL_3D, GPU_COMPUTE_HEAVY) with concrete VRAM defaults (16/64/256 MiB / 25%-of-VRAM-min-256MiB / 50%-of-VRAM-min-512MiB); closed `QueuePriority` enum (NICE/NORMAL/HIGH); closed `GpuKind` (INTEGRATED/DISCRETE) with class-to-kind routing (GPU_FULL_3D and GPU_COMPUTE_HEAVY prefer DISCRETE; GPU_PASSIVE_DISPLAY and GPU_BASIC_2D prefer INTEGRATED unless policy specifies); closed `CapabilityBindingState` (REQUESTED → ACTIVE → EXPIRED|REVOKED) with Ed25519 signature; per-group VkDevice partitioning on native (one `VkDevice` per active group on host primary GPU; secondary GPUs partitioned similarly); per-origin `GPUAdapter` sandboxing on Web (browser iframe enforcement); 25% per-group VRAM floor default; inactive group VkDevice TTL 5 minutes with reclaim; KWin reserve 256 MiB hard; dmabuf cross-group denied by default (`DmabufCrossGroupForbidden`); `_system:kwin` constitutionally privileged dmabuf peer for all classes producing render output (composes opaquely without copying pixels into another group's VkDevice); per-class peer sets (PASSIVE → kwin only; BASIC/FULL_3D → same `(group, subject)` + kwin; RICH_2D → same group + kwin; COMPUTE_HEAVY → same `(group, subject)` only); fence-counter dmabuf revocation with force-teardown via `RevokeCapabilityBinding(destroy_underlying_vk_device=true)`; hardware capability-lie detection at every binding request (driver claims vs kernel exposure mismatch → `HOST_CAPABILITY_LIE` evidence FOREVER + binding refused); IOMMU-unavailable degraded mode with operator-visible warning; recovery-mode disables Vulkan validation layers (smaller attack surface) emitting `GPU_VALIDATION_DISABLED_RECOVERY` evidence; eight golden fixtures (single-group GPU usage, cross-group VkDevice isolation, cross-group dmabuf denial, VRAM exhaustion fallback, hardware capability lie detected, compositor privileged dmabuf access, recovery-mode validation disabled, multi-GPU routing); twelve metrics with bounded label cardinality (capability_class/result/error_code/gpu_kind/priority as labels; subject/surface/device ids NEVER as labels). Sixteen new evidence record types queued for S3.1 follow-up (10 STANDARD_24M including `GPU_DEVICE_ENUMERATED`/`GPU_DEVICE_DISCONNECTED`/`GPU_VK_DEVICE_CREATED`/`GPU_VK_DEVICE_DESTROYED`/`GPU_DMABUF_GRANTED`/`GPU_DMABUF_DENIED`/`GPU_CAPABILITY_DENIED`/`GPU_VALIDATION_DISABLED_RECOVERY`/`GPU_VALIDATION_ENABLED_NORMAL`/`DRIVER_UNAVAILABLE`; 3 EXTENDED_60M `GPU_BUDGET_EXCEEDED`/`GPU_BUDGET_DOWNGRADED`/`IOMMU_UNAVAILABLE_DEGRADED`; 3 FOREVER `HOST_CAPABILITY_LIE`/`GPU_BINDING_FORGERY`/`GPU_DEVICE_FORCE_RECLAIMED`). Other queued cross-spec touch-ups: S2.3 closed condition fields `target.gpu_capability_class` + `target.gpu_device_kind` plus hard-deny `GpuComputeOutsideAuthorisedClass`; S2.4 primitive `gpu_binding_class()`; S3.2 `SandboxProfile.gpu_policy: GpuPolicy`; S2.1 query field `target.gpu_capability_class`. L8 layer status: SHELL → PARTIAL.
- **S7.2 (contract):** L7 Shared UI Schema. Initial contract written 2026-05-10 (1037 lines). Proto IDL with `aios.ui.v1alpha1` package and `UISchemaService` gRPC; closed `NodeKind` enum with 19 values (TEXT, LIST, FORM, CARD, TABLE, VISUALIZATION, STREAM, SURFACE*EMBED, CONTAINER, SECURITY_INDICATOR, APPROVAL_PROMPT, EVIDENCE_LINK, AGENT_MESSAGE, ACTION_BUTTON, DIVIDER, SPACER, HEADING, INLINE_CODE, CODE_BLOCK); render-target-agnostic — same tree compiles to DOM/Qt/CLI; trust-bearing kinds (SECURITY_INDICATOR, APPROVAL_PROMPT, EVIDENCE_LINK) constitutionally cannot be authored by AI subjects (issuer's tree-signing service refuses to sign trees containing those kinds when issuer is `AI_AGENT`); `is_ai_origin` and `is_trust_bearing` and `recovery_session_required` flags signer-controlled (not author-controlled) keeping INV-021 unforgeable; `AGENT_MESSAGE` payload validated to forbid trust-bearing descendants (prevents AI smuggling fake security indicators inside messages); closed `LayoutMode` (7 values), closed `SizeMode` (4), closed `AccessibilityRole` (18), closed `FormFieldKind` (7), closed `ApprovalChoice` (3 — ACCEPT/REJECT/DEFER), closed `RenderTreeErrorCode` (12); subtree Ed25519 signature with rejection on tamper (no partial render); `node_id = uin*<ulid>`stable across re-renders preserving focus/scroll/accessibility tree continuity; LIST/TABLE data binding to S2.1 query views with 10 000 cardinality cap and cursor pagination beyond; FORM submission produces S0.1 action envelope where`submitter` is the active session subject (never the tree's issuer when they differ — accountability separated from authorship); recovery-mode discipline (`recovery_only`rejected outside recovery; recovery_only branches in normal-mode tree dropped silently with`recovery_node_dropped_total`telemetry); per-tree 10 000-node bound preventing schema-flood DoS; eight golden fixtures and bounded-cardinality telemetry. Three new evidence record types queued for S3.1 vocabulary follow-up:`UI_TREE_VALIDATION_REJECTED`STANDARD_24M,`UI_TRUST_BEARING_AUTHORSHIP_REFUSED`FOREVER,`UI_RECOVERY_NODE_DROPPED`STANDARD_24M. Two new S2.4 verification primitives queued:`tree_contains_kind(tree_id, kind)`and`tree_max_depth(tree_id)`. Spec is purely structural — no colors, fonts, spacing values, motion, or visual treatment defined; that's L7.3 visual language.
- **S7.1 (contract):** L7 Surface + Composition Model. Initial contract written 2026-05-10 (723 lines) as the foundation under all L7 renderers. Proto IDL with `aios.surface.v1alpha1` package and `SurfaceService` gRPC (`CreateSurface`/`DestroySurface`/`PauseSurface`/`ResumeSurface`/`GetSurface`/`ListSurfaces`/`GetSurfaceServiceInfo`); closed `SurfaceKind` enum with four values (`AIOS_SURFACE` rendered by L7.2 UI schema, `APP_SURFACE` for app-provided renderers like games/CAD/Bevy/Wine, `STREAM_SURFACE` for live media, `XR_SURFACE` reserved-but-deferred); closed `CompositionZone` enum (`BACKGROUND`/`CONTENT`/`OVERLAY`/`CHROME`) with strict z-ordering and per-zone allowed-kind matrix (CHROME exclusively `AIOS_SURFACE` per INV-020; BACKGROUND and OVERLAY also `AIOS_SURFACE`-only); five-state Surface lifecycle FSM (`DRAFT → ACTIVE → PAUSED → RETIRED → DESTROYED`) with allowed transitions table; capability-brokered GPU access via `gpu_capability_class` reference into L8 GPU resource model (concurrent draft) — five classes `GPU_PASSIVE_DISPLAY` / `GPU_BASIC_2D` / `GPU_RICH_2D` / `GPU_FULL_3D` / `GPU_COMPUTE_HEAVY`; cross-group surface pixel-readback isolation enforced at GPU layer (per-group `VkDevice` on native, per-origin `GPUAdapter` on Web) consistent with S4.1 default-deny; AIOS chrome unbreakable from app surfaces (KWin `wlr-layer-shell` overlay layer on KDE survives fullscreen; shadow-root z-index 9999 on Web survives page fullscreen); recovery-mode surface stack constraint binding INV-022 (only `AIOS_SURFACE` permitted in recovery; `APP_SURFACE`/`STREAM_SURFACE` rejected with `RecoveryModeKindForbidden` and FOREVER-retained `RECOVERY_KIND_REJECTED` evidence); per-subject `max_active_surfaces_per_subject` quota (default 32) preventing surface-flood DoS; surface-id forgery rejection via Ed25519 signing by renderer; same `wgpu` Rust crate compiles to native (Vulkan/Metal/DX12) for KDE and to wasm+WebGPU for Web — apps written in `wgpu` get cross-target portability for free; eight golden fixtures (AIOS evidence viewer surface, Bevy game APP_SURFACE on KDE, NeuroCAD APP_SURFACE on KDE+Web showing wgpu unification, cross-group readback denied, recovery rejection of APP_SURFACE, chrome above fullscreen game, surface-id forgery rejected, surface-flood quota enforcement); seven new evidence record types added to S3.1 vocabulary (`SURFACE_CREATED` STANDARD_24M, `SURFACE_DESTROYED` STANDARD_24M, `SURFACE_GPU_BUDGET_EXCEEDED` EXTENDED_60M, `CROSS_SURFACE_READ_DENIED` FOREVER, `CROSS_ZONE_VIOLATION_ATTEMPTED` EXTENDED_60M, `RECOVERY_KIND_REJECTED` FOREVER, `SURFACE_NEVER_RENDERED` STANDARD_24M); twelve metrics with bounded label cardinality (surface_id/owner/group/namespace_path NEVER as labels). L7 layer status moves from `SHELL` to `PARTIAL`; downstream sub-specs (L7.2 schema, L7.3 visual language, L7.4 KDE renderer, L7.5 Web renderer, L7.6 CLI) renumbered in overview to make room for L7.1 foundation.
- **L0 invariants extension (applied 2026-05-10):** Four new constitutional invariants added to `04_invariants.md` for AIOS visual identity, locking in the constitutional layer of the L7 rendering architecture before any renderer spec is written. INV-019 visual identity preserved across renderers (KDE/Web/CLI/Voice/Mobile cannot invent their own chrome or trust UX), INV-020 trust indicators cannot be hidden by app surfaces (security indicators always visible above CHROME zone), INV-021 AI vs human action visually distinct (constitutional treatment separation that no theme or accessibility profile can collapse), INV-022 recovery mode aesthetically distinct from normal mode (operator must instantly recognize they are in recovery to prevent the "I thought I was rehearsing" failure mode that pairs catastrophically with INV-012). Closed `InvariantId` enum grows from 18 to 22 entries; four new S2.4 `PropertyType` enum entries queued for follow-up touch-up (`RENDERER_VISUAL_IDENTITY_PRESERVED`, `TRUST_INDICATORS_ALWAYS_VISIBLE`, `AI_HUMAN_VISUAL_DISTINCTION`, `RECOVERY_AESTHETIC_DISTINCT`) bringing the closed enum from 16 to 20 entries. See [DEC-018](02_design_decisions.md).
- **S6.1 / S6.2 / S6.4 (contract):** L0 Governance bundle — status taxonomy + evidence grades + constitutional invariants. Initial contracts written 2026-05-09 as the bottom of the dependency stack. **S6.1 status taxonomy** (466 lines): closed `CapabilityStatus` enum with eight values (`REAL`/`PARTIAL`/`SHELL`/`CONTRACT`/`DEFERRED`/`BLOCKED`/`UNKNOWN`/`RETIRED`); per-status definition with required minimum evidence grade; partial order on the capability axis (`SHELL < PARTIAL < REAL`) with `CONTRACT`/`DEFERRED`/`BLOCKED`/`UNKNOWN`/`RETIRED` off-axis; allowed transitions table (16 valid transitions); forbidden transitions explicitly rejected (`SHELL→REAL`, `CONTRACT→REAL`, `RETIRED→anything`); ten gates (`G1`–`G10`) with deterministic check functions; AI subjects rejected from gate G6 for capabilities they authored (`AISelfStatusPromotionBlocked` — L0 mirror of S2.3 §17); automatic demotion on regression (`G8`) and dependency status change (`G7`); layer/bundle headline status computed from sub-specs, not authored; status carrying via new `STATUS_TRANSITION` evidence record (STANDARD*24M); six golden fixtures and bounded-cardinality telemetry. **S6.2 evidence grades** (385 lines): closed `EvidenceGrade` enum with six values (`E0`–`E5`); per-grade artifact requirements (E1 artifact exists, E2 build/typecheck, E3 unit/integration test, E4 e2e/recovery/release-gate, E5 live operational with rolling 7-of-14d health receipts); grade subsumption (`E5 ⊃ E4 ⊃ E3 ⊃ E2 ⊃ E1`) enforced at verification; grade-to-status mapping (REAL requires E3, recovery-critical/release-gate capabilities require E4); seven new evidence record types added to S3.1 vocabulary (`ARTIFACT_RECORDED`, `BUILD_PASSED`, `TEST_PASSED`, `E2E_PASSED`, `RECOVERY_REHEARSAL_PASSED` FOREVER, `RELEASE_GATE_PASSED` FOREVER, `OPERATIONAL_HEALTHY`); closed `ArtifactKind` enum with thirteen values; AI subjects cannot emit grade-promotion receipts for code they authored (`ProducerCannotSelfGrade`); recovery-critical bundle (`reccritical*<hex>`) gates `E4`requirement; six golden fixtures and bounded-cardinality telemetry. **S6.4 constitutional invariants** (431 lines): closed`InvariantId` enum with 18 values cataloging the AIOS constitution (recovery independent of L5, AI proposes never executes, secrets are capabilities, recovery boundary preserved, evidence append-only, web UI localhost default, layer downward dependency, default deny, approval bound and expiring, AI self-approval blocked, cross-group access forbidden, recovery required for system mutation, AI system admin blocked, no proof no completion, evidence never contains secrets, AI cannot self-grade, sandbox floor constitutional, vault no raw secret leak); per-invariant Statement / Why / Enforced by / Verified by / Cannot be loosened by; signed invariant bundle (`invbundle\_<hex>`) with degraded-mode fallback on signature failure (only INV-001 + INV-002 active); six new properties added to S2.4 closed `PropertyType` enum (`FILESYSTEM_BOUNDARY_INTACT`, `WEB_UI_LOCALHOST_BOUND`, `APPROVAL_BOUND_AND_EXPIRING`, `RECOVERY_GATED_SYSTEM_MUTATIONS`, `AI_NEVER_SYSTEM_ADMIN`, `EVIDENCE_NO_SECRET_LEAK` — total now 16); three new evidence record types added to S3.1 vocabulary (`INVARIANT_BUNDLE_LOADED`FOREVER,`INVARIANT_RETIRED`FOREVER,`WEB_EXPOSURE_GRANTED`FOREVER); invariant retirement requires recovery mode + HUMAN_USER + FOREVER-retained evidence; five golden fixtures and bounded-cardinality telemetry. L0 layer status moves from`SHELL`to`PARTIAL`(3 of 4 sub-specs`CONTRACT`).
- **S5.1 (contract):** L4 Identity Model. Initial contract written 2026-05-09 against S4.1 §12.8 constraints: proto IDL with `aios.identity.v1alpha1` package and `IdentityService` gRPC service (`AuthenticateSubject`/`ResolveSubject`/`EnumerateMemberships`/`SwitchPrimaryGroup`/`IssueCapabilityBinding`/`RevokeCapabilityBinding`/`GetIdentityInfo`); closed `SubjectKind` taxonomy with seven kinds (`HUMAN_USER`, `AI_AGENT`, `APPLICATION`, `SERVICE`, `DEVICE`, `WORKFLOW`, `REMOTE_OPERATOR`); canonical subject id format `<group_id>:<kind_segment>[:<inner>]` with kind-specific inner shape (e.g., `family:alice`, `family:family-assistant`, `family:alice:diary-helper` for personal agent, `_system:service:translator`, `_system:remote:operator-247`); subject id immutability for the lifetime of the evidence trail (renaming requires retirement + new id); group as first-class identity unit with closed `GroupTier` enum (`PERSONAL`/`TEAM`/`ORGANIZATIONAL`/`FINANCE`); group registration only via recovery-mode action emitting `GROUP_REGISTERED` evidence with FOREVER retention; multi-group membership model with exactly one `primary_group_id` per session and `SwitchPrimaryGroup` requiring re-authentication and emitting `PRIMARY_GROUP_SWITCHED` evidence; recovery mode constitutionally separated (no path from normal to recovery except by reboot; 8-hour hard cap; recovery subjects always under `_system` scope); closed `SessionClass` enum (`PUBLIC`/`INTERACTIVE`/`STRONG`/`RECOVERY`/`SERVICE`) with per-class TTL budgets; closed `RiskFlagKind` with seven flags including `ELEVATED_PRIVILEGE_REQUEST` always set on AI subjects acting outside their home scope; `is_ai` constitutional and signed at registration (subjects cannot tamper); AI subjects rejected from `RECOVERY` session class (`AISubjectCannotEnterRecovery`); `NormalizedSubject` produced for S2.3 §3 normalization stage with Ed25519 signature; capability bindings scoped to `(subject, group_id, identity_bundle_version)` with vault broker enforcement at use; signed identity bundle `idbundle_<hex>` with degraded-mode fallback on signature failure; six golden fixtures (household authentication, multi-group primary switch, recovery operator session, AI agent rejected from recovery, forged signature rejection, capability binding scope enforcement) and bounded-cardinality telemetry (subject/group/user/session ids never as labels). Open deferrals: vault broker API, approval mechanics, emergency override mechanics, cross-machine federation, subject delegation, hardware-pinned device attestation, identity bundle hot-reload mid-session.
- **S4.1 (contract):** AIOS-FS namespace layout. Promoted 2026-05-09 from `DRAFT NOTES` to `CONTRACT` with the user-approved defaults from the 2026-05-08 brainstorm: proto IDL with `aios.namespace.v1alpha1` package and `NamespaceResolver` gRPC service (`ResolvePath`/`ListReservedNames`/`GetNamespaceInfo`); closed top-level vocabulary (`/aios/system/`, `/aios/groups/`); closed reserved-name enums at every scope (`SystemReservedName` with eight names, `GroupReservedName` with eleven names including three virtual views, `UserReservedName` with eight names including two virtual views); per-segment ID regex `[a-z][a-z0-9_-]{0,62}` with reverse-DNS form for app IDs and reserved `_*` prefix; deterministic stateless path resolution algorithm producing typed `NamespacePath`; cross-group access **forbidden by default** as a constitutional hard-deny (`CrossGroupAccessForbidden`) with a single exception path (`_system` scope + recovery mode + `system_audit_read` capability + human approver); recovery-only mutation boundary for `/aios/system/policy/`, `/aios/system/capabilities/`, `/aios/system/vault/`, `/aios/system/recovery/` (`RecoveryRequiredForSystemMutation` hard-deny); AI subjects rejected from system-admin operations even with `system_admin` capability (`AISystemAdminBlocked` hard-deny extending S2.3 §17); both-tier inbox semantics (group inbox + per-user inbox, both as virtual S2.1 named views); namespace catalog versioning `nscat_<hex_lower(BLAKE3(jcs(catalog_descriptor)))[:32]>` with Ed25519 root signature and degraded-mode-on-signature-failure fallback; performance budget p99 < 100 µs for parse-only resolution; adversarial robustness (path traversal rejected at parse time, ASCII-only segments blocking homograph attacks, 32-segment depth cap, 4096-byte path length cap, no regex backtracking, content-addressed group registration via S1.3 CAS); cross-spec touch-up requirements explicitly enumerated for S0.1 (target validation), S1.3 (`ScopeBinding` field, no cross-scope pointer moves), S2.1 (new query fields, formal inbox/outbox views), S2.3 (three new constitutional hard-denies, five new condition fields), S2.4 (`aiosfs_path_in_namespace` primitive, `NAMESPACE_NO_CROSS_GROUP_POINTERS` property), S3.1 (records carry `(scope, group_id, user_id)`, two new record types `SYSTEM_ADMIN_OPERATION` and `CROSS_GROUP_ACCESS_DENIED`), S3.2 (new `GROUP_FLOOR` composition source between policy and runtime floor, apply-time group ownership check), plus L4 (subject canonical id format including group binding), L5 (agent objects carry `group_owner` and nullable `user_owner`), L6 (`installable_scope` manifest field — `SYSTEM_ONLY`/`GROUP_ONLY`/`EITHER`); six golden fixtures (household, solo dev with homelab, mixed work+personal with stricter floor, path traversal rejection, catalog version stamping, inbox virtual view) and bounded-cardinality telemetry (subject/group/user/action/profile/agent/app ids never appear as labels). Scope: closed namespace, default-deny cross-group, recovery-only admin path, virtual inbox views, catalog-versioned determinism.
- **S3.2 (contract):** Sandbox composition language. Refined 2026-05-08 with deltas D1–D12: proto IDL with `aios.sandbox.v1alpha1` package and `SandboxComposer` gRPC service (`ComposeProfile`/`ValidateProfile`/`ApplyProfile`/`RevokeProfile`/`ExplainComposition`/`GetComposerInfo`); typed `SandboxProfile` shape with closed enum vocabulary (`FilesystemMode`, `NetworkMode`, `SecretsMode`, `StreamCapture`, `LinuxCapability`, `CompatibilityKind`); deterministic five-source composition algorithm (adapter default → app manifest → user request → policy required → runtime safety floor) with strictness-ranked merge per field type, per-field winner trace recorded in `CompositionMetadata.field_decisions`; runtime safety floor as constitutional minimum (per-class floors for human/AI/recovery subjects) cannot be loosened by any source; backend capability detection via host probe with `BackendCapabilities` snapshot, required-backend mapping per profile field, and fail-closed-or-degraded path gated by policy `allow_degraded` plus `approved_fallbacks`; profile lifecycle (DRAFT → VALIDATED → APPLIED → ENFORCING → REVOKED) with content-addressed `prof_<hex_lower(BLAKE3(...))[:32]>` ids and post-apply immutability; determinism contract `(adapter_default_id, manifest_hash, jcs(user_request), policy_decision_id, floor_id, host_capability_snapshot_id, composer_version) → identical SandboxProfile`; performance budgets (compose p95 < 5 ms fresh / < 1 ms cached, native apply p95 < 50 ms, Wine prefix bring-up p95 < 2 s, Waydroid container start p95 < 5 s, VM fallback boot p95 < 30 s); compatibility runtime profiles (Wine/Proton with isolated prefix and host home block, Waydroid with per-app data and brokered clipboard/portals, VM fallback with explicit storage shares and evidence bridge); evidence integration with six new record types added to S3.1 (`SANDBOX_PROFILE_COMPOSED`, `SANDBOX_PROFILE_APPLIED`, `SANDBOX_PROFILE_REVOKED`, `SANDBOX_DEGRADED_GRANTED` with FOREVER retention, `SANDBOX_APPLICATION_FAILED`, `SANDBOX_CAPABILITY_LIE`); adversarial robustness (omitted fields resolve to most-restrictive defaults — never permissive; permissive caller hints silently dropped with recorded drop reason; circular reference detection via DAG check; composition budget ≤ 100 fields / ≤ 1000 allowlist entries / ≤ 10 KB; per-subject rate limits; apply-time capability re-probe detects host lies and emits `TAMPER_DETECTED`); ten golden fixtures and bounded-cardinality telemetry (subject/app/action/profile ids never appear as labels). Scope unchanged: deterministic composition across policy/app/adapter/runtime constraints and Linux enforcement backends.

## Active sub-specs

See [00_MASTER_INDEX.md](00_MASTER_INDEX.md) for the full roadmap.

## Out of scope for rev.2

- Subject identity canonical format (deferred to L4 `03_identity_model.md`)
- Saga / batching composition of actions
- Approval delivery mechanics (deferred to L4 `04_approval_mechanics.md`)
- TTL / expiration policy on queued actions
- Resource budget hints

## See also

- [Rev.1 (frozen) — vision and canonical spec](../001.AI-OS.NET--SPECREV.1/00_MASTER_INDEX.md)
